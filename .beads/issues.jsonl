{"_type":"issue","id":"convfinqa-0hu.10","title":"Resolve shadcn CLI vs frontend/pnpm-workspace.yaml packages:[.] conflict","description":"## Conflict\n\nshadcn CLI v4.7 refuses every command (init / add / info / view) when `frontend/pnpm-workspace.yaml` contains `packages: [.]`, with:\n\n\u003e It looks like you are running \u003ccmd\u003e from a monorepo root.\n\u003e To use shadcn in a specific workspace, use the -c flag:\n\u003e   shadcn \u003ccmd\u003e -c .\n\n`-c .` does not help — the resolved workspace path equals the monorepo root, and shadcn expects a SUBDIRECTORY workspace (apps/web, packages/ui, etc.).\n\nDiagnostic (proven, repeatable):\n- Move `pnpm-workspace.yaml` aside → shadcn works perfectly (detects Vite, TS, Tailwind v4, src dir, globals.css path).\n- Re-add file with ONLY `minimumReleaseAge: 10080` + `blockExoticSubdeps: true` (no `packages` key) → shadcn still works, supply-chain hardening still applies.\n- Re-add `packages: [.]` → shadcn gate triggers again.\n\nSo the trigger is specifically the `packages:` key, not the file itself.\n\n## Why this matters\n\nEvery future slice that copies a shadcn component (rb3, dj6, bqx, 9ye, ebw, fxn, uf5, bmu) does `pnpm dlx shadcn@latest add \u003ccomponent\u003e` and will hit this gate. As a workaround, 0hu.4 used a temporary `mv pnpm-workspace.yaml ...bak \u0026\u0026 shadcn add \u0026\u0026 mv ...back` dance — that's fragile and undiscoverable for the next agent.\n\n## Rule under question\n\n`.claude/rules/frontend/package-manager.md` currently says `frontend/pnpm-workspace.yaml` MUST include `packages: [.]`. The rule's stated rationale is supply-chain (minimum-release-age + block-exotic-subdeps), not the `packages` declaration. `packages: [.]` is functionally inert for a single-package project.\n\n## Options\n\n1. **Drop `packages: [.]`.** Update the local rule to require only `minimumReleaseAge` + `blockExoticSubdeps`. Least invasive. shadcn works directly. supply-chain hardening preserved.\n2. **Move workspace yaml to convfinqa/ root** with `packages: [frontend]`. Frontend becomes a real workspace member of a parent monorepo. `pnpm dlx shadcn@latest add -c frontend` from repo root works. Bigger blast radius — touches repo-root files, may affect backend tooling.\n3. **Keep the rule as-is + a wrapper script** that does the mv-dance around every shadcn call. Brittle, easy to forget.\n\n## Recommendation\n\nOption 1. The rule's intent is supply-chain hardening, which `packages: [.]` does not contribute to.\n\n## Acceptance\n\n- Rule text updated (or explicit decision recorded in beads notes / git history)\n- `pnpm dlx shadcn@latest add \u003cany\u003e` works from frontend/ with no workarounds\n- 0hu.4 base infrastructure (components.json + lib/utils.ts) untouched","status":"closed","priority":1,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T23:13:30Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T23:21:18Z","started_at":"2026-05-10T23:18:35Z","closed_at":"2026-05-10T23:21:18Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.10","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-11T00:13:30Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.9","title":"Migrate frontend from Bun to pnpm + supply-chain hardening","description":"## Problem\n\nconvfinqa-0hu.1 scaffolded the frontend with Bun (`bun create vite`, `bun.lock`, ESLint set up by Vite's React-TS template). This **violates the global rule** at `~/.claude/rules/frontend/package-manager.md`:\n\n\u003e Use pnpm \u003e= 10.28.0; set `minimum-release-age=10080` (1 week); always use pnpm, never npm or yarn.\n\nThe PRD line 'Package manager: Bun' in the `convfinqa-0hu` epic was authored before frontend files existed, so the rule's `paths:` glob (`**/package.json`, `**/.npmrc`, `**/pnpm-workspace.yaml`) didn't attach and the decision slipped through.\n\n## Solution\n\nRip out Bun, replace with pnpm 10.x. Add supply-chain hardening: 1-week minimum release age + block exotic sub-deps. Both settings live in `pnpm-workspace.yaml` (pnpm 10.x supports global `catalog`/settings there).\n\n## Tasks\n\n### Remove Bun\n- `cd frontend \u0026\u0026 rm bun.lock`\n- Remove any `packageManager` / `engines.bun` field from `frontend/package.json` if present\n- Drop `bun` from `.gitignore` if specifically referenced (keep node_modules etc.)\n\n### Install pnpm\n- Local: `corepack enable \u0026\u0026 corepack prepare pnpm@latest-10 --activate` (don't pin a specific patch; let the lockfile drive)\n- `cd frontend \u0026\u0026 pnpm install` to regenerate the lockfile (`pnpm-lock.yaml`)\n- Add `packageManager\\: 'pnpm@10.x.y'` to `frontend/package.json` after lockfile generation so CI uses the same version via `corepack`\n\n### Supply-chain hardening (`frontend/pnpm-workspace.yaml`)\n```yaml\npackages:\n  - .\nminimumReleaseAge: 10080         # 1 week, in minutes (pnpm 10.x)\nblockExoticSubdeps: true         # block git/tarball/file: sub-dependencies\n```\n\nNote: `minimum-release-age=10080` in `.npmrc` and `minimumReleaseAge: 10080` in `pnpm-workspace.yaml` are equivalent in pnpm 10.x. Prefer the workspace YAML (single source of truth, version-controlled, applies to whole workspace).\n\n### Edit epic description\n- Strip 'Package manager: Bun' line from `convfinqa-0hu` epic description + Further Notes\n- Replace all `bun ...` examples in user stories with `pnpm ...` equivalents\n- Update `Codebase Patterns` note that calls out `~/.bun/bin/bun`\n\n### Downstream tickets\n- **0hu.7** (Makefile + CI): change `frontend-install` to `cd frontend \u0026\u0026 pnpm install --frozen-lockfile`; `frontend-dev` → `pnpm dev`; same for test/e2e. CI: replace `oven-sh/setup-bun@v2` with `pnpm/action-setup@v4` (uses `packageManager` field); cache key on `pnpm-lock.yaml`.\n- **0hu.8** (Biome + Lefthook): re-spec `bun add -D ...` → `pnpm add -D ...`; `bunx` → `pnpm exec` / `pnpm dlx`.\n- **0hu.2–0hu.6**: any installs run via pnpm (no per-ticket text change needed if they re-read this issue first).\n\n## Acceptance\n\n- `cd frontend \u0026\u0026 pnpm install --frozen-lockfile` succeeds from a clean checkout.\n- `pnpm dev` boots the SPA on :5173; `pnpm build` produces `dist/`; existing smoke test still passes.\n- No `bun.lock` / `bunfig.toml` / `bun` references in `frontend/`.\n- `pnpm-workspace.yaml` contains both `minimumReleaseAge` and `blockExoticSubdeps`.\n- Attempting `pnpm add some-package-released-yesterday` is refused by the release-age gate.\n\n## Follow-up (separate issue, do NOT fix here)\n\n- Broaden `~/.claude/rules/frontend/package-manager.md` `paths:` glob so the rule attaches when a frontend dir is created (e.g. `**/vite.config.*`, `**/bun.lock`). Otherwise the next PRD-writer agent will repeat the same mistake.","status":"closed","priority":1,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T22:25:32Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T22:33:18Z","started_at":"2026-05-10T22:29:15Z","closed_at":"2026-05-10T22:33:18Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.9","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T23:25:32Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":7,"comment_count":0}
{"_type":"issue","id":"convfinqa-tvq.2","title":"Streaming /v1/chat/stream (Vercel AI SDK v5 UI Message Stream)","description":"## Parent PRD\n\nEpic convfinqa-tvq\n\n## What to build\n\nAdd the streaming endpoint that consumes the SAME SendMessageUseCase.stream() generator as the sync endpoint, mapping each StreamEvent to a Vercel AI SDK v5 UI Message Stream SSE frame.\n\nIncludes:\n- POST /v1/chat/stream returning StreamingResponse(media_type='text/event-stream') with response headers x-vercel-ai-ui-message-stream: v1, Cache-Control: no-cache, Connection: keep-alive.\n- Frame mapping per PRD wire contracts: start, data-conversation, text-start, text-delta*, text-end, finish, [DONE]; on error emit data: {\"type\":\"error\",\"errorText\":\"...\"} then [DONE].\n- Refactor sync_chat to consume the same generator (proves no-drift abstraction).\n- Streaming tests via httpx.AsyncClient + asgi-lifespan LifespanManager.\n- Tests: happy-path frame ordering, mid-stream LLM error -\u003e error frame + [DONE] + INTERRUPTED in DB, client disconnect mid-stream -\u003e INTERRUPTED in DB, response headers presence, provider-portability smoke (Gemini-shaped fake chunks).\n\n## User stories addressed\n\n2, 4, 7, 8, 12","acceptance_criteria":"- [ ] curl --no-buffer -X POST /v1/chat/stream emits start, data-conversation, text-start, \u003e=1 text-delta, text-end, finish, [DONE]\n- [ ] Response headers include text/event-stream and x-vercel-ai-ui-message-stream: v1\n- [ ] data-conversation.data.conversationId matches a row that persists in DB\n- [ ] LLM mid-stream error emits error frame + [DONE]; DB has assistant msg with stop_reason=INTERRUPTED\n- [ ] Client disconnect mid-stream persists assistant msg with stop_reason=INTERRUPTED\n- [ ] Sync /v1/chat still passes (uses the same generator now)\n- [ ] Provider-portability smoke test passes with Gemini-shaped fake chunks","status":"closed","priority":1,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-09T22:52:15Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T00:18:17Z","started_at":"2026-05-09T23:59:56Z","closed_at":"2026-05-10T00:18:17Z","close_reason":"streaming /v1/chat/stream green: 4 streaming tests + 6 sync tests pass; AI SDK v5 wire frames verified against ai-sdk.dev primary source; hexagonal layering audit clean; pyright + ruff clean","labels":["afk","milestone:chat-slice-1"],"dependencies":[{"issue_id":"convfinqa-tvq.2","depends_on_id":"convfinqa-tvq","type":"parent-child","created_at":"2026-05-09T23:52:15Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-tvq.2","depends_on_id":"convfinqa-tvq.1","type":"blocked-by","created_at":"2026-05-09T23:52:15Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-tvq.1","title":"Foundation + sync /v1/chat (Postgres tracer bullet)","description":"## Parent PRD\n\nEpic convfinqa-tvq\n\n## What to build\n\nEnd-to-end tracer bullet for the synchronous chat path. After this slice, `curl -X POST /v1/chat -H 'X-User-Id: dev' -d '{\"message\":\"hi\"}'` returns Anthropic-shaped JSON and persists user+assistant rows to Postgres.\n\nIncludes:\n- Pyproject deps: sqlalchemy[asyncio]\u003e=2.0, asyncpg\u003e=0.30, alembic\u003e=1.13, httpx==0.28.1, python-json-logger==4.1.0, dev: testcontainers[postgresql]\u003e=4, asgi-lifespan\u003e=2.\n- docker-compose.yml with postgres service.\n- .env.example: LLM_MODEL, SYSTEM_PROMPT, DATABASE_URL, AWS_PROFILE, AWS_REGION.\n- Settings extended (llm_model, system_prompt, database_url).\n- src/convfinqa/logging.py with python-json-logger configure_logging().\n- src/convfinqa/domain/{entities.py, value_objects.py, ports/{llm.py, repository.py}}: pure dataclasses + Protocols, no framework imports.\n- src/convfinqa/adapters/persistence/sqlalchemy/{models.py, engine.py, repository.py}.\n- alembic/ + alembic.ini + initial migration creating conversations + messages tables (with role CHECK and indexes).\n- src/convfinqa/adapters/llm/litellm_adapter.py wrapping litellm.acompletion(stream=True, stream_options={'include_usage': True}). Maps delta + final usage chunk.\n- src/convfinqa/application/use_cases/send_message.py: SendMessageUseCase.stream() async generator that persists user msg, calls LLM, buffers assistant text, persists with stop_reason on finish or INTERRUPTED on error.\n- src/convfinqa/entrypoints/api/errors.py: RFC 9457 Problem model + global exception handlers that JSON-log via python-json-logger.\n- src/convfinqa/entrypoints/api/chat.py: chat_router prefix=/v1, current_user_id dep extracting X-User-Id, sync_chat consumes use case generator and concatenates deltas.\n- Container extended with llm, conversations, send_message; main.py wires logging + chat_router + engine disposal.\n- tests/conftest.py session-scoped Postgres testcontainer + per-test transactional rollback + container override.\n- tests/api/test_chat.py covering: happy path, continuation, cross-tenant 404, missing X-User-Id 401, empty message 422, LLM raises 502 with INTERRUPTED partial persist.\n- tests/fakes/llm.py FakeLLMPort with scripted-chunks and raise-mid-stream modes.\n\n## User stories addressed\n\n1, 5, 6, 9, 10, 11, 13, 14, 15, 16, 17, 18, 21, 22, 23, 24, 25, 26","acceptance_criteria":"- [ ] uv sync installs new deps\n- [ ] docker compose up -d postgres starts a healthy Postgres\n- [ ] uv run alembic upgrade head creates conversations + messages tables\n- [ ] curl -X POST localhost:8000/v1/chat -H 'X-User-Id: dev' -d '{\"message\":\"hi\"}' returns Anthropic-shaped JSON with conversation_id\n- [ ] Continuation request with the returned conversation_id sees prior history (4 rows in DB)\n- [ ] Cross-tenant request returns 404 application/problem+json\n- [ ] Missing X-User-Id returns 401 application/problem+json\n- [ ] Empty message returns 422\n- [ ] LLM error returns 502 application/problem+json AND DB has user msg + partial assistant msg with stop_reason=INTERRUPTED\n- [ ] All tests pass under uv run pytest -q tests/\n- [ ] uv run pyright src tests passes\n- [ ] uv run ruff check src tests passes","status":"closed","priority":1,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-09T22:51:59Z","created_by":"Francesco Albanese","updated_at":"2026-05-09T23:08:07Z","started_at":"2026-05-09T22:53:32Z","closed_at":"2026-05-09T23:08:07Z","close_reason":"sync /v1/chat tracer bullet green: 6 integration tests pass, pyright + ruff clean, alembic migration drives schema in fixture","labels":["afk","milestone:chat-slice-1"],"dependencies":[{"issue_id":"convfinqa-tvq.1","depends_on_id":"convfinqa-tvq","type":"parent-child","created_at":"2026-05-09T23:51:59Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-tvq","title":"Bare-minimum chat slice (sync + stream + CLI)","description":"First vertical slice: POST /v1/chat (Anthropic-shaped JSON), POST /v1/chat/stream (Vercel AI SDK v5 UI Message Stream), Typer CLI REPL. Server-stateful conversations in Postgres scoped by X-User-Id header. LiteLLM -\u003e Bedrock Claude Haiku 4.5. Single use-case generator drives both endpoints.","notes":"## Codebase Patterns (added 2026-05-10)\n- SSE presenter for AI SDK v5 UI Message Stream lives in src/convfinqa/entrypoints/api/sse.py; one match-stmt over StreamEvent yields data: \u003cjson\u003e\\n\\n frames; DONE_FRAME constant + UI_MESSAGE_STREAM_HEADERS dict are reused for any future streaming presenter\n- Pre-stream peek pattern: route handlers that return StreamingResponse must call await anext(events) BEFORE constructing StreamingResponse so ConversationNotFoundError (and any pre-yield exception) returns RFC 9457 problem+json instead of leaking after text/event-stream headers are flushed\n- Async-generator GeneratorExit cleanup is testable at the use-case layer via events.aclose(); httpx ASGITransport cannot mid-stream-disconnect, but Starlette's StreamingResponse takes the same aclose() path on real client disconnect — no need to spin up a real server in tests\n- AsyncGenerator (not AsyncIterator) is the right return type when callers may need .aclose() — pyright strict-mode rejects aclose on AsyncIterator\n## Codebase Patterns (added 2026-05-10)\n- ConversationLockPort lives at src/convfinqa/domain/ports/lock.py; impl at src/convfinqa/adapters/persistence/sqlalchemy/lock.py uses pg_try_advisory_xact_lock(hashtextextended(:cid, 0)) with SQLAlchemy text() bound params (no SQL injection)\n- Lock semantics: async with locks.try_acquire(cid) as acquired; the lock-transaction is held for the lifetime of the with block, so use cases that yield inside the with hold a DB connection idle for that duration. Concurrency bounded by SQLAlchemy pool_size + max_overflow.\n- New StreamEvent variants must be handled in BOTH the sync match (chat.py sync_chat) AND the SSE match (sse.py to_ui_message_stream) — pyright's exhaustiveness will catch missing cases. ConcurrentRequest is intercepted by the route's pre-stream peek and never reaches the SSE body, but the SSE match still needs an explicit no-op case for exhaustiveness.\n- ConversationBusyError: route-level exception with a conversation_id field; mapped to 409 application/problem+json with type=https://convfinqa.local/problems/conversation-busy via add_exception_handler in errors.py\n- Concurrent test pattern: FakeLLMPort exposes asyncio.Event gate + stream_started flags. Spawn task A, await stream_started.wait() to know A holds the lock, then spawn task B and assert 409, then gate.set() and assert A finishes 200.","status":"closed","priority":1,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-09T22:49:37Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T00:56:12Z","closed_at":"2026-05-10T00:56:12Z","close_reason":"All children completed","labels":["milestone:chat-slice-1"],"comments":[{"id":"019e0f40-0443-7659-adc7-11380ec680e4","issue_id":"convfinqa-tvq","author":"Francesco Albanese","text":"# 2026-05-10 — milestone:chat-slice-1\n\n- **Task completed**: convfinqa-tvq.2 Streaming /v1/chat/stream (Vercel AI SDK v5 UI Message Stream)\n- **Files changed**: src/convfinqa/entrypoints/api/sse.py (new SSE presenter), src/convfinqa/entrypoints/api/chat.py (added stream_chat + _prepend), src/convfinqa/application/use_cases/send_message.py (return type AsyncIterator -\u003e AsyncGenerator), tests/api/test_chat_stream.py (4 tests)\n- **Decisions**: SSE presenter is a separate module from chat.py for testability and to keep the route file small; ai-sdk.dev primary source confirmed wire shapes (delta not textDelta, data nested under data key, [DONE] terminator framed as data: [DONE]); pre-stream peek via anext(events) so ConversationNotFoundError returns 404 problem+json BEFORE SSE headers go out; disconnect test exercised at use-case layer (events.aclose()) since httpx ASGITransport cannot mid-stream-disconnect — Starlette's StreamingResponse cancel path is identical\n- **Quality gate findings addressed**: removed duplicate continuation test, renamed provider-portability test for spec traceability, added WHY comment to consumer-aborts test\n- **Architectural decisions**: single-generator rule preserved (both endpoints still consume send_message.stream() directly, no parallel complete() method); Finish.created_at remains the timestamp single source of truth\n- **Notes for next iteration**: imports were globally migrated to convfinqa.* (from src.convfinqa.*) earlier in the same session; rules under .claude/rules/python/hexagonal.md and logging.md updated to match; concurrent-request 409 lock (tvq.3) is the next P2 ready task and now unblocked","created_at":"2026-05-10T00:18:34Z"},{"id":"019e0f53-c17e-7f2a-985c-94db0eefd868","issue_id":"convfinqa-tvq","author":"Francesco Albanese","text":"# 2026-05-10 — milestone:chat-slice-1\n\n- **Task completed**: convfinqa-tvq.3 Concurrent-request 409 lock via pg_try_advisory_xact_lock\n- **Files changed**: src/convfinqa/domain/ports/lock.py (new ConversationLockPort), src/convfinqa/adapters/persistence/sqlalchemy/lock.py (new SqlAlchemyConversationLock), src/convfinqa/application/use_cases/send_message.py (added ConcurrentRequest event + locked LLM call), src/convfinqa/container.py (wires lock), src/convfinqa/entrypoints/api/chat.py (sync + stream map ConcurrentRequest -\u003e 409), src/convfinqa/entrypoints/api/errors.py (ConversationBusyError + handler), src/convfinqa/entrypoints/api/sse.py (moved prepend_event helper here, added exhaustiveness case), src/convfinqa/adapters/persistence/sqlalchemy/engine.py (pool/lock trade-off doc), tests/fakes/llm.py (gate + stream_started events), tests/api/test_chat_concurrent_lock.py (4 new tests)\n- **Decisions made**: separate ConversationLockPort (not folded into ConversationRepository) — single-responsibility; lock-tx held across yields inside the use case generator (one connection per in-flight chat — concurrency bounded by SQLAlchemy pool_size + max_overflow, default 5+10); reordered so ConversationCreated yields INSIDE the lock so stream presenter can peek the first event and convert ConcurrentRequest -\u003e 409 before opening the SSE stream; user message persisted OUTSIDE the lock per PRD body (input preserved even when rejected — AC checkbox text contradicted PRD body, deferred to body per AC's own clarifying note)\n- **Architectural decisions**: kept single async-generator rule (both presenters consume use_case.stream); did NOT fold ConcurrentRequest into ConversationCreated — separate event type means presenters route deterministically via match-stmt + isinstance peek\n- **Quality gate findings addressed**: documented connection-pool trade-off in engine.py; strengthened sync 409 type assertion to exact match; clarified WHY-comment for unreachable ConcurrentRequest branch in sse.py; renamed exception-rollback test to make AC #5 traceability obvious; dropped a connection-drop simulation test that engine.dispose() does not actually exercise (rollback path covers same Postgres release semantics)\n- **Backlog filed**: convfinqa-tw6 — rename ConversationCreated to ConversationResolved (or split into Created vs Resumed); pre-existing misnomer made more visible by the lock-scope reordering\n- **Notes for next iteration**: connection-pool ceiling = pool_size + max_overflow (5+10 default). If concurrent in-flight chats exceed this, requests will queue in SQLAlchemy's pool acquire — not deadlock, just slow. Tune in engine.py if needed. Remaining tasks in milestone: tvq.4 (Typer CLI REPL), tvq.5 (docs).","created_at":"2026-05-10T00:40:08Z"},{"id":"019e0f5c-0d40-7b71-b33f-a2a488d05496","issue_id":"convfinqa-tvq","author":"Francesco Albanese","text":"# 2026-05-10 — milestone:chat-slice-1\n\n- **Task completed**: convfinqa-tvq.4 Typer CLI REPL convfinqa chat\n- **Files changed**: src/convfinqa/entrypoints/cli/app.py (new, 132 lines), tests/cli/test_smoke.py (new, --help smoke test), pyproject.toml ([project.scripts] convfinqa entry added alongside main)\n- **Decisions made**: `@app.callback()` with docstring main(ctx) keeps `chat` as a subcommand instead of letting Typer collapse a single-command app to root (cleaner than pyright-ignored empty callback); --user-id default resolved at runtime via _default_user_id() rather than default_factory so --help does not bake in the installer's $USER; --system flag exposed in --help (AC requirement) but emits a one-time stderr 'reserved and ignored' notice on use since ChatRequest does not yet accept system-prompt override; KeyboardInterrupt wrapped at asyncio.run boundary for clean Ctrl-C mid-stream; sys.stdout.write/flush throughout (T201 print rule)\n- **Architectural decisions**: CLI is a pure HTTP presenter — zero imports from convfinqa.application/adapters/domain (verified via grep); all conversation state held in the REPL closure (conversation_id), capture from data-conversation.data.conversationId frame\n- **Quality gate findings addressed**: HIGH KeyboardInterrupt mid-stream now caught at asyncio.run boundary; LOW _root callback replaced with idiomatic main(ctx) docstring callback; LOW `del system` replaced with stderr warning so unwired-flag UX is honest\n- **Notes for next iteration**: remaining tasks: tvq.5 (docs how-to-run-the-app.md + CLAUDE.md build/test commands) and tw6 (rename ConversationCreated -\u003e ConversationResolved). Quality gate also flagged three optional follow-ups: behavior test for _handle_frame, wiring --system through to ChatRequest, surfacing problem+json detail field — these are P3 polish, not blockers for the milestone.","created_at":"2026-05-10T00:49:11Z"},{"id":"019e0f5f-68c7-7756-b134-77b62b8f322a","issue_id":"convfinqa-tvq","author":"Francesco Albanese","text":"# 2026-05-10 — milestone:chat-slice-1\n- **Task completed**: convfinqa-tw6 Rename ConversationCreated -\u003e ConversationResolved\n- **Files changed**: src/convfinqa/application/use_cases/send_message.py, src/convfinqa/entrypoints/api/chat.py, src/convfinqa/entrypoints/api/sse.py\n- **Decision made**: rename (single event) instead of split (Created vs Resumed). Both presenters only consume conversation_id today; splitting is YAGNI. If a future client needs to differentiate, add an is_new: bool field to ConversationResolved — non-breaking.\n- **Architectural decisions**: StreamEvent union members are pure value objects; rename is pure refactor — match-statement exhaustiveness verified by pyright in both presenters.\n- **Quality gate findings**: clean — no leftover references, hexagonal invariants preserved, no drive-by changes.\n- **Notes for next iteration**: only convfinqa-tvq.5 (docs how-to-run-the-app.md + CLAUDE.md build/test commands) remains in milestone-chat-slice-1.","created_at":"2026-05-10T00:52:52Z"},{"id":"019e0f62-687d-7320-aed5-bcee39bc1b14","issue_id":"convfinqa-tvq","author":"Francesco Albanese","text":"# 2026-05-10 — milestone:chat-slice-1\n- **Task completed**: convfinqa-tvq.5 Docs how-to-run-the-app.md + CLAUDE.md Build \u0026 Test\n- **Files changed**: docs/how-to-run-the-app.md (new, 101 lines), CLAUDE.md (Build \u0026 Test placeholder replaced)\n- **Decisions made**: kept the AWS-login + .env setup as steps 1-2 (Bedrock default model needs creds before uvicorn boots cleanly); included --factory flag in every uvicorn invocation since create_app() is a factory not a module-level app; documented X-User-Id on every cURL example since dependencies.current_user_id rejects missing/blank header; -N flag on the streaming cURL so SSE frames don't get stuck in curl's buffer; CLAUDE.md Build \u0026 Test stays minimal (5 commands) and links out to the full walkthrough rather than duplicating it\n- **Architectural decisions**: none — pure docs\n- **Notes for next iteration**: milestone chat-slice-1 is complete (6/6 children done). Run `bd epic close-eligible` to close convfinqa-tvq.","created_at":"2026-05-10T00:56:08Z"}],"dependency_count":0,"dependent_count":0,"comment_count":5}
{"_type":"issue","id":"convfinqa-0hu.11","title":"Backend: /v1/healthz parity OR amend PRD 0hu criterion 2","description":"## Context\n\nPRD 0hu.4 acceptance criterion 2: `fetch('/v1/healthz')` from the running dev server returns 200 with no CORS error.\n\nBackend reality: `/healthz` lives at the root (no /v1 prefix), per k8s-liveness convention. `/v1/*` is the chat-router prefix. So `fetch('/v1/healthz')` returns 404.\n\n## Workaround applied in 0hu.4\n\n`vite.config.ts` proxies BOTH `/v1` and `/healthz` (and `/readyz`) to `http://localhost:8000`. Verified end-to-end:\n- GET /healthz through dev server → 200 `{\"status\":\"ok\"}`\n- GET /readyz through dev server → 200 `{\"status\":\"ready\"}`\n- GET /v1/healthz through dev server → 404 (backend has no such route)\n\nThe SPIRIT (proxy works, no CORS) passes. The LITERAL criterion does not.\n\n## Decision needed\n\n1. **Add `/v1/healthz` to backend** (move or duplicate the route). Trivial — `health_router_inner.prefix = '/v1'` or `api_router.include_router(router=health_router_inner, prefix='/v1')` plus a copy at `/healthz` for k8s probes if anything depends on the unprefixed path. Lets criterion 2 pass literally.\n2. **Amend PRD criterion 2** to `fetch('/healthz')` (which IS the canonical health endpoint). Update the PRD on convfinqa-0hu and tasks 0hu.5/0hu.6 (smoke tests reference the placeholder, not healthz, so probably no smoke-test impact — verify).\n3. **Status quo** — accept the spirit-vs-literal gap; the proxy works and the criterion is moot for downstream slices that hit `/v1/chat` etc.\n\n## Recommendation\n\nOption 2 — the PRD was written before the backend's actual API surface stabilised. The k8s health convention (unversioned) is defensible.","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T23:15:15Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T23:15:15Z","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.11","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-11T00:15:14Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.8","title":"Replace ESLint+Prettier with Biome; add Lefthook pre-commit hooks for frontend","description":"## Problem\n\nThe Vite scaffold (closed in convfinqa-0hu.1) ships ESLint 10 + typescript-eslint 8.59 with no formatter. Two-tool setup (ESLint + Prettier) is slower and noisier than a single Rust-based tool. We also have no pre-commit hooks on the frontend — only the backend has prek wired up.\n\n## Solution\n\n1. **Biome replaces ESLint + Prettier** in `frontend/`.\n2. **Lefthook** provides Git hooks for the frontend (pre-commit + commit-msg), scoped to staged files only.\n\n## Tasks\n\n### Biome\n- `cd frontend \u0026\u0026 pnpm add -D -E @biomejs/biome`\n- `pnpm exec biome init` → generates `biome.json`\n- `pnpm exec biome migrate eslint --write --include-inspired` (migrate rules from existing `eslint.config.js`)\n- `pnpm exec biome migrate prettier --write` (no prettier config today, but run for default-style alignment)\n- Delete `frontend/eslint.config.js`; remove `eslint`, `@eslint/js`, `eslint-plugin-react-*`, `typescript-eslint`, `globals` from `frontend/package.json` devDependencies\n- Replace `scripts.lint` with: `format` → `biome format --write`, `lint` → `biome lint --write`, `check` → `biome check --write`, `ci` → `biome ci`\n- Tune `biome.json`: keep 2-space indent + single quotes to match current Vite scaffold style (Biome defaults to tabs); enable `organizeImports`; respect `.gitignore`\n\n### Lefthook\n- `cd frontend \u0026\u0026 pnpm add -D lefthook`\n- `pnpm exec lefthook install` (writes `.git/hooks/*` from `frontend/lefthook.yml`)\n- `frontend/lefthook.yml` with parallel pre-commit jobs on `{staged_files}`:\n  - `biome check --write --no-errors-on-unmatched`\n  - `tsc --noEmit` (project-wide; staged-only doesn't make sense for type checks)\n- Decide scope: hook lives in `frontend/` and only fires on frontend-path staged files; backend keeps its existing prek setup untouched\n\n## CI\n\n- Add `pnpm exec biome ci .` to the `frontend-unit` job (planned in convfinqa-0hu.7). Fails on any lint/format drift.\n\n## Acceptance\n\n- `pnpm exec biome ci .` exits 0 in CI.\n- `git commit` on a deliberately-unformatted file gets auto-fixed and re-staged via Lefthook.\n- No `eslint` / `prettier` references remain in `frontend/package.json`.\n- Existing scaffold smoke test still passes (`pnpm exec vitest run`).\n\n## Out of Scope\n\n- Backend tooling (ruff + pyright + prek stays as-is).\n- Tailoring rule severities beyond Biome's recommended preset (do that reactively).\n- `commit-msg` enforcement (commitlint) — separate ticket if we want it.","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T22:20:14Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T22:31:50Z","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.8","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T23:20:14Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-0hu.8","depends_on_id":"convfinqa-0hu.9","type":"blocks","created_at":"2026-05-10T23:25:49Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bqx.3","title":"Gherkin: responsive.feature with viewport variants","description":"Two scenarios: '600px viewport — sidebar drawer + bottom sheet' and '900px viewport — same behavior'. Use Playwright's `test.use({ viewport })`.","acceptance_criteria":"- [ ] Both scenarios pass\n- [ ] Layout does not break (no horizontal scroll bar) at 600px","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:30Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:30Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-bqx.3","depends_on_id":"convfinqa-bqx","type":"parent-child","created_at":"2026-05-10T21:52:30Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bqx.2","title":"Right-panel as bottom sheet below lg","description":"When `lg:` breakpoint not satisfied AND `pinnedDocId` present, render right panel as a fixed bottom sheet at 90% height. A 'View document' button appears in the chat header to toggle it. Above `lg:` it remains a right column.","acceptance_criteria":"- [ ] Playwright @ 600px: pin a doc → 'View document' button visible; click → sheet slides up\n- [ ] Playwright @ 600px: dismiss sheet via close button\n- [ ] Playwright @ 1280px: pin a doc → right column visible (no 'View document' button)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:29Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:29Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-bqx.2","depends_on_id":"convfinqa-bqx","type":"parent-child","created_at":"2026-05-10T21:52:28Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bqx.1","title":"responsiveStore + hamburger button + sidebar drawer below lg","description":"`ui/responsiveStore.ts` slice for sidebar drawer open/closed. Sidebar renders as drawer with backdrop below `lg`. Hamburger button anchored top-left toggles it. Tailwind utility classes drive the transitions.","acceptance_criteria":"- [ ] Vitest: store toggles correctly\n- [ ] Playwright @ 800px: hamburger visible, click opens drawer, backdrop click closes it\n- [ ] Playwright @ 1280px: hamburger hidden, sidebar always visible","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:28Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:28Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-bqx.1","depends_on_id":"convfinqa-bqx","type":"parent-child","created_at":"2026-05-10T21:52:27Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bqx","title":"PRD: Responsive sidebar + right-panel drawer behavior","description":"## Problem Statement\n\nUnder 1024px the three-panel layout breaks: the sidebar squeezes the chat, the right panel overlaps content. The mockups are desktop-only but we need to not break catastrophically on smaller screens.\n\n## Solution\n\nBelow 1024px: sidebar collapses behind a hamburger button anchored top-left; opening it slides it over the chat as a drawer. Below 1024px: right panel becomes a bottom sheet with a \"View document\" toggle in the chat header. Above 1024px: behavior unchanged from PRD 4/6.\n\n## User Stories\n\n1. As a tablet user (768–1023px), I want a hamburger to access the sidebar.\n2. As a tablet user, I want a \"View document\" button in the chat header that opens the document table as a bottom sheet.\n3. As a phone user (\u003c768px), I want the same drawer behavior but with the sheet covering ~90% viewport height.\n4. As a user resizing the window across the 1024px breakpoint, I want the layout to adapt without losing my place or active chat.\n5. As a developer, I want responsive behavior keyed off CSS only (no media-query React state) where possible to avoid hydration mismatches.\n\n## Implementation Decisions\n\n- Use Tailwind v4 `@media (max-width: 1023px)` utilities. Sidebar gets `lg:translate-x-0 -translate-x-full` (or equivalent) plus a backdrop. State managed by a UI store slice `responsiveStore` for drawer open/closed.\n- Hamburger button is always rendered but `display: none` at `\u003e=1024px`.\n- Right panel below `lg` becomes a fixed bottom sheet (`position: fixed; bottom: 0; left: 0; right: 0;`) with its own open/close state.\n- shadcn `Sheet` primitive may simplify both drawers — adopt if it fits.\n\n## Testing Decisions\n\n- Vitest is limited for media queries — we'll cover the toggle stores and a few snapshot tests at fixed sizes via `matchMedia` mocking.\n- playwright-bdd at multiple viewport sizes (`viewport: { width, height }` per scenario): `responsive.feature` with scenarios for 600px and 900px viewports.\n\n## Out of Scope\n\n- Hand-polishing mobile typography or spacing.\n- A separate mobile-first design pass.\n- Touch gesture handling (swipe to open drawer).\n\n## Further Notes\n\nThis is intentionally minimal — we want the desktop UX to feel right at all sizes without committing to a mobile design that may diverge later.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:27Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:27Z","labels":["afk","milestone:m4-polish"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-uf5.4","title":"Apply effectiveTheme to \u003chtml data-theme\u003e + Gherkin","description":"A small effect in the AuthProvider or root layout subscribes to `effectiveTheme` and writes `document.documentElement.dataset.theme`. Add `features/theme.feature` with scenarios for: default-matches-system, toggle-persists, revert-to-system.","acceptance_criteria":"- [ ] All three Gherkin scenarios pass\n- [ ] \u003chtml data-theme\u003e attribute updates in response to setMode calls","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:26Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:26Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-uf5.4","depends_on_id":"convfinqa-uf5","type":"parent-child","created_at":"2026-05-10T21:52:25Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-uf5.3","title":"UserMenu theme submenu + state-syncing UI","description":"Add a 'Theme' submenu with three radio items: System, Light, Dark. Selecting one calls `themeStore.setMode`. Active radio reflects current mode.","acceptance_criteria":"- [ ] Vitest: clicking each radio updates the store\n- [ ] Vitest: radio reflects the current mode on render\n- [ ] Visual: integrates into mockup E/F user-menu pattern","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:25Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:25Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-uf5.3","depends_on_id":"convfinqa-uf5","type":"parent-child","created_at":"2026-05-10T21:52:24Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-uf5.2","title":"Inline theme bootstrap script in index.html + extracted unit-tested function","description":"Extract the resolve-theme-from-storage logic to `src/theme/resolveInitialTheme.ts` (pure function). Inline-mirror it as a blocking \u003cscript\u003e in index.html. Unit-test the function for each initial state combination.","acceptance_criteria":"- [ ] Unit test: each combination of (storage value, system pref) yields expected theme\n- [ ] Visual: no theme flash on hard reload\n- [ ] Inline script is \u003c 1KB","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:23Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:23Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-uf5.2","depends_on_id":"convfinqa-uf5","type":"parent-child","created_at":"2026-05-10T21:52:23Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-uf5.1","title":"themeStore (TanStack Store) + matchMedia listener + unit tests","description":"`src/theme/themeStore.ts` exposes `mode`, `effectiveTheme`, `setMode`. Listens to `matchMedia('(prefers-color-scheme: light)')` and updates effectiveTheme when mode is 'system'.","acceptance_criteria":"- [ ] Unit test: setMode('light') sets effectiveTheme to light\n- [ ] Unit test: setMode('system') with system=light yields effectiveTheme=light\n- [ ] Unit test: matchMedia change event updates effectiveTheme when in system mode","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:22Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:22Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-uf5.1","depends_on_id":"convfinqa-uf5","type":"parent-child","created_at":"2026-05-10T21:52:22Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-uf5","title":"PRD: Light theme toggle with system preference + persistence","description":"## Problem Statement\n\nThe frontend foundation defines both dark and light theme tokens, but there is no toggle and the dark theme is hardcoded as the default. Users with `prefers-color-scheme: light` should see the light theme by default; everyone should be able to override.\n\n## Solution\n\nOn first load, respect `prefers-color-scheme`. Once the user toggles via UserMenu, the choice persists in localStorage and overrides the system pref. An inline script in `index.html` sets `\u003chtml data-theme\u003e` before React hydrates to avoid theme flash. TanStack Store holds the active theme; the UserMenu shows a toggle (or three-state: system / light / dark).\n\n## User Stories\n\n1. As a user with system light mode, I want the app to render in light theme on first load.\n2. As a user with system dark mode, I want the app to render in dark theme on first load.\n3. As a user, I want to override the theme via the UserMenu.\n4. As a user, I want my choice to persist across reloads.\n5. As a user, I want to revert to \"system\" if I changed my mind.\n6. As a user, I want no theme flash on initial paint.\n\n## Implementation Decisions\n\n- Theme store `src/theme/themeStore.ts`: `mode: 'system' | 'light' | 'dark'`. Effective theme = mode === 'system' ? matchMedia query : mode.\n- Inline blocking script in `index.html` reads `localStorage['theme.mode']` (or default 'system'), resolves to a concrete theme, and sets `\u003chtml data-theme=\"...\"\u003e` before any CSS or JS runs. Prevents flash.\n- UserMenu shows a sub-menu \"Theme\" with three radio items: System / Light / Dark.\n- Subscribe to `matchMedia('(prefers-color-scheme: light)')` events so toggling system preference in OS updates the app immediately while in 'system' mode.\n\n## Testing Decisions\n\n- Vitest: theme store transitions correctly between modes; localStorage write/read.\n- Vitest: inline script logic extracted into a function and unit-tested for each initial state.\n- playwright-bdd: `theme.feature` — system pref light → renders light; manual switch to dark → persists across reload; switch back to system → reflects system pref.\n\n## Out of Scope\n\n- Per-page or per-component theme overrides.\n- High-contrast or print stylesheets.\n- Animated transition between themes.\n\n## Further Notes\n\nCSS variables are already defined in `globals.css` from PRD 2. Switching themes is purely a `[data-theme]` attribute change; no component code references theme directly.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:21Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:21Z","labels":["afk","milestone:m4-polish"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bmu.4","title":"Gherkin: stop-and-recover.feature","description":"Scenarios: 'Stop preserves partial', '409 conflict surfaces inline with retry', 'Retry completes the next stream'.","acceptance_criteria":"- [ ] All three scenarios pass under playwright-bdd\n- [ ] Use a backend test fixture that streams slowly so Stop has time to halt","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:20Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:20Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-bmu.4","depends_on_id":"convfinqa-bmu","type":"parent-child","created_at":"2026-05-10T21:52:20Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bmu.3","title":"Wire stopped-badge into MessageBubble","description":"Pass `stoppedIds: Set\u003cstring\u003e` from the route into MessageList → MessageBubble. Render a small chip badge below stopped assistant messages.","acceptance_criteria":"- [ ] Vitest: badge renders when message id is in stoppedIds\n- [ ] Visual: chip uses the design token for muted accent","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:19Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:19Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-bmu.3","depends_on_id":"convfinqa-bmu","type":"parent-child","created_at":"2026-05-10T21:52:19Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bmu.2","title":"StreamErrorBanner + retry semantics","description":"`components/StreamErrorBanner.tsx` maps errors to copy. Retry resends the last user message. For 409, retry polls `useChatMessages` until the previous stream's `text-end` is observed.","acceptance_criteria":"- [ ] Vitest: each error class maps to expected copy\n- [ ] Vitest: retry calls sendMessage with the last user message text\n- [ ] Vitest: 409 retry waits until poll returns the previous assistant message","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:18Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:18Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-bmu.2","depends_on_id":"convfinqa-bmu","type":"parent-child","created_at":"2026-05-10T21:52:18Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bmu.1","title":"StopButton component + integration with useChat.stop()","description":"`components/StopButton.tsx` renders in the composer area when status is streaming or submitted. Click calls `stop()` and records the message id in a local stopped set.","acceptance_criteria":"- [ ] Vitest: button renders only when streaming\n- [ ] Vitest: click invokes stop()\n- [ ] Vitest: stopped message id is recorded for badge rendering","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:16Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:16Z","labels":["afk","milestone:m4-polish"],"dependencies":[{"issue_id":"convfinqa-bmu.1","depends_on_id":"convfinqa-bmu","type":"parent-child","created_at":"2026-05-10T21:52:16Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-bmu","title":"PRD: Stop button + stream error UI","description":"## Problem Statement\n\nStreams can be long. Users need to interrupt them. Also: streams can fail (network drop, server error, 409 concurrent same-conversation, 500). Right now those failures crash silently or render nothing.\n\n## Solution\n\nAdd a Stop button to the composer area that appears while `useChat({ status }) === 'streaming'`. Clicking it calls `useChat`'s `stop()` and the partial assistant message is preserved with a \"stopped\" badge. Surface errors as an inline banner inside the chat thread with a \"retry\" affordance that re-sends the last user message.\n\n## User Stories\n\n1. As a user mid-stream, I want a visible Stop button.\n2. As a user, I want clicking Stop to halt within ~1 second and preserve the partial response.\n3. As a user whose stream errored, I want a clear inline banner telling me what failed.\n4. As a user, I want a \"Retry\" button on the error banner that re-sends the message.\n5. As a user, I want a 409 conflict (concurrent same-conv request) to show a tailored message (\"another response is in progress\"); retry waits for the in-progress stream to finish.\n6. As a user, I want the input composer re-enabled after Stop so I can immediately type the next message.\n\n## Implementation Decisions\n\n- StopButton component renders inside the composer area; appears when `status === 'streaming' || status === 'submitted'`.\n- Stopped assistant messages get a small chip badge (\"stopped\"). Implementation: track a Set\u003cmessageId\u003e of locally-stopped ids in component state, render the badge for those.\n- Error banner: `components/StreamErrorBanner.tsx` consumes the `error` value returned from `useChat`. Maps error → friendly copy: 409 → \"Another response is in progress\", 5xx → \"Server error\", network → \"Connection lost\". Banner is dismissable.\n- Retry: re-invokes `sendMessage` with the most recent user message that had no successful assistant reply. For 409, retry is delayed until query polling shows no in-flight conversation.\n- AI SDK 5 `onError` hook surfaces the error; we also listen for an `error` frame from the stream itself.\n\n## Testing Decisions\n\n- Vitest: StopButton renders only during streaming; click invokes stop callback.\n- Vitest: StreamErrorBanner maps each error class to the right copy.\n- Vitest: retry resends the last user message.\n- playwright-bdd: `stop-and-recover.feature` — start a stream, click Stop, see partial + badge; trigger a 409 via a fake adapter; assert banner copy; click Retry; second stream completes.\n\n## Out of Scope\n\n- Auto-retry on network failure (intentionally avoided — streams aren't idempotent server-side).\n- Diff between partial and full responses.\n- Backend changes — the 409 is already emitted; we just render it.\n\n## Further Notes\n\nWe won't auto-retry transient errors because backend conversations may already have committed the user message. Retry must be user-initiated.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:15Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:15Z","labels":["afk","milestone:m4-polish"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-fxn.5","title":"Frontend: seed useChat with initialMessages on resume + Gherkin","description":"In `routes/_authed/app/index.tsx`, when `chatId` search param is present, fetch `useChatMessages(chatId)` and pass to `useChat({ initialMessages: data })`. Add `features/resume-chat.feature` with scenarios for: page reload preserves active chat; clicking a sidebar entry loads it; new conversation appears within 1s of stream finish.","acceptance_criteria":"- [ ] Gherkin: reload-preserves-chat scenario passes\n- [ ] Gherkin: click-sidebar-entry loads history scenario passes\n- [ ] Gherkin: new-conv-appears-in-sidebar after stream finish (uses query invalidation)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:14Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:14Z","labels":["afk","milestone:m3-persistence"],"dependencies":[{"issue_id":"convfinqa-fxn.5","depends_on_id":"convfinqa-fxn","type":"parent-child","created_at":"2026-05-10T21:52:14Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-fxn.4","title":"Frontend: Sidebar grouped chat list + filter input + Virtual","description":"Update `components/Sidebar.tsx`: replace empty-state with the real grouped list when data is present. Collapsible doc-group headers (open by default). Filter input narrows by `last_message_preview`. TanStack Virtual wraps the flattened row list when count \u003e50.","acceptance_criteria":"- [ ] Vitest: grouped rendering for a sample of 3 docs x 2 chats each\n- [ ] Vitest: filter input narrows visible rows\n- [ ] Vitest: clicking a row calls `router.navigate` with both `chatId` and `pinnedDocId` search params","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:13Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:13Z","labels":["afk","milestone:m3-persistence"],"dependencies":[{"issue_id":"convfinqa-fxn.4","depends_on_id":"convfinqa-fxn","type":"parent-child","created_at":"2026-05-10T21:52:13Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-fxn.3","title":"Frontend: useChatList + useChatMessages queries + groupByDocument transform","description":"`lib/queries/chats.ts` exposes both queries. `lib/transforms/groupByDocument.ts` is a pure function returning `[{ document, conversations: [...] }]` sorted by document.title then conversation.last_message_at desc.","acceptance_criteria":"- [ ] Unit test: groupByDocument output shape and ordering\n- [ ] Unit test: queries build the right URL and parse the response shape\n- [ ] TypeScript types match backend contract","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:12Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:12Z","labels":["afk","milestone:m3-persistence"],"dependencies":[{"issue_id":"convfinqa-fxn.3","depends_on_id":"convfinqa-fxn","type":"parent-child","created_at":"2026-05-10T21:52:12Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-fxn.2","title":"Backend: GET /v1/chats and /v1/chats/{id}/messages routes","description":"Add `entrypoints/api/routes/chats.py`. `X-User-Id` header required. Integration tests against testcontainer with seeded conversations + messages + docs.","acceptance_criteria":"- [ ] Integration: 2 users, 3 conversations across 2 docs → each user sees only theirs grouped correctly\n- [ ] Integration: get-messages with wrong user returns 404\n- [ ] Integration: ordering is stable and matches the spec","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:11Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:11Z","labels":["afk","milestone:m3-persistence"],"dependencies":[{"issue_id":"convfinqa-fxn.2","depends_on_id":"convfinqa-fxn","type":"parent-child","created_at":"2026-05-10T21:52:10Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-fxn.1","title":"Backend: list_chats + get_chat_messages use cases + ports","description":"Add use cases under `application/use_cases/`. Extend the conversation repo port with `list_for_user(user_id)` (joined with document) and `get_messages(conversation_id, user_id)` (404 on cross-user).","acceptance_criteria":"- [ ] Unit tests pass with a fake repo\n- [ ] Domain port has zero framework imports\n- [ ] Use cases return domain objects, presenters serialize to wire shapes","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:10Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:10Z","labels":["afk","milestone:m3-persistence"],"dependencies":[{"issue_id":"convfinqa-fxn.1","depends_on_id":"convfinqa-fxn","type":"parent-child","created_at":"2026-05-10T21:52:09Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-fxn","title":"PRD: Sidebar chat history — list + resume conversations","description":"## Problem Statement\n\nAfter sending the first message in PRD 7, the conversation id appears in the URL but reloading the page doesn't restore the chat. The sidebar shows no real conversations. The backend persists conversations + messages but has no read endpoints.\n\n## Solution\n\nBackend: two new read endpoints. `GET /v1/chats` returns the user's conversations joined with their pinned document for sidebar grouping. `GET /v1/chats/{id}/messages` returns the ordered message list. Both require `X-User-Id`. Both follow hexagonal layering.\n\nFrontend: the Sidebar's chat list groups by document (collapsible doc sections, ticker + year on the header, conversation rows show first-user-message preview + timestamp). Clicking a row sets `?chatId=\u003cid\u003e\u0026pinnedDocId=\u003cdoc_id\u003e` and `useChat`'s `initialMessages` is seeded from the messages query. Stream completion invalidates `['chats']` so the sidebar refreshes.\n\n## User Stories\n\n1. As a returning user, I want to see all my prior conversations in the sidebar.\n2. As a returning user, I want conversations grouped by their pinned document.\n3. As a returning user, I want to click a conversation and have it open with the full message history.\n4. As a returning user, I want the right panel to re-pin the conversation's document automatically.\n5. As a returning user, I want to refresh the page and have the active chat persist.\n6. As a returning user, I want the sidebar's filter input to live-search across conversation titles.\n7. As a returning user, I want recently-active conversations sorted to the top within each document group.\n8. As a user, I want a new conversation I just sent to appear in the sidebar within a beat (optimistic or via cache invalidation).\n\n## Implementation Decisions\n\n- Backend endpoint contract:\n  - `GET /v1/chats` → `{ items: [{ id, document: {id, ticker, year, title}, last_message_preview, last_message_at }] }`, sorted by `last_message_at desc` server-side. (No pagination for now — typical user count is small.)\n  - `GET /v1/chats/{id}/messages` → `{ items: [{id, role, content, created_at}] }`, ordered by `created_at asc`. 404 if conversation not owned by `X-User-Id`.\n- Hexagonal layering: new `application/use_cases/list_chats.py`, `get_chat_messages.py`. Reuse existing conversation repo, extend with the join.\n- Frontend: `lib/queries/chats.ts` exposes `useChatList()` and `useChatMessages(chatId)`. `useChat({ initialMessages: ... })` seeds from the messages query when present.\n- Sidebar grouping: client-side `Map\u003cdocumentId, Conversation[]\u003e` built from the list. Doc header shows ticker + year. Filter input narrows by case-insensitive substring across `last_message_preview`.\n- `last_message_preview` is the first 80 chars of the most recent user message (computed server-side via SQL).\n- TanStack Virtual wraps the flat row list when grouped count exceeds a threshold.\n\n## Testing Decisions\n\n- Backend integration: seed two conversations, two docs, then `GET /v1/chats` returns the right groups + ordering. `GET /v1/chats/{id}/messages` returns messages in order. Cross-user access returns 404.\n- Backend unit: ordering / preview calculation in the repo's joined query.\n- Frontend unit: groupByDocument transform is a pure function with unit tests.\n- Frontend component: sidebar renders grouped conversations, click navigates with both URL params.\n- playwright-bdd: `resume-chat.feature` — send a message (PRD 7), reload page, conversation appears in sidebar, click it, message history re-renders, right panel re-pins doc.\n\n## Out of Scope\n\n- Renaming or deleting conversations.\n- Pinning conversations to the top.\n- Search across the message body (not just preview).\n- Pagination of the chat list.\n\n## Further Notes\n\n`last_message_preview` is denormalized as a SQL expression — no schema column needed unless query cost becomes a problem.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:09Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:09Z","labels":["afk","milestone:m3-persistence"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3.7","title":"Frontend: integrate Composer + MessageList + useChat in /app route + Gherkin","description":"Wire it all together in `routes/_authed/app/index.tsx`. On `onFinish`, `queryClient.invalidateQueries(['chats'])`. Optimistic user message added via `useChat`'s `sendMessage`. After first turn, update the URL with `?chatId=\u003cid\u003e`. Add `features/send-and-stream.feature` with the full scenario.","acceptance_criteria":"- [ ] Gherkin scenario passes: send Cmd+Enter, see optimistic message, see streaming, end with chatId in URL\n- [ ] Vitest: route component renders without errors when a doc is pinned\n- [ ] Integration: after stream ends, sidebar reflects new conversation (post PRD 8 wiring; for this slice asserted via query cache spy)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:08Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:08Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-rb3.7","depends_on_id":"convfinqa-rb3","type":"parent-child","created_at":"2026-05-10T21:52:07Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3.6","title":"Frontend: MessageList + MessageBubble (react-markdown)","description":"`components/MessageList.tsx` lays out messages; `components/MessageBubble.tsx` renders user (right-aligned, plain text) vs assistant (left-aligned, react-markdown + remark-gfm). Pulsing cursor element appears in the assistant bubble while streaming.","acceptance_criteria":"- [ ] Vitest: user bubble renders plain text; assistant bubble renders markdown bold/italic/lists\n- [ ] Vitest: pulsing cursor rendered until `status` transitions out of 'streaming'\n- [ ] Visual: matches mockup E mid-stream rendering","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:07Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:07Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-rb3.6","depends_on_id":"convfinqa-rb3","type":"parent-child","created_at":"2026-05-10T21:52:06Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3.5","title":"Frontend: Composer component + Cmd+Enter + disabled state","description":"`components/Composer.tsx` built on TanStack Form. Auto-growing textarea. Cmd/Ctrl+Enter sends; Enter inserts newline. Disabled (with tooltip 'Pin a document first') when no `pinnedDocId`. Character counter renders at \u003e28k.","acceptance_criteria":"- [ ] Vitest: pressing Cmd+Enter with non-empty value calls onSend\n- [ ] Vitest: pressing Enter inserts newline (does not send)\n- [ ] Vitest: tooltip 'Pin a document first' visible when disabled\n- [ ] Vitest: character counter appears when value \u003e28000 chars","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:05Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:05Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-rb3.5","depends_on_id":"convfinqa-rb3","type":"parent-child","created_at":"2026-05-10T21:52:05Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3.4","title":"Frontend: createChatTransport + useChat wiring + Vitest unit","description":"`lib/transport/createChatTransport.ts`: configures `DefaultChatTransport({api: '/v1/chat/stream', headers: () =\u003e ({ 'X-User-Id': userId }), prepareSendMessagesRequest})`. The prepare hook includes `document_id` when no `conversation_id` is known, only `conversation_id` thereafter.","acceptance_criteria":"- [ ] Unit test: first send includes document_id, no conversation_id\n- [ ] Unit test: second send (after conv id is known) includes conversation_id, no document_id\n- [ ] Unit test: header callback returns the current userId from auth state","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:04Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:04Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-rb3.4","depends_on_id":"convfinqa-rb3","type":"parent-child","created_at":"2026-05-10T21:52:04Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3.3","title":"Backend: POST /v1/chat[/stream] body validation + integration test","description":"Pydantic model gains `document_id: str | None`. Custom validator: required iff `conversation_id is None`. 400 with problem+json on violation. Integration test against testcontainer Postgres + a fake LLM port.","acceptance_criteria":"- [ ] Integration test: new conv with document_id streams successfully\n- [ ] Integration test: new conv WITHOUT document_id returns 400\n- [ ] Integration test: existing conv id resumes correctly (document_id loaded from row)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:03Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:03Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-rb3.3","depends_on_id":"convfinqa-rb3","type":"parent-child","created_at":"2026-05-10T21:52:03Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3.2","title":"Backend: thread document_id through start_chat use case + system prompt","description":"Update `start_chat` use case signature; require `document_id` iff creating a new conversation. Fetch document, build system prompt via new `application/prompts/system_prompt.py` template. Add unit tests on the prompt builder.","acceptance_criteria":"- [ ] Use case unit test: new conversation requires document_id\n- [ ] Use case unit test: existing conversation ignores document_id in the body\n- [ ] Prompt builder unit test: pre_text/post_text truncated at 8KB each","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:02Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:02Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-rb3.2","depends_on_id":"convfinqa-rb3","type":"parent-child","created_at":"2026-05-10T21:52:02Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3.1","title":"Alembic migration: conversations.document_id NOT NULL FK","description":"Add column to `conversations` with `NOT NULL` and `REFERENCES documents(id)`. Since the table is effectively empty in dev/prod today, no backfill needed. Downgrade drops the column.","acceptance_criteria":"- [ ] `alembic upgrade head` applies on a fresh DB and on a DB that has the prior migration\n- [ ] Downgrade drops the column cleanly\n- [ ] Foreign key enforces referential integrity (insert with bogus doc id fails)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:01Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:01Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-rb3.1","depends_on_id":"convfinqa-rb3","type":"parent-child","created_at":"2026-05-10T21:52:00Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-rb3","title":"PRD: Send message + stream end-to-end","description":"## Problem Statement\n\nThe whole point of the app is to chat about the pinned document. The user has a doc pinned but cannot yet send a message. Backend already streams SSE in Vercel-AI-SDK-v5 frame format — we need to (a) thread `document_id` through conversation creation in the backend, (b) wire the composer + message list + streaming hook in the frontend.\n\n## Solution\n\nBackend: schema migration adds `conversations.document_id NOT NULL` with FK to `documents.id`. `POST /v1/chat` and `POST /v1/chat/stream` accept `document_id` in the body when no `conversation_id` is provided; when `conversation_id` IS provided, the document is loaded from the conversation row. The system prompt template incorporates the document's pre_text, table, and post_text.\n\nFrontend: composer at the bottom of the main column (TanStack Form, auto-growing textarea, Cmd/Ctrl+Enter to send, disabled when no doc pinned). On send, append a user message optimistically and start streaming via `@ai-sdk/react`'s `useChat` with a `DefaultChatTransport` pointing at `/v1/chat/stream`. The transport's headers callback injects `X-User-Id` from `useAuth()`. Messages render via react-markdown + remark-gfm. On `onFinish`, invalidate the chat-list query so the sidebar refreshes.\n\n## User Stories\n\n1. As a user with a pinned doc, I want to type a question and press Cmd/Ctrl+Enter to send it.\n2. As a user, I want my message to appear immediately in the chat thread, before the assistant responds.\n3. As a user, I want to see the assistant's response stream in token-by-token.\n4. As a user, I want a pulsing cursor / typing indicator in the assistant bubble until the first token arrives.\n5. As a user, I want a new conversation to be created when I send my first message with a pinned doc.\n6. As a user, I want the conversation id to appear in the URL after the first turn so I can share or bookmark.\n7. As a user, I want assistant messages to render basic markdown (bold, italic, lists, tables, code blocks).\n8. As a user with no doc pinned, I want the composer disabled with a tooltip \"Pin a document first\".\n9. As a user near the character limit, I want a counter at \u003e28k characters (limit is 32k).\n\n## Implementation Decisions\n\n- Schema: `conversations.document_id text NOT NULL REFERENCES documents(id)`. The table is currently effectively empty (no users) — safe to ship as NOT NULL immediately.\n- Use case: `start_chat` (current path) now requires `document_id` when `conversation_id` is None; raises `ValueError` otherwise. The use case fetches the doc, builds a system prompt with the doc payload, and emits the stream.\n- API contract: `POST /v1/chat[/stream]` body becomes `{message, conversation_id?, document_id?}`. Validation: `document_id` required iff `conversation_id` is absent.\n- System prompt template lives in `application/prompts/system_prompt.py`. Includes a short framing + the doc's pre_text + table (JSON) + post_text. Token budget: cap pre_text and post_text at 8KB each (truncate from the middle if needed) to leave plenty of headroom for multi-turn.\n- Frontend: `lib/transport/createChatTransport.ts` returns a configured `DefaultChatTransport` with dynamic headers and `prepareSendMessagesRequest` to shape the body (includes `document_id` on first turn, `conversation_id` thereafter).\n- Composer: TanStack Form, `textarea` with `field-sizing: content`-style auto-grow shim, key handler for Cmd/Ctrl+Enter. Character count appears at \u003e28k. Send button disabled when value is empty OR when no pinned doc.\n- MessageList: virtualized via TanStack Virtual once we have many messages, but trivially scrollable in this slice.\n- MessageBubble: user (right-aligned) vs assistant (left-aligned), assistant uses react-markdown + remark-gfm. Pulsing cursor renders inside the bubble until first `text-delta`.\n\n## Testing Decisions\n\n- Backend integration: `POST /v1/chat/stream` with `document_id` and no `conversation_id` creates a conversation, returns the conversation id in the `data-conversation` frame, persists user message + assistant message. Stream completes with `[DONE]`. Without `document_id` AND without `conversation_id` → 400.\n- Backend unit: system_prompt template renders correctly with realistic doc shapes.\n- Frontend unit: `createChatTransport` builds the right body on first turn vs subsequent turns.\n- Frontend component: Composer renders, Cmd+Enter triggers send when not empty + doc pinned, disabled state shows tooltip.\n- Frontend component: MessageBubble renders markdown (bold + list).\n- playwright-bdd: `send-and-stream.feature` — pin doc, type message, press Cmd+Enter, see optimistic user message + pulsing assistant bubble + streamed tokens + final URL contains `?chatId=`.\n\n## Out of Scope\n\n- Stop button (PRD 9).\n- Error UI (PRD 9).\n- Chat history list rendering real data (PRD 8).\n- Resume an existing chat (PRD 8).\n- Reasoning trace / citations rendering (later epic).\n\n## Further Notes\n\nThe system prompt's exact wording is intentionally simple in this slice — we just need the assistant to be aware of the pinned doc. Prompt-engineering for accuracy is its own deliverable, deferred.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:52:00Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:52:00Z","labels":["afk","milestone:m2-core-chat"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-9ye.5","title":"Gherkin: pin-document.feature","description":"Scenarios: 'Pinning shows the table', 'Resize persists across reload', 'Copy-as-CSV writes the right bytes', 'Change document reopens the picker'.","acceptance_criteria":"- [ ] All four scenarios pass under playwright-bdd\n- [ ] Test seeds a known doc (JKHY 2009 page-28-3) for deterministic assertions","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:59Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:59Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-9ye.5","depends_on_id":"convfinqa-9ye","type":"parent-child","created_at":"2026-05-10T21:51:58Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-9ye.4","title":"RightPanel wrapper + header + Copy-as-CSV","description":"`components/RightPanel.tsx` composes the document header (title + 'Change document' button), DocTable, and pre_text + post_text below. Copy-as-CSV button uses `navigator.clipboard.writeText` with a properly quoted CSV serializer (Vitest-covered).","acceptance_criteria":"- [ ] Vitest: clicking 'Copy as CSV' writes a CSV with the visible rows + columns to clipboard (mocked)\n- [ ] Vitest: 'Change document' click opens the picker (asserted via URL search param)\n- [ ] Pre_text and post_text render with prose styling","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:58Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:58Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-9ye.4","depends_on_id":"convfinqa-9ye","type":"parent-child","created_at":"2026-05-10T21:51:57Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-9ye.3","title":"ResizableSplit + persisted panel width","description":"`components/ResizableSplit.tsx` exposes a draggable handle on the left edge of the right panel. Min 240px, max 60% of viewport. Double-click resets to default. Width persisted in TanStack Store, hydrated on mount.","acceptance_criteria":"- [ ] Vitest: pointer-down + pointer-move + pointer-up changes panel width within bounds\n- [ ] Vitest: double-click handle resets width\n- [ ] Vitest: width persists across re-mount via the store","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:57Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:57Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-9ye.3","depends_on_id":"convfinqa-9ye","type":"parent-child","created_at":"2026-05-10T21:51:56Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-9ye.2","title":"DocTable component (TanStack Table + sticky first column)","description":"`components/DocTable.tsx` builds column defs from row-major output. First column (row labels) is sticky-left via CSS. Numeric cells right-aligned, JetBrains Mono. Long header text wraps with `overflow-wrap: break-word`.","acceptance_criteria":"- [ ] Vitest: renders given a sample row-major dataset\n- [ ] Vitest: first column stays in DOM with `position: sticky` styling\n- [ ] Visual: matches mockup D pinned-context section","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:55Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:55Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-9ye.2","depends_on_id":"convfinqa-9ye","type":"parent-child","created_at":"2026-05-10T21:51:55Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-9ye.1","title":"useDocument query + tableToRowMajor pure transform","description":"`lib/queries/documents.ts` adds `useDocument(id)` (`staleTime: Infinity`). `lib/transforms/tableToRowMajor.ts` exports a pure function and types. Unit-test the transform on at least 3 sample shapes.","acceptance_criteria":"- [ ] Unit test: transform handles nested-dict input correctly\n- [ ] Unit test: handles missing cells (sparse rows) by inserting nulls\n- [ ] Unit test: preserves column order from input dict insertion order","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:54Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:54Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-9ye.1","depends_on_id":"convfinqa-9ye","type":"parent-child","created_at":"2026-05-10T21:51:54Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-9ye","title":"PRD: Pin doc + render financial table","description":"## Problem Statement\n\nPicking a doc sets `?pinnedDocId=\u003cid\u003e` in the URL but nothing renders. The right panel should show the document's table (the financial-statement data the chat will reason about) plus the pre/post narrative text. The user should also be able to resize / collapse the right panel.\n\n## Solution\n\nWhen `pinnedDocId` is present, the right panel renders three sections: a header with the document title + \"Change document\" button (re-opens picker), a TanStack Table view of the structured `table_data` field, and the pre/post text below. The panel is resizable (drag handle) and collapsible (icon button); width persists in TanStack Store. Long column headers wrap; numeric values are right-aligned in JetBrains Mono. A \"Copy as CSV\" button serializes the visible table.\n\n## User Stories\n\n1. As a user, I want to see the financial table of the pinned document on the right side of the screen.\n2. As a user, I want long header strings (e.g. \"Year ended June 30, 2009\") to wrap or truncate gracefully.\n3. As a user, I want the row labels (first column) to stay visible while I horizontally scroll wide tables.\n4. As a user, I want numeric values to be right-aligned and rendered in a monospace font for readability.\n5. As a user, I want to copy the table as CSV with one click.\n6. As a user, I want to resize the right panel by dragging its left edge.\n7. As a user, I want the panel's width to persist across reloads.\n8. As a user, I want to click \"Change document\" to open the picker again.\n9. As a user, I want to see the pre_text and post_text below the table for context.\n\n## Implementation Decisions\n\n- TanStack Query: `useDocument(id)` with `staleTime: Infinity` (full payload is immutable once fetched).\n- `lib/transforms/tableToRowMajor.ts`: takes `{col: {row: value}}` → returns `{ columns: string[], rowLabels: string[], data: (number|null|string)[][] }`. Pure function, unit tested.\n- TanStack Table headless API: column defs derived from the row-major shape, sticky first column via CSS (`position: sticky; left: 0; background: var(--bg-page);`).\n- `components/DocTable.tsx` renders the table; `components/RightPanel.tsx` is the wrapper with header, table, narrative text, and copy-as-CSV.\n- `components/ResizableSplit.tsx`: pointer-based resize handle with min/max constraints (240px – 60% of viewport). Double-click resets to default 480px. Width persisted in `useSidebarStore` sibling slice `panelWidthStore`.\n- Sentence-case for \"Change document\"; opens the picker dialog from PRD 5 by setting an internal `pickerOpen` URL search param.\n\n## Testing Decisions\n\n- Vitest: `tableToRowMajor` unit tests against a few realistic shapes including the JKHY 2009 sample (mixed numeric/string cells, varied column counts).\n- Vitest: DocTable renders headers + rows from a given row-major dataset; sticky-column CSS is asserted via `getComputedStyle`.\n- Vitest: ResizableSplit pointer events drag the width; double-click resets.\n- playwright-bdd: `pin-document.feature` — picks JKHY 2009 page-28-3, asserts the table row \"net cash from operating activities\" appears in the right panel; resizes the panel and reloads; \"Copy as CSV\" puts the right bytes on the clipboard.\n\n## Out of Scope\n\n- Citation overlays on cells (deferred — needs PRD that adds reasoning stream parts).\n- Editing or annotating the table.\n- Server-side CSV download.\n- Rendering very wide tables differently (handled via overflow + sticky column).\n\n## Further Notes\n\nReal ConvFinQA tables have between ~3 and ~20 columns and rarely more than ~30 rows. We don't need virtualization for the table body — but if a doc surprises us, TanStack Virtual can be dropped in.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:53Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:53Z","labels":["afk","milestone:m2-core-chat"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6.7","title":"Gherkin: pick-document.feature","description":"Scenarios: 'Search by ticker', 'Narrow by year', 'Pin a result via URL param'. Each runs against the seeded testcontainer.","acceptance_criteria":"- [ ] All three scenarios pass under playwright-bdd\n- [ ] e2e seeds at least 20 deterministic docs before running","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:52Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:52Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-dj6.7","depends_on_id":"convfinqa-dj6","type":"parent-child","created_at":"2026-05-10T21:51:52Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6.6","title":"Frontend: DocPicker dialog + debounced search + year slider","description":"`components/DocPicker.tsx`. Opens from EmptyState CTA and from a button in the Sidebar header. shadcn `Dialog`. Search input debounced 200ms via TanStack Pacer. Year-range slider (shadcn `Slider` if available, else native). Virtualized result list (TanStack Virtual) of clickable rows. Click → `router.navigate({ search: (prev) =\u003e ({ ...prev, pinnedDocId: id }) })`.","acceptance_criteria":"- [ ] Vitest: typing in the search box triggers a debounced query (uses fake timers)\n- [ ] Vitest: clicking a result calls router.navigate with the right search param\n- [ ] Visual matches mockup C's picker variant (or extrapolated from the design tokens)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:51Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:51Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-dj6.6","depends_on_id":"convfinqa-dj6","type":"parent-child","created_at":"2026-05-10T21:51:51Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6.5","title":"Frontend: useDocumentList infinite query + Vitest unit","description":"`lib/queries/documents.ts` exposes a `useDocumentList` infinite query keyed on `{q, yearMin, yearMax}`. Vitest: mock fetch, assert the URL is built correctly and pagination cursor is threaded through.","acceptance_criteria":"- [ ] Unit test: URL has the expected query string for given filters\n- [ ] Unit test: infinite query merges pages via next_cursor\n- [ ] TypeScript types for items match the backend contract","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:50Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:50Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-dj6.5","depends_on_id":"convfinqa-dj6","type":"parent-child","created_at":"2026-05-10T21:51:49Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6.4","title":"Seeder script + make seed-docs target","description":"`backend/scripts/seed_documents.py` reads `data/convfinqa_dataset.json`, derives `ticker` via regex on id, derives `title` from id slug + page, batch INSERTs with `ON CONFLICT (id) DO UPDATE`. Add `make seed-docs` Makefile target. Wire into `make up` after `alembic upgrade head`.","acceptance_criteria":"- [ ] `make seed-docs` on a clean DB inserts ~3458 rows\n- [ ] Rerunning is a no-op (row count unchanged)\n- [ ] `make up` ends with seeded data accessible via curl","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:49Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:49Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-dj6.4","depends_on_id":"convfinqa-dj6","type":"parent-child","created_at":"2026-05-10T21:51:48Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6.3","title":"FastAPI routes /v1/documents and /v1/documents/{id}","description":"Add `entrypoints/api/routes/documents.py`. Use `Depends` for use case injection. Validate query params with Pydantic. Status codes from `fastapi.status`. 404 on missing id.","acceptance_criteria":"- [ ] Integration test: empty DB returns `{items: [], next_cursor: null}`\n- [ ] Integration test: 20 seeded docs + `q=jkhy` returns the JKHY docs\n- [ ] Integration test: pagination via cursor reaches every row exactly once with no duplicates\n- [ ] Integration test: `/v1/documents/{unknown}` returns 404","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:47Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:47Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-dj6.3","depends_on_id":"convfinqa-dj6","type":"parent-child","created_at":"2026-05-10T21:51:47Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6.2","title":"Hexagonal: documents port + adapter + use cases","description":"Add `domain/ports/documents_port.py` (Protocol with `list(q, year_min, year_max, cursor, limit)` and `get(id)`). Add `adapters/persistence/documents_repo.py` implementing it via SQLAlchemy async. Add `application/use_cases/list_documents.py` and `get_document.py`. Wire into `Container.bootstrap_application()`.","acceptance_criteria":"- [ ] Domain port has zero framework imports (matches hexagonal rules)\n- [ ] Adapter implements Protocol structurally (verified by pyright)\n- [ ] Use case unit tests pass with a fake repo","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:46Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:46Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-dj6.2","depends_on_id":"convfinqa-dj6","type":"parent-child","created_at":"2026-05-10T21:51:46Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6.1","title":"Alembic migration: documents table + indexes","description":"Create migration `add_documents_table`. Columns + GENERATED tsvector + GIN/btree indexes per the PRD. Schema additive only.","acceptance_criteria":"- [ ] `alembic upgrade head` against a clean DB applies cleanly\n- [ ] `alembic downgrade -1` drops the table cleanly\n- [ ] psql shows `gin(search_tsv)`, `btree(year)`, `btree(ticker, year)` indexes present","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:45Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:45Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-dj6.1","depends_on_id":"convfinqa-dj6","type":"parent-child","created_at":"2026-05-10T21:51:45Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-dj6","title":"PRD: Document picker — backend documents schema + endpoints + frontend picker UI","description":"## Problem Statement\n\nThe ConvFinQA dataset (3,458 docs / 20 MB JSON) is the corpus the LLM reasons over. The frontend needs a snappy, searchable picker so users can pin a document to start a conversation. Today the dataset only lives on disk and the backend has no document endpoints — both halves need to be built.\n\n## Solution\n\nBackend: add a `documents` table (Postgres) with a generated `tsvector` for FTS, plus a btree on year. An idempotent seed script reads `data/convfinqa_dataset.json` and inserts rows. Two new GET endpoints: `/v1/documents` (paginated metadata + free-text + year-range search) and `/v1/documents/{id}` (full payload). Built via the hexagonal pattern — domain port + adapter + use case.\n\nFrontend: a `DocPicker` modal/dialog opened from the EmptyState CTA and from a sidebar button. Search box debounced via TanStack Pacer. Year-range slider. Virtualized result list via TanStack Virtual. Click a result → URL gets `?pinnedDocId=\u003cid\u003e` (pin handling itself is PRD 6).\n\n## User Stories\n\n1. As a user, I want to search across thousands of documents by ticker, title, or topic.\n2. As a user, I want to narrow results by year range.\n3. As a user, I want results to appear within ~100ms as I type.\n4. As a user, I want to see at least the title, ticker, year, and page on each result row.\n5. As a user, I want to paginate / infinite-scroll through results without UI lag.\n6. As a developer, I want a stable JSON contract on `/v1/documents` so the picker can swap UIs without backend changes.\n7. As an operator, I want a one-shot `make seed-docs` to (idempotently) load the dataset into Postgres.\n\n## Implementation Decisions\n\n- Schema: `documents(id text pk, ticker text, year int, page int, title text, pre_text text, post_text text, table_data jsonb, search_tsv tsvector GENERATED ALWAYS AS to_tsvector('english', coalesce(title,'')||' '||coalesce(ticker,'')||' '||coalesce(pre_text,'')) STORED)`. Indexes: `gin(search_tsv)`, `btree(year)`, `btree(ticker, year)`.\n- Title derivation at seed time: use the doc id's file slug + page number as a human-readable title.\n- Ticker derivation: regex out of the doc id's `Single_\u003cTICKER\u003e/\u003cyear\u003e/page_\u003cn\u003e.pdf-\u003cchunk\u003e` shape.\n- Endpoint: `GET /v1/documents?q=\u0026year_min=\u0026year_max=\u0026cursor=\u0026limit=` returning `{ items: [{id, ticker, year, page, title}], next_cursor: string|null, total: int? }`. `q` is run through `plainto_tsquery('english', $1)` and matched against `search_tsv`. Pagination is keyset on `(rank DESC, id ASC)` to keep it stable as the table is seeded.\n- Endpoint: `GET /v1/documents/{id}` returns the full row including `pre_text`, `post_text`, `table_data`.\n- Hexagonal layering: `domain/ports/documents_port.py` (Protocol), `application/use_cases/list_documents.py` and `get_document.py`, `adapters/persistence/documents_repo.py` (SQLAlchemy async), `entrypoints/api/routes/documents.py` (FastAPI).\n- Seeder: `backend/scripts/seed_documents.py`. Streams the JSON, batch-inserts with `ON CONFLICT (id) DO UPDATE`. Wired into `make seed-docs` and called automatically by `make up` after `alembic upgrade head`.\n- Frontend: `lib/queries/documents.ts` exposes `useDocumentList({q, yearMin, yearMax})` returning an infinite query keyed on filters. `DocPicker.tsx` opens as a shadcn `Dialog`, has the search input (debounced 200ms via TanStack Pacer), year-range slider, and virtualized result list.\n\n## Testing Decisions\n\n- Backend integration test: seed a sample of 20 docs into the testcontainer, query `/v1/documents?q=jkhy` → see JKHY rows; query with year_min=2010 → filter applies; pagination via cursor reaches every row exactly once.\n- Backend unit test: `list_documents` use case with a fake repo asserts the search and filter arguments are passed through.\n- Frontend unit: `lib/queries/documents.ts` — given a mocked fetch, the query builds the right URL.\n- Frontend component test: DocPicker renders, typing triggers a debounced refetch, clicking a result navigates with the URL param set.\n- playwright-bdd: `pick-document.feature` — open picker, type 'JKHY', see at least one result, refine by year, click result, URL contains `?pinnedDocId=`.\n\n## Out of Scope\n\n- Pinning behavior past setting the URL param (PRD 6).\n- Rendering the document table (PRD 6).\n- User-uploaded documents.\n- Editing or deleting documents.\n\n## Further Notes\n\nThe seeder must be idempotent so the alembic migration can run it in CI/dev/prod without re-fetching. Batch size of 200 rows per INSERT is plenty given the corpus size. Keep `pre_text` length capped at a sensible truncation when feeding into the FTS column (full text in the row, but only the first 2KB in the index) — current plan uses the whole thing, revisit if FTS index grows too large.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:44Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:44Z","labels":["afk","milestone:m2-core-chat"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-gbl.5","title":"Gherkin: empty app + collapse-persists scenarios","description":"Add `features/empty-app.feature` with: 'New user sees empty state' and 'Sidebar collapse persists across reload'.","acceptance_criteria":"- [ ] Both scenarios pass under playwright-bdd\n- [ ] Sidebar collapse scenario reloads the page and re-asserts collapsed state","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:43Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:43Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-gbl.5","depends_on_id":"convfinqa-gbl","type":"parent-child","created_at":"2026-05-10T21:51:43Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-gbl.4","title":"useSidebarStore (TanStack Store) + localStorage persistence","description":"Create `src/ui/sidebarStore.ts` using TanStack Store. State: `{ collapsed: boolean }`. Persist via custom localStorage middleware. Provide `useSidebarCollapsed()` hook.","acceptance_criteria":"- [ ] Unit test: toggle flips state\n- [ ] Unit test: persistence — second instance reads previous value from storage\n- [ ] Sidebar uses the hook and reflects collapsed state visually","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:42Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:42Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-gbl.4","depends_on_id":"convfinqa-gbl","type":"parent-child","created_at":"2026-05-10T21:51:41Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-gbl.3","title":"EmptyState + UserMenu (placeholder) components","description":"`components/EmptyState.tsx` renders mockup C's hero text + CTA 'Pin a document'. `components/UserMenu.tsx` uses shadcn `DropdownMenu` with avatar (initials), 'Sign out' item, placeholder 'Theme: dark/light' label (no toggle yet).","acceptance_criteria":"- [ ] EmptyState matches mockup C text\n- [ ] UserMenu opens, 'Sign out' is functional (calls signOut)\n- [ ] Avatar shows first 2 chars of userId","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:41Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:41Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-gbl.3","depends_on_id":"convfinqa-gbl","type":"parent-child","created_at":"2026-05-10T21:51:40Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-gbl.2","title":"Sidebar skeleton + brand + new-chat button + search","description":"Build `components/Sidebar.tsx` (presenter). Top: brand + collapse button. Middle: 'New conversation' primary button + search input. Bottom: scrollable area showing empty state 'No conversations yet'. Wire TanStack Virtual (trivial small list now).","acceptance_criteria":"- [ ] Vitest: Sidebar renders brand, button, empty message\n- [ ] Click 'New conversation' calls passed-in callback (no-op in this slice)\n- [ ] Search input is focusable and accepts text","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:40Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:40Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-gbl.2","depends_on_id":"convfinqa-gbl","type":"parent-child","created_at":"2026-05-10T21:51:39Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-gbl.1","title":"Three-panel grid layout in _authed/__root.tsx","description":"CSS grid: sidebar (resizable, default 280px), main (flex), right (collapsible to 0). The right column hidden when `?pinnedDocId` search param is absent. Use TanStack Router search params with zod schema.","acceptance_criteria":"- [ ] Grid renders correctly at \u003e=1024px viewport\n- [ ] Right column hidden when search param absent\n- [ ] TS-strict search param schema rejects invalid values","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:39Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:39Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-gbl.1","depends_on_id":"convfinqa-gbl","type":"parent-child","created_at":"2026-05-10T21:51:38Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-gbl","title":"PRD: Empty app shell + sidebar skeleton","description":"## Problem Statement\n\nAfter signing in, the user lands on `/app`. With no chats and no document pinned, the user needs an unambiguous \"what do I do next\" UI. The three-panel layout (sidebar / chat / right-panel) is established here so subsequent slices just fill in cells.\n\n## Solution\n\nRender the authed app layout with: (a) a left sidebar containing \"ConvFinQA\" brand, a \"New conversation\" button, an (empty) chat list grouped by document, and a search input — all skeleton; (b) a main column with an empty-state hero \"Pin a document to start chatting\" and a CTA to open the (yet-to-be-built) doc picker; (c) the right panel hidden until a doc is pinned. The sidebar is collapsible, with state persisted via TanStack Store.\n\n## User Stories\n\n1. As an authed user, I want to see the three-panel layout when I visit /app.\n2. As an authed user with no chats, I want a clear empty state telling me to pin a document.\n3. As an authed user, I want a \"New conversation\" button visible at the top of the sidebar.\n4. As an authed user, I want a search input in the sidebar (functionally inert this slice — wired in PRD 8).\n5. As an authed user, I want to collapse the sidebar to a thin rail to reclaim space.\n6. As an authed user, I want my sidebar collapse state to persist across reloads.\n7. As an authed user, I want a UserMenu (bottom-left or top-right) with sign-out and theme-toggle (toggle wired in PRD 10).\n\n## Implementation Decisions\n\n- Layout route `src/routes/_authed/__root.tsx` with three CSS grid columns. Right column collapses to width 0 until `pinnedDocId` URL search param is set.\n- Sidebar component is a stateless presenter that takes a grouped chat list (empty for now) plus callbacks. TanStack Virtual is wired but trivially small in this slice.\n- TanStack Store slice `ui/sidebarStore.ts` for `collapsed` + width. localStorage persistence via the store's middleware.\n- EmptyState renders inside the main column when there is no `chatId` URL param.\n- UserMenu component placeholder with avatar (initials from `userId`) and dropdown — uses shadcn `DropdownMenu`.\n- No data fetching this slice — Sidebar always passes `chats=[]`. The shape is locked so PRD 8 can swap in real data with zero component changes.\n\n## Testing Decisions\n\n- Vitest component tests for Sidebar (renders brand, button, empty state) and EmptyState.\n- Vitest test for `useSidebarStore` (default collapsed=false, toggle, persist to localStorage).\n- playwright-bdd scenarios: \"Empty app shows empty state\", \"Sidebar collapses and stays collapsed after reload\".\n\n## Out of Scope\n\n- Real chat list data (PRD 8).\n- Doc picker open/close (PRD 5).\n- Theme toggle wiring (PRD 10) — UserMenu has the placeholder slot only.\n- Right panel content (PRD 6).\n\n## Further Notes\n\nPixel target: mockup C (empty state, no doc pinned). All spacing, type sizes, and colors must come from the design tokens — no magic numbers.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:38Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:38Z","labels":["afk","milestone:m2-core-chat"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-ebw.5","title":"Sign-out affordance + tests","description":"Add a 'Sign out' menu item in a placeholder UserMenu component (will be enriched in PRD 4). Clicking it calls `auth.signOut()` and navigates to `/sign-in`.","acceptance_criteria":"- [ ] Click 'Sign out' clears localStorage userId\n- [ ] Gherkin scenario: 'Sign out lands on /sign-in'\n- [ ] Vitest: UserMenu click invokes auth.signOut","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:37Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:37Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-ebw.5","depends_on_id":"convfinqa-ebw","type":"parent-child","created_at":"2026-05-10T21:51:36Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-ebw.4","title":"Anonymous + authed layout routes with redirect guard","description":"Create `src/routes/_anonymous.tsx` (layout for /sign-in, /sign-up) and `src/routes/_authed.tsx` (guarded layout for /app and below). `_authed`'s `beforeLoad` calls `useAuth()` or reads storage and `throw redirect({ to: '/sign-in' })` if no userId. Anonymous redirects authed users to /app.","acceptance_criteria":"- [ ] Visiting `/app` while signed out lands on `/sign-in`\n- [ ] Visiting `/sign-in` while signed in lands on `/app`\n- [ ] Guard works on reload (storage is hydrated synchronously before route load)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:35Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:35Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-ebw.4","depends_on_id":"convfinqa-ebw","type":"parent-child","created_at":"2026-05-10T21:51:35Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-ebw.3","title":"Sign-up route — same skeleton, different copy","description":"Create `src/routes/_anonymous/sign-up.tsx` reusing the sign-in component shape. Title 'Create an account', primary 'Sign up' button, link 'Already have an account?' to `/sign-in`. Same stub behavior.","acceptance_criteria":"- [ ] /sign-up renders and matches mockup B\n- [ ] 'Sign up' click also signs in and lands on /app\n- [ ] Link to /sign-in works","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:34Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:34Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-ebw.3","depends_on_id":"convfinqa-ebw","type":"parent-child","created_at":"2026-05-10T21:51:34Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-ebw.2","title":"Sign-in route + form + Gherkin scenario","description":"Create `src/routes/_anonymous/sign-in.tsx`. Layout matches mockup A (centered card, email + password TanStack Form fields, primary 'Sign in' button, divider, 'Continue with Google' secondary button). Both buttons call `auth.signIn()` then `router.navigate({ to: '/app' })`. Write Gherkin: 'Email+password landing' and 'Continue with Google' scenarios.","acceptance_criteria":"- [ ] Vitest component test: renders inputs and both buttons; click 'Sign in' calls auth + navigates\n- [ ] Gherkin scenarios in `features/auth.feature` pass under playwright-bdd\n- [ ] Visual: matches mockup A tokens (dark theme by default)","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:33Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:33Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-ebw.2","depends_on_id":"convfinqa-ebw","type":"parent-child","created_at":"2026-05-10T21:51:33Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-ebw.1","title":"AuthProvider + useAuth hook + Vitest unit tests","description":"Implement `src/auth/AuthProvider.tsx`. Context value: `{ userId: string | null, signIn(): void, signOut(): void }`. `signIn` generates `dev-user-\u003cuuid\u003e`, writes to `localStorage['auth.userId']`. Hydrate from storage on mount. Wrap `RootRoute` in main.tsx.","acceptance_criteria":"- [ ] Unit test: initial userId is null when storage is empty\n- [ ] Unit test: signIn writes a uuid prefixed with `dev-user-` and exposes it\n- [ ] Unit test: signOut clears storage and exposes null","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:32Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:32Z","labels":["afk","milestone:m2-core-chat"],"dependencies":[{"issue_id":"convfinqa-ebw.1","depends_on_id":"convfinqa-ebw","type":"parent-child","created_at":"2026-05-10T21:51:32Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-ebw","title":"PRD: Sign-in stub + AuthProvider seam","description":"## Problem Statement\n\nThe mockups include sign-in and sign-up pages. Real Cognito + Google auth is explicitly out of scope for this session, but we still need (a) the visual sign-in flow and (b) a single seam so swapping in `react-oidc-context` later is a one-file change.\n\n## Solution\n\nRender `/sign-in` and `/sign-up` routes pixel-matching mockups A and B. Both \"Sign in\" and \"Continue with Google\" buttons write `auth.userId = \"dev-user-\u003cuuid\u003e\"` to localStorage and navigate to `/app`. A central `AuthProvider` exposes `useAuth()` returning `{ userId, signOut }`. A route guard on `/app/*` redirects to `/sign-in` if no userId in storage.\n\n## User Stories\n\n1. As a visitor, I want to land on the sign-in page if I am not authenticated.\n2. As a visitor, I want to click \"Sign in\" with any credentials and land on `/app`.\n3. As a visitor, I want to click \"Continue with Google\" and land on `/app` (visual stub only).\n4. As an authenticated user, I want my user id to persist across page reloads.\n5. As an authenticated user, I want a \"sign out\" affordance that clears the stored user id and returns me to `/sign-in`.\n6. As a developer, I want a single `useAuth()` hook that the rest of the app consumes for `X-User-Id`.\n\n## Implementation Decisions\n\n- AuthProvider lives in `frontend/src/auth/AuthProvider.tsx` with React context. Internally reads/writes localStorage key `auth.userId`. On mount, hydrates from storage. Exposes `signIn()` (writes a fresh uuid prefixed `dev-user-`) and `signOut()` (clears the key).\n- Route guard: a layout route `_authed.tsx` that calls `useAuth()`; if `userId` is null, it `redirect()`s to `/sign-in`.\n- Form library: TanStack Form for the email/password inputs. No validation beyond non-empty (we are visual-only).\n- AuthLayout (`_anonymous.tsx`) for the sign-in/sign-up shell — branded header, centered card.\n- Future Cognito swap point: replace `AuthProvider` internals with `react-oidc-context`'s `useAuth()` re-exposed under the same shape. Consumers do not change.\n\n## Testing Decisions\n\n- Vitest: hook unit test for `useAuth()` (initial state from storage, signIn writes a uuid, signOut clears).\n- Vitest: component test for the sign-in form (renders inputs, click \"Sign in\" calls auth.signIn and navigates).\n- playwright-bdd: `auth.feature` with two scenarios (email path, Google button path) asserting localStorage and final URL.\n\n## Out of Scope\n\n- Real OAuth code flow.\n- Token storage or refresh.\n- Cognito/Google credential validation.\n- Password reset / email verification flows.\n- Form validation errors (no-op).\n\n## Further Notes\n\nMockups A and B are the design source of truth. Buttons must be sized and colored exactly to the tokens defined in m1-foundation's globals.css. Sign-up route can reuse the sign-in component with different copy and a footer link.\n","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:31Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:31Z","labels":["afk","milestone:m2-core-chat"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.7","title":"Makefile targets + GitHub Actions frontend workflow","description":"Add `frontend-install`, `frontend-dev`, `frontend-test`, `frontend-e2e` to Makefile (each `cd frontend \u0026\u0026 pnpm ...`). Create `.github/workflows/frontend.yml` with `frontend-unit` (runs on every PR) and `frontend-e2e` (paths-filter on `frontend/**` and `backend/**`, uses Postgres service + alembic upgrade head + uv run main in background, then runs Playwright). Use `pnpm/action-setup@v4` (reads `packageManager` from `frontend/package.json`); cache the pnpm store path (`pnpm store path`) keyed on `frontend/pnpm-lock.yaml`. Also cache `~/.cache/ms-playwright`.","acceptance_criteria":"- [ ] `make frontend-test` runs vitest from repo root and exits 0\n- [ ] `make frontend-e2e` runs the full e2e flow locally\n- [ ] Workflow YAML lints cleanly; a sample PR (or local act run) shows both jobs green","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:30Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T22:31:32Z","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.7","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T21:51:30Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-0hu.7","depends_on_id":"convfinqa-0hu.9","type":"blocks","created_at":"2026-05-10T23:25:48Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.6","title":"Set up Playwright + playwright-bdd + smoke feature","description":"Install `@playwright/test`, `playwright-bdd`. Run `bunx playwright install chromium`. Create `playwright.config.ts` that runs `vite preview` as `webServer`, points `testDir` at `frontend/tests/e2e/.features-gen/` (generated by `bddgen`). Write `frontend/tests/e2e/features/boot.feature` with a single scenario asserting the placeholder renders, plus a minimal step-defs file.","acceptance_criteria":"- [ ] `bunx bddgen \u0026\u0026 bunx playwright test` exits 0 with the boot scenario passing\n- [ ] `features/` is human-authored Gherkin; `.features-gen/` is gitignored\n- [ ] Playwright report HTML is generated and gitignored","status":"open","priority":2,"issue_type":"task","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:29Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T20:51:29Z","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.6","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T21:51:29Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-0hu.6","depends_on_id":"convfinqa-0hu.9","type":"blocks","created_at":"2026-05-10T23:25:48Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.5","title":"Set up Vitest + smoke unit test","description":"Install `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`. Create `vitest.config.ts` with `jsdom` environment and `setupFiles` that import jest-dom. Write `src/routes/__tests__/index.test.tsx` that asserts the placeholder string renders.","acceptance_criteria":"- [ ] `bunx vitest run` exits 0 with at least one passing test\n- [ ] Test output uses TanStack Router's `createMemoryHistory` for routing in jsdom\n- [ ] Adding `*.test.tsx` next to a component picks it up automatically","status":"in_progress","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:28Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T23:23:01Z","started_at":"2026-05-10T23:23:01Z","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.5","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T21:51:27Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-0hu.5","depends_on_id":"convfinqa-0hu.9","type":"blocks","created_at":"2026-05-10T23:25:47Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.4","title":"Configure shadcn/ui base + Vite dev proxy","description":"Run `bunx shadcn@latest init`, picking the design tokens to reference our CSS vars. Do NOT copy any components yet. In `vite.config.ts`, add `server.proxy['/v1'] = { target: 'http://localhost:8000', changeOrigin: true }`.","acceptance_criteria":"- [ ] `components.json` exists, references our globals.css path\n- [ ] `fetch('/v1/healthz')` from the running dev server returns 200 with no CORS error\n- [ ] `bunx shadcn add button` (sample) works without errors (revert after verification)","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:27Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T23:16:11Z","started_at":"2026-05-10T22:59:48Z","closed_at":"2026-05-10T23:16:11Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.4","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T21:51:26Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-0hu.4","depends_on_id":"convfinqa-0hu.9","type":"blocks","created_at":"2026-05-10T23:25:46Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.3","title":"Install + wire TanStack Router + Query","description":"Install `@tanstack/react-router`, `@tanstack/router-plugin`, `@tanstack/router-devtools`, `@tanstack/react-query`, `@tanstack/react-query-devtools`. Add the router plugin to Vite. Create `src/routes/__root.tsx` and `src/routes/index.tsx` (placeholder). Wire `QueryClientProvider` in `main.tsx`. Routes generated to `src/routeTree.gen.ts` (gitignored).","acceptance_criteria":"- [ ] Visiting `/` renders the placeholder via TanStack Router\n- [ ] React Query devtools open in dev with no errors\n- [ ] `routeTree.gen.ts` is generated and ignored by git","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:26Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T22:58:12Z","started_at":"2026-05-10T22:51:09Z","closed_at":"2026-05-10T22:58:12Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.3","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T21:51:25Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-0hu.3","depends_on_id":"convfinqa-0hu.9","type":"blocks","created_at":"2026-05-10T23:25:45Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.2","title":"Install + configure Tailwind v4 with theme tokens","description":"Install `tailwindcss@^4` and `@tailwindcss/vite`. Add `tailwindcss()` to `vite.config.ts`. Create `src/styles/globals.css` with `@import 'tailwindcss'` and a `@theme` block defining both themes (dark default + light alt) as CSS custom properties extracted from the example-html mockups: bg-page, text-primary, text-secondary, accent, plus Inter + JetBrains Mono font-family imports.","acceptance_criteria":"- [ ] `\u003chtml data-theme=\"dark\"\u003e` and `\u003chtml data-theme=\"light\"\u003e` both render different color palettes from the same component code\n- [ ] Inter loads for body text, JetBrains Mono for numeric utility class\n- [ ] No `tailwind.config.js` file exists (CSS-first config)","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:25Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T22:49:52Z","started_at":"2026-05-10T22:43:34Z","closed_at":"2026-05-10T22:49:52Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.2","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T21:51:24Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-0hu.2","depends_on_id":"convfinqa-0hu.9","type":"blocks","created_at":"2026-05-10T23:25:45Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu.1","title":"Scaffold frontend/ workspace with Vite + React + TS","description":"`cd backend \u0026\u0026 cd .. \u0026\u0026 bun create vite frontend -- --template react-ts` (manually if needed). Set up `package.json`, `tsconfig.json`, `tsconfig.node.json`, `index.html`, `src/main.tsx`, `src/App.tsx` placeholder. Strict TypeScript on.","acceptance_criteria":"- [ ] `cd frontend \u0026\u0026 bun install` succeeds\n- [ ] `cd frontend \u0026\u0026 bun run dev` serves on localhost:5173\n- [ ] `cd frontend \u0026\u0026 bun run build` produces a clean dist/ with no TS errors","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:23Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T21:42:53Z","started_at":"2026-05-10T21:34:43Z","closed_at":"2026-05-10T21:42:53Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-0hu.1","depends_on_id":"convfinqa-0hu","type":"parent-child","created_at":"2026-05-10T21:51:23Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-0hu","title":"PRD: Frontend foundation — Vite + React + TanStack + tests + CI","description":"## Problem Statement\n\nThere is no frontend yet. We need a clean React+Vite+TypeScript SPA scaffold that subsequent vertical slices can build on, with the test harness and CI green from day one.\n\n## Solution\n\nScaffold a new `frontend/` workspace using Vite 7 + React 19 + TypeScript. Install and configure: TanStack Router (file-based via Vite plugin), TanStack Query, Tailwind v4 with design-token CSS vars for both themes, shadcn/ui base config, Vitest + @testing-library/react, Playwright + playwright-bdd. Configure a Vite dev proxy for `/v1/*` to `http://localhost:8000`. Add `Makefile` targets and a GitHub Actions workflow with two new jobs: `frontend-unit` (always) and `frontend-e2e` (on frontend or backend changes).\n\n## User Stories\n\n1. As a developer, I want `cd frontend \u0026\u0026 pnpm dev` to serve the app on `http://localhost:5173`.\n2. As a developer, I want API requests under `/v1/*` to proxy to the FastAPI backend without CORS errors in dev.\n3. As a developer, I want `pnpm exec vitest run` and `pnpm exec playwright test` to run from `frontend/`.\n4. As a developer, I want `make frontend-test` and `make frontend-e2e` for parity with backend make targets.\n5. As CI, I want the frontend unit job to run on every PR and the e2e job to run only on frontend/backend changes.\n6. As a future contributor, I want the design tokens for the dark and light themes already defined as CSS vars in `globals.css`.\n\n## Implementation Decisions\n\n- Package manager: pnpm (\u003e= 10.28). Supply-chain hardening (`minimumReleaseAge: 10080`, `blockExoticSubdeps: true`) lives in `frontend/pnpm-workspace.yaml`. `packageManager` field in `package.json` pins the exact version so CI/corepack agree.\n- Vite plugin set: `@vitejs/plugin-react`, `@tanstack/router-plugin/vite`, `tailwindcss/vite` (v4 plugin).\n- TanStack Router file-based: routes live under `frontend/src/routes/`, generated tree in `frontend/src/routeTree.gen.ts`.\n- Tailwind v4: CSS-first config via `@theme` block in `globals.css`. No `tailwind.config.js`.\n- shadcn/ui: initialize `components.json` with a custom theme that references our CSS vars. Do NOT copy any components yet — that happens in later slices.\n- Vitest: `jsdom` environment, `@testing-library/react`, co-located `*.test.tsx`.\n- Playwright-bdd: `frontend/tests/e2e/features/*.feature` + step defs in `frontend/tests/e2e/steps/*.ts`. `pnpm exec bddgen` generates Playwright specs before each run.\n- CI cache: pnpm store path (`pnpm store path`) keyed on `frontend/pnpm-lock.yaml`, and `~/.cache/ms-playwright`.\n- A placeholder landing route at `/` that renders \"ConvFinQA — coming online\" so smoke tests have something to assert.\n\n## Testing Decisions\n\n- Smoke test (Vitest): renders the root route, asserts the placeholder string.\n- Smoke test (playwright-bdd): one Gherkin scenario `Feature: App boots / Scenario: Root route renders` that visits `/` and sees the placeholder.\n- CI: both must be green before merging the foundation PR. No skipped tests.\n\n## Out of Scope\n\n- Auth UI (PRD 3).\n- App shell components (PRD 4).\n- Any feature route — only the placeholder landing.\n- shadcn component copy-ins (each slice copies what it needs).\n\n## Further Notes\n\nThe Makefile targets `frontend-install`, `frontend-dev`, `frontend-test`, `frontend-e2e` are convenience aliases that `cd frontend \u0026\u0026 pnpm ...`. The CI workflow YAML uses `pnpm/action-setup@v4` (reads `packageManager` from `frontend/package.json`).","notes":"## Codebase Patterns (added 2026-05-10)\n- Frontend lives at repo-root `frontend/` (sibling of `backend/`). Package manager: pnpm (\u003e= 10.28). Local install: `cd frontend \u0026\u0026 pnpm install --frozen-lockfile`.\n- Supply-chain hardening lives in `frontend/pnpm-workspace.yaml`: `minimumReleaseAge: 10080` (1-week release-age gate) and `blockExoticSubdeps: true` (refuses git/http/file: transitive deps). `packageManager` in `package.json` pins the exact pnpm version so corepack/CI agree.\n- Strict TypeScript baseline in tsconfig.{app,node}.json: `strict` + `noUncheckedIndexedAccess` + `noImplicitOverride` (on top of Vite scaffold's `noUnusedLocals/Parameters/erasableSyntaxOnly/noFallthroughCasesInSwitch`).\n- Placeholder copy is \"ConvFinQA — coming online\" — keep this string so smoke tests in tasks 0hu.5 / 0hu.6 stay stable.\n- Root `.dockerignore` excludes `frontend/`; if the SPA ever needs serving from the backend image, add a multi-stage build instead of removing the ignore.\n- Scaffolded versions (May 2026): React 19.2 · TS 6.0 · Vite 8 · @vitejs/plugin-react 6 · ESLint 10 · typescript-eslint 8.59. Newer than PRD text; revisit if regression seen.\n- Vite scaffold drops a `frontend/.gitignore` that already covers `node_modules`, `dist`, `dist-ssr`, `*.log`, `.vscode/*`. Add `routeTree.gen.ts` and `playwright-report/`/`test-results/` in later tasks.\n\n## Codebase Patterns (added 2026-05-10, migration)\n- Local pnpm rule (`.claude/rules/frontend/package-manager.md`) has NO `paths:` filter — always attaches in this repo. Use `pnpm`/`pnpm exec`/`pnpm dlx`; never `bun`/`bunx`/`npm`/`yarn`.\n- `pnpm install` honours `minimumReleaseAge: 10080` (workspace YAML). Recently-published versions will be skipped in favour of the most recent version that is at least 1 week old. Expect pnpm output to list \"X.Y.Z is available\" for very fresh releases — that's the gate working, not an error.\n- CI: use `pnpm/action-setup@v4` (it reads `packageManager` from `package.json`). Never `oven-sh/setup-bun` or `actions/setup-node` with npm caching. Cache the pnpm store via `pnpm store path` keyed on `frontend/pnpm-lock.yaml`.\n## Codebase Patterns (added 2026-05-10, TanStack)\n- TanStack Router/Query versions installed: react-router 1.169.1 · react-query 5.100.9 · react-router-devtools 1.166.13 · react-query-devtools 5.100.9 · router-plugin 1.167.32. Release-age gate (10080) downgraded fresh patches as expected (e.g. react-router 1.169.2 noted available).\n- vite.config plugin order is load-bearing: tanstackRouter() MUST precede react(). Reverse = stale route tree / HMR weirdness.\n- autoCodeSplitting: true splits every route's `component` into a separate chunk via `?tsr-split=component`. Test mounts and Playwright must await router-rendered DOM (`findByText`, not `getByText`), since the chunk loads asynchronously.\n- Routes use createFileRoute() → `src/routes/__root.tsx` (with createRootRouteWithContext for QueryClient plumbing) + `src/routes/index.tsx` (placeholder). routeTree.gen.ts is generated by the plugin and is gitignored + eslint-ignored.\n- main.tsx wires QueryClientProvider OUTSIDE RouterProvider. Router gets context { queryClient } so loaders can `useQueryClient()` without prop-drilling. Type registration via `declare module \"@tanstack/react-router\" { interface Register { router: typeof router } }`.\n- Devtools (router + query) live in __root.tsx behind `import.meta.env.DEV`. Do NOT mount them in main.tsx — keeps the production bundle free of devtools code.\n- ESLint: `react-refresh/only-export-components` is `off` for src/routes/**. The plugin's allowExportNames doesn't help because route components are referenced (not exported) — they trip the \"localComponents\" branch. TanStack's vite plugin already injects its own __TSR_REACT_REFRESH__ shim that makes Route hot-reload-safe.\n## Codebase Patterns (added 2026-05-11, shadcn base)\n- shadcn/ui base config landed in 0hu.4. components.json points `tailwind.css` → `src/styles/globals.css`, aliases `@/*` → `src/*`, baseColor `neutral` (token names match our orange palette so utilities use brand colors). `lib/utils.ts` exports `cn()` (clsx + tailwind-merge).\n- BLOCKER for shadcn add: `pnpm-workspace.yaml` `packages: [.]` triggers shadcn's monorepo gate. Workaround until 0hu.10 resolves: `mv pnpm-workspace.yaml pnpm-workspace.yaml.bak; pnpm dlx shadcn@latest add \u003ccomponent\u003e; mv pnpm-workspace.yaml.bak pnpm-workspace.yaml`. `*.bak` is gitignored.\n- Theming integration: globals.css has `@custom-variant dark (\u0026:where([data-theme=\"dark\"], [data-theme=\"dark\"] *));` — shadcn's `dark:` Tailwind variants fire when an ancestor (or self) has `data-theme=\"dark\"`, NOT on `.dark` class or media query. `:where()` keeps zero specificity so component classes win.\n- shadcn presets (radix-nova, etc.) want extras we don't use: `@fontsource-variable/geist` (we use Inter), `shadcn` package (its tailwind.css overrides our theme), `radix-ui` (only when a component imports Slot/etc.). Audit deps post-init and prune to: clsx, tailwind-merge, class-variance-authority, lucide-react, tw-animate-css.\n- Vite proxy in vite.config.ts: `/v1`, `/healthz`, `/readyz` → http://localhost:8000 with `changeOrigin: true`. Shared `backendProxy` const. NOT just `/v1` because backend's health endpoints live at root (k8s convention) — see 0hu.11.\n- TS 6 deprecates `baseUrl`. `paths: { \"@/*\": [\"./src/*\"] }` works without `baseUrl` and resolves relative to the tsconfig file. Both tsconfig.json (shadcn reads this) and tsconfig.app.json (tsc -b for src/) need it.\n- ESLint `react-refresh/only-export-components: off` is now scoped to BOTH `src/routes/**` AND `src/components/ui/**`. shadcn components export Component + variants constant — same root cause as the route-component pattern.\n- Tokens our globals.css does NOT define (yet): `--color-destructive`, `--color-destructive-foreground`, sidebar-* tokens, chart-N tokens. Add per-slice when the first component referencing them lands. shadcn components compile fine without them; the tokens just resolve to empty `var()`.\n## Codebase Patterns (added 2026-05-11, shadcn unblock)\n- frontend/pnpm-workspace.yaml MUST NOT contain a 'packages:' key for the current single-package layout. Keep only 'minimumReleaseAge: 10080' + 'blockExoticSubdeps: true'. shadcn CLI v4.7 refuses every command if 'packages:' is present — even 'packages: [.]' trips its 'monorepo root' gate.\n- 'pnpm dlx shadcn@latest add \u003ccomponent\u003e' works directly with no workaround. The mv-pnpm-workspace.yaml-aside dance from 0hu.4 is OBSOLETE — do not propagate it.\n- Re-adding 'packages:' is only legitimate when the layout actually becomes multi-package (e.g. frontend/apps/web + frontend/packages/ui); then invoke shadcn with '-c apps/web'.","status":"open","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:22Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T23:21:39Z","labels":["afk","milestone:m1-foundation"],"comments":[{"id":"019e13d8-103a-7167-8d0a-bcb5a9623115","issue_id":"convfinqa-0hu","author":"Francesco Albanese","text":"# 2026-05-10 (milestone: milestone:m1-foundation)\n- **Task completed**: Scaffold frontend/ workspace with Vite + React + TS (convfinqa-0hu.1)\n- **Files changed**: frontend/ (new — bun create vite, src/{App,main}.tsx, src/index.css, index.html, tsconfig.{app,node,json}, vite.config.ts, eslint.config.js, package.json, bun.lock, public/favicon.svg, README.md, .gitignore), .dockerignore (add frontend/)\n- **Decisions made**:\n  - Bun was not pre-installed; ran the official installer to ~/.bun. Subsequent foundation tasks (and CI) need bun on PATH.\n  - bun create vite picked React 19.2 + Vite 8 + TS 6 (newer than PRD-stated Vite 7). Acceptance criteria are version-agnostic and all three pass. Flag if downgrade is wanted.\n  - Strict TS: enabled strict + noUncheckedIndexedAccess + noImplicitOverride in both tsconfig.app.json and tsconfig.node.json.\n  - Deleted Vite demo assets (hero.png/react.svg/vite.svg/icons.svg sprite/App.css) so next tasks start from a clean slate; Tailwind/theme tokens land in 0hu.2.\n  - .dockerignore: added frontend/ so backend image doesn't pull the SPA tree.\n- **Blockers encountered**: bun install required `bun.sh` host access (sandbox bypass on first install + bun create vite).\n- **Architectural decisions**: None — placeholder App, no router/query/tailwind yet (they own their own tasks).\n- **Notes for next iteration**:\n  - 0hu.2 (Tailwind v4): import in src/index.css with @import 'tailwindcss' + @theme block; current index.css is a tiny reset (overwrite freely). example-html/ has the dark/light palettes.\n  - 0hu.3 (TanStack Router): add `@tanstack/router-plugin/vite` to vite.config.ts, gitignore `frontend/src/routeTree.gen.ts`.\n  - 0hu.4 (shadcn): proxy `/v1` → `http://localhost:8000` in vite.config.ts server.proxy.\n  - Bun path: PRD assumes `bun` on PATH. CI workflow (0hu.7) should use `oven-sh/setup-bun@v2`. Local dev: `~/.bun/bin/bun`.\n  - All three acceptance criteria verified: install (154 pkgs), dev (HTTP 200 on :5173 with `\u003ctitle\u003eConvFinQA\u003c/title\u003e` + App.tsx serves placeholder), build (`tsc -b \u0026\u0026 vite build` → dist/ in 74ms, no TS errors).","created_at":"2026-05-10T21:43:08Z"},{"id":"019e1406-4385-7d00-9121-da588b446921","issue_id":"convfinqa-0hu","author":"Francesco Albanese","text":"# 2026-05-10 (milestone: milestone:m1-foundation)\n- **Task completed**: Migrate frontend from Bun to pnpm + supply-chain hardening (convfinqa-0hu.9)\n- **Files changed**: frontend/{package.json, README.md, pnpm-lock.yaml (new), pnpm-workspace.yaml (new), bun.lock (deleted)}, plus pre-staged .claude/rules/frontend/package-manager.md + .gitignore + .beads/issues.jsonl\n- **Decisions made**:\n  - Pinned packageManager to 'pnpm@10.33.2' (locally installed version). Corepack will activate this exact version in CI.\n  - Supply-chain hardening lives in pnpm-workspace.yaml (not .npmrc) — single source of truth, version-controlled, applies to whole workspace.\n  - Kept README example commands aligned with the new flow (`pnpm install --frozen-lockfile`, `pnpm dev`, `pnpm build`, `pnpm lint`).\n  - Updated 0hu.7 (Makefile/CI) and 0hu.8 (Biome/Lefthook) descriptions inline — bun → pnpm, bunx → pnpm exec, oven-sh/setup-bun → pnpm/action-setup@v4, cache key on pnpm-lock.yaml + pnpm store path.\n- **Blockers encountered**: None. pnpm install needed sandbox bypass (network + ~/Library/pnpm store writes).\n- **Architectural decisions**:\n  - 1-week release-age gate (`minimumReleaseAge: 10080`) is mandatory and silently downgrades fresh patches — expect 'X.Y.Z is available' lines from pnpm output (e.g. react 19.2.5 installed while 19.2.6 noted available). Not an error.\n  - `blockExoticSubdeps: true` refuses git/http/file: transitive deps — any future PR that pulls one in will fail install. Document in 0hu.7 CI runbook if it bites.\n- **Notes for next iteration**:\n  - 0hu.2 (Tailwind v4): install via `pnpm add tailwindcss @tailwindcss/vite`.\n  - 0hu.3 (TanStack Router): `pnpm add @tanstack/react-router @tanstack/react-query` + `-D @tanstack/router-plugin`.\n  - 0hu.4 (shadcn): `pnpm dlx shadcn@latest init`; proxy /v1 → http://localhost:8000 in vite.config.ts server.proxy.\n  - Acceptance verified: `pnpm install --frozen-lockfile` from clean node_modules → 154 pkgs in 568ms; `pnpm build` → dist/ in 84ms; `pnpm dev` → :5173 serves index with \u003ctitle\u003eConvFinQA\u003c/title\u003e.\n  - The release-age gate means CI scaffolds in later tasks may install slightly older patches than developers see when they do an interactive install of a fresh dep — fine, lockfile pins it.","created_at":"2026-05-10T22:33:35Z"},{"id":"019e1415-881c-74ca-b876-bef8bbbf1aac","issue_id":"convfinqa-0hu","author":"Francesco Albanese","text":"# 2026-05-10 (milestone: milestone:m1-foundation)\n- **Task completed**: Install + configure Tailwind v4 with theme tokens (convfinqa-0hu.2)\n- **Files changed**: frontend/{package.json, pnpm-lock.yaml, vite.config.ts, index.html, src/App.tsx, src/main.tsx, src/styles/globals.css (new — replaces src/index.css)}\n- **Decisions made**:\n  - Pivoted from PRD's literal token names (bg-page/text-primary/text-secondary/accent) to shadcn-aligned semantic names (background/foreground/card/popover/primary/secondary/muted/accent/border/input/ring + foregrounds). Reason: 0hu.4 will install shadcn, which expects these exact tokens — this keeps that task a true no-op instead of a rename refactor.\n  - Direct token values in @theme (no var() indirection) per Tailwind v4 idiom: dark palette baked into @theme; [data-theme=\"light\"] block overrides every --color-* var so utilities re-evaluate automatically.\n  - Default theme is dark (declared in @theme + explicit data-theme=\"dark\" on \u003chtml\u003e in index.html for the uf5 toggle slice).\n  - Custom @utility numeric { font-family: var(--font-mono); font-variant-numeric: tabular-nums slashed-zero } — semantic primitive for financial figures (slice 9ye consumes this).\n  - Fonts via @fontsource-variable/inter + @fontsource-variable/jetbrains-mono (variable woff2, ~50KB each subset). Single subset auto-loaded; build emits 12 woff2 files but only latin is requested by default for the placeholder page.\n  - App.tsx demos themed surfaces (bg-background + text-foreground wrapper, text-muted-foreground subtitle, text-accent + numeric on a sample figure) so the data-theme swap is visibly different from the same component code.\n  - Placeholder copy 'ConvFinQA — coming online' kept verbatim — 0hu.5/0hu.6 smoke tests will assert on it.\n- **Blockers encountered**: pnpm install + dev server needed sandbox bypass (pnpm store writes to ~/Library/pnpm + Vite network listen).\n- **Architectural decisions**:\n  - shadcn token namespace adopted PRE-shadcn — every color token is --color-\u003cname\u003e via @theme so Tailwind v4 generates bg-/text-/border- utilities automatically. 0hu.4's components.json baseColor='neutral' or 'stone' will slot into these without renames.\n  - Token palette extracted from example-html (C dark / F light): bg surfaces 0B0E14→1D2330 (dark) / FAF8F4→FFFFFF (light); accent orange E8973C dark vs C2671D light (AA contrast on cream); accent-link 7CC4FF/2E6FB8 reserved for hyperlinks (--color-link, not yet utility-bound).\n- **Notes for next iteration**:\n  - 0hu.3 (TanStack Router/Query): `pnpm add @tanstack/react-router @tanstack/react-query @tanstack/router-devtools \u0026\u0026 pnpm add -D @tanstack/router-plugin`. Add routerPlugin BEFORE react() in vite.config.ts plugins[]. gitignore frontend/src/routeTree.gen.ts.\n  - 0hu.4 (shadcn): `pnpm dlx shadcn@latest init` — when it asks about CSS variables, choose YES (we already have the shadcn-aligned vars). It will write components.json + may add a couple of compat helpers (clsx, tailwind-merge). Proxy /v1 → http://localhost:8000 in vite.config.ts server.proxy.\n  - Acceptance verified: `pnpm build` clean (dist/assets/index-*.css 14.48 kB gz 5.40 kB), `pnpm lint` clean, dev server boots :5174 in 153ms, served HTML carries data-theme=\"dark\" and emitted CSS contains both @theme color tokens and [data-theme=\"light\"] override block; `tailwind.config.*` confirmed absent (CSS-first config).\n  - The numeric utility is wired and visible on the placeholder (,234,567.89 in mono+tabular). Slice 9ye should adopt it for the financial table cells.\n  - --color-link is set but no utility is bound — Tailwind generates text-link/bg-link/etc. automatically because the var is in @theme. Anchor tags can use `text-link hover:underline`.","created_at":"2026-05-10T22:50:16Z"},{"id":"019e141d-1ea5-7bfe-b3d3-6972e228f99f","issue_id":"convfinqa-0hu","author":"Francesco Albanese","text":"# 2026-05-10 (milestone: milestone:m1-foundation)\n- **Task completed**: Install + wire TanStack Router + Query (convfinqa-0hu.3)\n- **Files changed**: frontend/{package.json, pnpm-lock.yaml, .gitignore, eslint.config.js, vite.config.ts, src/main.tsx, src/routes/__root.tsx (new), src/routes/index.tsx (renamed from src/App.tsx), src/App.tsx (deleted)}\n- **Decisions made**:\n  - Plugin order matters: tanstackRouter() BEFORE react() in vite.config plugins[]. Reversed = stale route tree / HMR weirdness.\n  - Package name drift in task description: actual published name is @tanstack/react-router-devtools (not @tanstack/router-devtools). Installed the correct one.\n  - Chose router context plumbing (createRootRouteWithContext\u003c{ queryClient }\u003e() + context: { queryClient }) over plain QueryClientProvider-wraps-Router. ~5 extra lines now, saves a refactor in rb3/fxn when loaders need useQueryClient.\n  - autoCodeSplitting: true on the router plugin — every route's `component` becomes a separate chunk via `?tsr-split=component`. Beyond literal PRD ask but TanStack's default best practice and visible in built bundle (routes-*.js chunk separate from main).\n  - Deleted App.tsx — moved its placeholder JSX verbatim into routes/index.tsx, so smoke tests in 0hu.5/0hu.6 still assert on \"ConvFinQA — coming online\" (em-dash, not hyphen).\n  - Devtools live in __root.tsx behind `import.meta.env.DEV`, NOT in main.tsx. Both \u003cTanStackRouterDevtools /\u003e and \u003cReactQueryDevtools /\u003e are dev-only.\n- **Blockers encountered**: pnpm registry needed sandbox bypass for install + version queries. None functional.\n- **Architectural decisions**:\n  - eslint config: scoped `react-refresh/only-export-components: \"off\"` for `src/routes/**`. TanStack's plugin already handles HMR for `Route` exports via its own `__TSR_REACT_REFRESH__` shim (visible in served bundle). The plugin's allowExportNames did not suppress — root cause is that the route component (LandingPage) is declared but not exported (it's referenced via Route.component), tripping the \"localComponents\" branch of the rule. Disabling on routes/** is the clean fix.\n  - QueryClient is instantiated at module scope in main.tsx. Fine for SPA. If SSR/RSC ever enters scope, move it inside a component to avoid cross-request leakage.\n- **Notes for next iteration**:\n  - 0hu.4 (shadcn): `pnpm dlx shadcn@latest init`. components.json baseColor='neutral' or 'stone' — our shadcn-aligned tokens from 0hu.2 are already in place so init should be a no-op for colors. Proxy /v1 → http://localhost:8000 in vite.config.ts server.proxy.\n  - 0hu.5 (Vitest): Route components are LAZY (autoCodeSplitting). Smoke test must await async router rendering — `await screen.findByText(...)` not `screen.getByText(...)`. Mount via @testing-library/react with `\u003cRouterProvider router={createRouter({ routeTree })} /\u003e`. The placeholder string \"ConvFinQA — coming online\" lives in routes/index.tsx — em-dash verbatim.\n  - 0hu.6 (Playwright): same string. Also: browser-level verification of \"React Query devtools open with no errors\" was DEFERRED from 0hu.3 — add an explicit scenario or step that clicks the devtools toggle and asserts no console errors. Build-time/lint-time clean ≠ runtime clean.\n  - 0hu.8 (Biome/Lefthook): when migrating ESLint → Biome, the routes/** rule override carries over conceptually — Biome's correctness rules likely won't fire, but if you re-enable any \"exports must be components\" rule, scope it the same way.\n  - Acceptance verified: pnpm build clean (167 modules, JS 299KB, CSS 14.48KB); pnpm lint clean (0 errors / 0 warnings); routeTree.gen.ts generated by vite plugin and confirmed ignored via git check-ignore; dev server on :5173 serves the placeholder via split chunk (grep \"coming online\" → 1 hit in `?tsr-split=component`).","created_at":"2026-05-10T22:58:33Z"},{"id":"019e142d-f4a1-7dcf-9fdd-cca280e37a08","issue_id":"convfinqa-0hu","author":"Francesco Albanese","text":"# 2026-05-11 (milestone: milestone:m1-foundation)\n- **Task completed**: Configure shadcn/ui base + Vite dev proxy (convfinqa-0hu.4)\n- **Files changed**: frontend/{components.json (new), src/lib/utils.ts (new), src/styles/globals.css, vite.config.ts, tsconfig.json, tsconfig.app.json, eslint.config.js, package.json, pnpm-lock.yaml, .gitignore}\n- **Decisions made**:\n  - shadcn CLI v4.7 refuses every command when frontend/pnpm-workspace.yaml has `packages: [.]` (\"monorepo root, use -c flag\" — but `-c .` doesn't help because . == monorepo root). Diagnosed: gate is the `packages:` key itself, not the file. Filed convfinqa-0hu.10 for user decision (keep rule + dance, drop `packages: [.]`, or restructure). Workaround for this task: temporarily `mv pnpm-workspace.yaml{,.bak}` around the shadcn add call, restore after. Saved as bd memory `shadcn-cli-v4-7-monorepo-gate-presence-of` for next agent.\n  - shadcn add chose preset \"radix-nova\" (Radix base + Lucide icons + Geist font). Pruned unwanted preset extras post-hoc: removed `radix-ui` (only needed by the reverted button), `@fontsource-variable/geist` (we use Inter), `shadcn` package itself (was added because globals.css imported \"shadcn/tailwind.css\" — restored globals.css drops that import). Kept the BASE: components.json, src/lib/utils.ts, clsx, tailwind-merge, class-variance-authority, lucide-react, tw-animate-css.\n  - shadcn rewrote globals.css with its own oklch neutral palette in `:root` + `@theme inline` redirection block — fully restored our custom orange theme via `git restore` and added only the minimum needed for shadcn-component compatibility:\n    - `@import \"tw-animate-css\";` (used by Dialog/Sheet/Toast in later slices)\n    - `@custom-variant dark (\u0026:where([data-theme=\"dark\"], [data-theme=\"dark\"] *));` so shadcn's `dark:` Tailwind variants fire on our `data-theme=\"dark\"` attribute (vs default media-query behaviour). `:where()` keeps zero specificity so component styles can override.\n  - tsconfig: shadcn requires path aliases. Added `paths: { \"@/*\": [\"./src/*\"] }` to BOTH tsconfig.json and tsconfig.app.json (shadcn checks tsconfig.json; tsc -b needs it in app.json for src). NO `baseUrl` — TS 6 deprecates it (TS5101 error); `paths` works without `baseUrl` in TS 6+.\n  - Vite alias: mirrored `@/*` → `src/*` via `resolve.alias` so runtime imports resolve.\n  - Vite proxy: PRD said `/v1` only. Backend has `/healthz` and `/readyz` UNVERSIONED (k8s convention) and `/v1/*` for chat router. Added proxy entries for ALL THREE so any backend endpoint works through dev server. PRD criterion 2 (`fetch('/v1/healthz')` returns 200) cannot pass literally — `/v1/healthz` returns 404 from backend. Filed convfinqa-0hu.11 for user decision (add /v1/healthz to backend, amend PRD criterion 2, or accept the gap).\n  - eslint: extended the `react-refresh/only-export-components: off` override to `src/components/ui/**` (previously just `src/routes/**`). shadcn components export both Button + buttonVariants — the rule fires on the cva constant export. Same root-cause as the routes/** override.\n  - .gitignore: added `*.bak` so the temporary pnpm-workspace.yaml.bak from the shadcn add dance can never get committed.\n- **Acceptance gap (user attention required)**:\n  - Criterion 2 (`fetch('/v1/healthz')` 200): SPIRIT met (proxy works, no CORS — verified `/healthz` and `/readyz` return 200 through Vite at :5175). LITERAL not met (backend has no `/v1/healthz`). See 0hu.11.\n- **Blockers encountered**: shadcn CLI sandbox bypass needed for npm registry (dlx) and pnpm store writes. Functional blocker on `packages: [.]` documented above.\n- **Architectural decisions**:\n  - Hybrid shadcn integration: keep our direct-value `@theme` block (orange brand colors, `[data-theme=\"light\"]` overrides, `numeric` utility) — do NOT adopt shadcn's `:root`+`@theme inline` indirection. Reason: our token names are already shadcn-aligned (background/foreground/card/etc. — set up in 0hu.2) so utilities resolve to our brand colors. shadcn components that reference tokens we don't define yet (e.g. `--color-destructive`, sidebar tokens, chart-N) will gracefully fail with empty CSS vars; add them in the slice that introduces the consuming component.\n  - `dark:` variant maps to `data-theme=\"dark\"` (not `.dark` class, not media query). Keeps our existing toggle plan (uf5) viable without rewriting every component.\n- **Notes for next iteration**:\n  - 0hu.5 (Vitest) and 0hu.6 (Playwright): smoke test still asserts on \"ConvFinQA — coming online\" in routes/index.tsx (em-dash verbatim). No change.\n  - For ANY future `pnpm dlx shadcn@latest add \u003ccomponent\u003e`: until 0hu.10 is resolved, do `mv pnpm-workspace.yaml pnpm-workspace.yaml.bak; pnpm dlx shadcn@latest add \u003ccomponent\u003e; mv pnpm-workspace.yaml.bak pnpm-workspace.yaml`. Or have the user resolve 0hu.10 first.\n  - When the FIRST shadcn component lands that uses destructive variant (likely Toast in bmu, or Dialog in bqx), add `--color-destructive` and `--color-destructive-foreground` to both `@theme` and `[data-theme=\"light\"]` blocks in globals.css.\n  - Acceptance verified: `pnpm build` clean (CSS 16.11 KB / gz 5.61 KB, JS 299.74 KB / gz 93.89 KB, 167 modules, 116ms); `pnpm lint` clean; `components.json` references `src/styles/globals.css`; through-proxy curls: `/healthz` → 200 `{\"status\":\"ok\"}`, `/readyz` → 200 `{\"status\":\"ready\"}`; `pnpm dlx shadcn@latest add button` (with workspace yaml moved aside) succeeded — wrote button.tsx + utils.ts, build green, lint green; reverted button.tsx + radix-ui per criterion 3.","created_at":"2026-05-10T23:16:57Z"},{"id":"019e1432-2ddb-7d88-9ccd-a5844fe0173e","issue_id":"convfinqa-0hu","author":"Francesco Albanese","text":"# 2026-05-11 (milestone: milestone:m1-foundation)\n- **Task completed**: Resolve shadcn CLI vs frontend/pnpm-workspace.yaml packages:[.] conflict (convfinqa-0hu.10)\n- **Files changed**: frontend/pnpm-workspace.yaml (dropped 'packages: [.]'), .claude/rules/frontend/package-manager.md (rule updated)\n- **Decisions made**:\n  - Picked Option 1 from the task description: dropped 'packages: [.]' from frontend/pnpm-workspace.yaml. Kept 'minimumReleaseAge: 10080' + 'blockExoticSubdeps: true'.\n  - Updated the local rule to require ONLY those two keys for the current single-package layout, with an explicit forward-looking note: if a real multi-package workspace is ever introduced (apps/web, packages/ui), re-add 'packages:' with the actual subdirs and invoke shadcn with '-c \u003csubdir\u003e'.\n  - Rejected Options 2 (move workspace yaml to repo root with 'packages: [frontend]') and 3 (mv-dance wrapper script). Option 2 has too much blast radius for zero current benefit; Option 3 is fragile and undiscoverable.\n- **Empirical verification (acceptance criterion 2)**:\n  - From clean frontend/ with the new workspace yaml, ran 'pnpm dlx shadcn@latest add card' with NO mv-dance workaround → succeeded, wrote src/components/ui/card.tsx.\n  - 'pnpm build' clean (CSS 16.11 KB gz 5.61 KB / JS 299.74 KB gz 93.89 KB / 120ms); 'pnpm lint' clean.\n  - Reverted the test artifact (removed card.tsx + empty components/ui dir) so 0hu.4 base infrastructure stays untouched per acceptance criterion 3.\n- **Blockers encountered**: shadcn add needed sandbox bypass for npm registry/pnpm store writes. No functional blocker.\n- **Architectural decisions**:\n  - Single-package pnpm workspaces should NOT declare 'packages:'. The key is inert and breaks shadcn CLI. This is now codified in the rule.\n- **Notes for next iteration**:\n  - Any future 'pnpm dlx shadcn@latest add \u003ccomponent\u003e' invocation works directly. The mv-dance from 0hu.4 is OBSOLETE — do not copy it forward.\n  - bd memory updated: 'shadcn-cli-v4-7-monorepo-gate-presence-of' (workaround-only) was forgotten; 'shadcn-cli-v4-7-monorepo-gate-resolved-2026' replaces it with the resolution narrative.\n  - .gitignore keeps '*.bak' — harmless defensive entry, no longer needed for the shadcn dance but cheap to keep.\n  - Remaining 0hu children: .5 (Vitest), .6 (Playwright), .7 (Makefile + CI), .8 (Biome + Lefthook), .11 (/v1/healthz parity — needs user decision).","created_at":"2026-05-10T23:21:33Z"}],"dependency_count":0,"dependent_count":0,"comment_count":6}
{"_type":"issue","id":"convfinqa-aar.5","title":"Update docs and verify end-to-end","description":"Grep `src/convfinqa` across `docs/`, `README.md`, `AGENTS.md`, `CLAUDE.md`, replace with `backend/src/convfinqa` where appropriate. Final sanity: `make sync \u0026\u0026 make test \u0026\u0026 make up \u0026\u0026 curl http://localhost:8000/healthz` from a clean checkout.","acceptance_criteria":"- [ ] No stale path references remain in docs (`rg 'src/convfinqa' docs/ README.md AGENTS.md CLAUDE.md` is empty)\n- [ ] Full `make sync \u0026\u0026 make test \u0026\u0026 make up` cycle passes on a clean checkout\n- [ ] `curl http://localhost:8000/healthz` returns `{\"status\": \"ok\"}`","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:22Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T21:28:37Z","started_at":"2026-05-10T21:17:06Z","closed_at":"2026-05-10T21:28:37Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-aar.5","depends_on_id":"convfinqa-aar","type":"parent-child","created_at":"2026-05-10T21:51:21Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-aar.4","title":"Update alembic.ini, Makefile, and GitHub workflows","description":"`alembic.ini` `script_location` updated. `Makefile` targets (`make test`, `make up`, `make run`, etc.) reference new paths. `.github/workflows/*.yml` path filters and any `working-directory` blocks updated.","acceptance_criteria":"- [ ] `make test` runs successfully against the new layout\n- [ ] `make up` brings up the stack and applies migrations\n- [ ] GitHub workflow YAML lints cleanly via `actionlint` or `gh workflow view`","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:21Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T21:28:37Z","started_at":"2026-05-10T21:17:05Z","closed_at":"2026-05-10T21:28:37Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-aar.4","depends_on_id":"convfinqa-aar","type":"parent-child","created_at":"2026-05-10T21:51:20Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-aar.3","title":"Update Dockerfile and docker-compose.yml","description":"Adjust `COPY` paths so the Docker build context still picks up sources, scripts, and alembic from `backend/`. Keep the entrypoint behavior identical.","acceptance_criteria":"- [ ] `docker build .` succeeds locally without modification of the build context\n- [ ] `docker compose up` boots the API and Postgres, and `/healthz` returns 200\n- [ ] Image size has not regressed materially","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:19Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T21:28:36Z","started_at":"2026-05-10T21:17:05Z","closed_at":"2026-05-10T21:28:36Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-aar.3","depends_on_id":"convfinqa-aar","type":"parent-child","created_at":"2026-05-10T21:51:19Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-aar.2","title":"Update pyproject.toml for new layout","description":"Update `[tool.hatch.build.targets.wheel] packages`, `[tool.pytest.ini_options] pythonpath` and `testpaths`, `[tool.ruff]` `src` and `include`, and any `[tool.alembic]` block. Imports stay rooted at `convfinqa.*` — do NOT add a `src.convfinqa.*` shim.","acceptance_criteria":"- [ ] `uv sync` succeeds and installs the package\n- [ ] `uv run python -c 'import convfinqa; print(convfinqa.__file__)'` resolves to the new path\n- [ ] Ruff and pytest configurations resolve files from the new layout","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:18Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T21:28:35Z","started_at":"2026-05-10T21:17:03Z","closed_at":"2026-05-10T21:28:35Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-aar.2","depends_on_id":"convfinqa-aar","type":"parent-child","created_at":"2026-05-10T21:51:18Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-aar.1","title":"Move backend source tree with git mv","description":"`git mv src/convfinqa backend/src/convfinqa`, `git mv tests backend/tests`, `git mv alembic backend/alembic`, `git mv scripts backend/scripts`. Verify each move with `git status` showing renames (not new+delete).","acceptance_criteria":"- [ ] `backend/src/convfinqa/` exists and `src/convfinqa/` does not\n- [ ] `backend/tests/`, `backend/alembic/`, `backend/scripts/` exist with full history\n- [ ] `git log --follow backend/src/convfinqa/__init__.py` shows pre-move history","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:17Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T21:28:34Z","started_at":"2026-05-10T21:14:34Z","closed_at":"2026-05-10T21:28:34Z","close_reason":"Closed","labels":["afk","milestone:m1-foundation"],"dependencies":[{"issue_id":"convfinqa-aar.1","depends_on_id":"convfinqa-aar","type":"parent-child","created_at":"2026-05-10T21:51:17Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-aar","title":"PRD: Backend reorg — src/ → backend/src/","description":"## Problem Statement\n\nThe repo started as a Python backend and has the Python source at `src/convfinqa/`. It has now evolved into a full-stack project; we need to add a sibling `frontend/` folder. The current layout makes the boundary ambiguous and clutters the project root.\n\n## Solution\n\nMove `src/convfinqa/` → `backend/src/convfinqa/`, move `scripts/` → `backend/scripts/`, move `alembic/` → `backend/alembic/`, move `tests/` → `backend/tests/`. Update every tool that hardcoded the old path. Imports stay rooted at `convfinqa.*` per the project imports rule — only the wheel package path in `pyproject.toml` changes.\n\n## User Stories\n\n1. As a developer, I want backend code under `backend/` so that the frontend can live under `frontend/` without naming collisions.\n2. As a developer, I want `make sync \u0026\u0026 make test \u0026\u0026 make up` to keep working unchanged after the reorg.\n3. As CI, I want the existing GitHub Actions workflow to keep passing after the move.\n4. As a Docker builder, I want `docker build` to find the source at its new path.\n5. As an Alembic user, I want `alembic upgrade head` to find migrations at the new path.\n\n## Implementation Decisions\n\n- Use `git mv` to preserve history on every file.\n- `pyproject.toml`: change `[tool.hatch.build.targets.wheel] packages = [\"src/convfinqa\"]` → `[\"backend/src/convfinqa\"]`. Move `[tool.pytest.ini_options] pythonpath`, `testpaths`, `[tool.ruff]` `src`/`include` accordingly.\n- `Dockerfile`: update `COPY` paths from `src/` and `scripts/` to `backend/src/` and `backend/scripts/`.\n- `alembic.ini`: update `script_location` from `alembic` to `backend/alembic`.\n- `Makefile`: update any references to `src/`, `alembic/`, `scripts/`, `tests/`.\n- `.github/workflows/*.yml`: update path filters and any `cd` / working-directory references.\n- `docker-compose.yml`: update build context if it hardcodes paths.\n- `docs/`: update path references where they appear.\n- `.beads/`, `data/`, `example-html/`, `frontend/` (when created) stay at repo root.\n\n## Testing Decisions\n\n- Existing test suite (`make test`) must pass after the move with zero changes to test code.\n- New integration sanity: a CI step that builds the Docker image and curls `/healthz`.\n- No new unit tests needed — this is a pure reorganization.\n\n## Out of Scope\n\n- Frontend bootstrap (PRD 2).\n- Any new feature work.\n- Restructuring inside `backend/src/convfinqa/` itself.\n\n## Further Notes\n\nThis PRD is ALL AFK. The agent loops over the child tasks; each is mechanical. Rollback = `git revert`.\n","notes":"## Codebase Patterns (added 2026-05-10)\n- After moving src/, ruff isort no longer auto-detects `convfinqa` as first-party. Fix: `[tool.ruff] src = [\"backend/src\", \"backend/tests\"]` in pyproject.toml.\n- Conftest computes `PROJECT_ROOT = Path(__file__).resolve().parents[2]` — relative-up-three from `backend/tests/conftest.py` lands at repo root, where `alembic.ini` lives.\n- `alembic.ini` stays at repo root with `script_location = %(here)s/backend/alembic`. `%(here)s` resolves to wherever alembic.ini sits, so the override is robust to the file moving later.\n- Dockerfile COPY pattern: `COPY backend/src/ ./backend/src/` (preserve repo layout in image) — entrypoint reads `alembic.ini` from `/app/` and resolves `%(here)s/backend/alembic` accordingly.\n- `backend/scripts/ralph/` must be in BOTH `.gitignore` AND `.dockerignore` (local Ralph state, including secrets in `notify.env`).\n- Multi-task reorg PRDs: bundle into one atomic commit. Reference each closed task in the commit body for granular history.","status":"closed","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T20:51:16Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T21:32:11Z","closed_at":"2026-05-10T21:32:11Z","close_reason":"All children completed","labels":["afk","milestone:m1-foundation"],"comments":[{"id":"019e13cd-cb54-7806-b3ad-10add597c8e1","issue_id":"convfinqa-aar","author":"Francesco Albanese","text":"# 2026-05-10 — backend reorg landed (milestone:m1-foundation)\n- **Tasks completed**: convfinqa-aar.1..5 bundled into a single atomic commit (`7f42a21` reorg, `23bad03` quality-gate scrubs)\n- **Files changed**: git mv of `src/convfinqa`, `tests`, `alembic`, `scripts` under `backend/`; updates to `pyproject.toml` (wheel/pytest/ruff src/pyright include), `alembic.ini`, `Dockerfile`, `.dockerignore`, `.gitignore`, `Makefile` fragments, `backend/tests/conftest.py` PROJECT_ROOT, `.claude/rules/python/{imports,hexagonal,logging,dependency-injection}.md`\n- **Decisions**: bundled all 5 tasks into one commit per advisor — intermediate single-task commits would leave the repo unbootable (broken pyproject/Dockerfile/alembic between commits); PRD itself says rollback = `git revert` (singular). Kept `alembic.ini` at repo root and updated `script_location` to `%(here)s/backend/alembic` per PRD literal intent, instead of moving `alembic.ini` into `backend/` — preserves `uv run alembic upgrade head` from repo root.\n- **Architectural decisions**: added `src = [\"backend/src\", \"backend/tests\"]` to `[tool.ruff]` so isort treats `convfinqa` as first-party after the move\n- **Blockers**: none. AGENTS.md was deleted-staged at session start by some non-bd hook — restored from HEAD.\n- **Notes for next iteration**: \n  - Frontend epic `convfinqa-0hu` is now unblocked. Top-level `frontend/` is free.\n  - No `.github/workflows/` exists yet, so aar.4 GH-workflow AC was moot.\n  - Verified: `uv sync`, `uv run pytest` (18 pass), `uv run pyright` (0 err), `docker build` + `docker compose up` + `/healthz=200`, `uv run main` + `/healthz=200`.","created_at":"2026-05-10T21:31:55Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"convfinqa-s3m.3","title":"Slice 3: docker-compose app service + AWS creds flow + docs update","description":"## Parent PRD\n\nconvfinqa-s3m\n\n## What to build\n\nWire the Dockerfile from Slice 2 into `docker-compose.yml` as a new `app` service alongside the existing `postgres` service, add the AWS credentials handoff (`make aws-creds` → `.aws.env` → compose `env_file`), add `make up` / `make down`, and update `CLAUDE.md` + `docs/how-to-run-the-app.md` to reference the new make targets.\n\nEnd-to-end behaviour delivered:\n\n- `make aws-creds` materialises STS credentials from the developer's SSO session into `.aws.env`.\n- `make up` builds and starts the full stack; the `app` container waits for Postgres healthcheck, applies migrations, then serves `/health` 200.\n- `make down` stops and removes the stack.\n- `.aws.env` is gitignored.\n- Documentation reflects the new commands.\n\n## How to build it\n\n### 1. `docker-compose.yml` — add `app` service\n\nAppend to the existing compose file (Postgres service unchanged, image stays pinned at `postgres:18.3-bookworm` per `.claude/rules/python/pyproject.md`):\n\n```yaml\n  app:\n    build:\n      context: .\n      dockerfile: Dockerfile\n    container_name: convfinqa-app\n    depends_on:\n      postgres:\n        condition: service_healthy\n    ports:\n      - \"8000:8000\"\n    env_file:\n      - path: .aws.env\n        required: false\n    environment:\n      API_HOST: 0.0.0.0\n      DATABASE_URL: postgresql+asyncpg://convfinqa:convfinqa@postgres:5432/convfinqa\n      LLM_MODEL: ${LLM_MODEL:-bedrock/eu.anthropic.claude-haiku-4-5-20251001-v1:0}\n      AWS_REGION: ${AWS_REGION:-eu-west-2}\n```\n\nNotes on the contract:\n\n- `env_file.required: false` so `docker compose up` does not error when `.aws.env` does not exist (e.g. first-time clone before `make aws-creds` runs). When the file is missing, AWS calls will fail at runtime with a clear boto3 error — the developer then runs `make aws-creds`.\n- The DB hostname is `postgres` (compose service name), NOT `localhost`. The host-side `make run` workflow continues to use `localhost:5432` via the developer's `.env`.\n- `API_HOST: 0.0.0.0` overrides `Settings.api_host` default of `127.0.0.1` so external mappings work. The host workflow keeps the safer loopback.\n- Do not duplicate values that live in `Settings` defaults unless they MUST differ between host and container (only `API_HOST` and `DATABASE_URL` qualify).\n\n### 2. `.gitignore`\n\nAppend:\n\n```\n.aws.env\n```\n\nConfirm `.env` and `.env.*` are already covered (today `.env.example` is committed and `.env` is gitignored — verify before merge).\n\n### 3. `makefiles/docker.mk`\n\nIf Slice 1 punted this file, create it now:\n\n```makefile\n.PHONY: up down aws-creds\n\nup: ## Build and start the dockerised stack (Postgres + app)\n\tdocker compose up -d --build\n\ndown: ## Stop and remove the dockerised stack\n\tdocker compose down\n\naws-creds: ## Refresh STS credentials into .aws.env (uses AWS_PROFILE or sandbox-admin)\n\taws configure export-credentials --profile $${AWS_PROFILE:-sandbox-admin} --format env-no-export \u003e .aws.env\n```\n\nNotes:\n\n- `$${AWS_PROFILE:-sandbox-admin}` — Make eats single `$`; doubled `$$` reaches the shell as a single `$`. The shell then evaluates `${AWS_PROFILE:-sandbox-admin}`.\n- `aws configure export-credentials --format env-no-export` produces `KEY=value` lines (no `export` prefix) — exactly what `env_file` expects.\n- Add to root `Makefile`: `include makefiles/docker.mk`.\n- Do NOT auto-trigger `aws-creds` from `up` (per the EPIC discussion). Document the order in CLAUDE.md.\n\n### 4. Documentation updates\n\n`CLAUDE.md` — replace the Build \u0026 Test section block with:\n\n```markdown\n## Build \u0026 Test\n\n```bash\nmake sync                              # Install deps via uv\ndocker compose up -d postgres          # OR: make up (full stack)\nuv run alembic upgrade head            # Only when running the host workflow\nmake aws-creds                         # Refresh STS creds (only when using `make up`)\nmake run                               # Run the API on the host\nmake test                              # Run unit + integration tests\n```\n\nFull walkthrough (AWS login, env file, cURL verification, CLI REPL) lives in\n[docs/how-to-run-the-app.md](./docs/how-to-run-the-app.md).\n```\n\n`docs/how-to-run-the-app.md` — for each section, lead with the make target, then show the underlying command in a \"what this runs\" callout. Sections to update:\n\n- AWS login → `make aws-creds` (with the `aws-login` and `switch-aws-env` prerequisites unchanged).\n- Local Postgres → `make up` for full stack OR `docker compose up -d postgres` for host workflow.\n- Migrations → `uv run alembic upgrade head` (host only — `make up` runs migrations inside the container).\n- API server → `make run` (host) or `make up` (container).\n- cURL verification → unchanged commands, but reference the make targets.\n- CLI REPL → `make cli ARGS=\"chat\"`.\n\nConstraints (all rules under `.claude/rules/`):\n\n- No comments explaining WHAT in code or scripts.\n- No `[project.scripts]` additions.\n- No reading `SETTINGS` directly inside route handlers / use cases (this slice does not touch route code).\n- Imports rooted at `convfinqa.*`.\n- Variable naming: no leading underscores in module-level constants.\n- No new pytest tests for compose / make targets — the existing manual smoke is sufficient per the PRD's testing decisions.\n\n### 5. Manual verification (single end-to-end smoke)\n\nBefore marking the slice done:\n\n1. Fresh checkout, no `.aws.env`.\n2. `aws-login \u0026\u0026 switch-aws-env sandbox-admin \u0026\u0026 AWS_PROFILE=sandbox-admin aws sts get-caller-identity` — confirm SSO session live.\n3. `make aws-creds` — file created, contains `AWS_ACCESS_KEY_ID=...`, `AWS_SECRET_ACCESS_KEY=...`, `AWS_SESSION_TOKEN=...`.\n4. `make up` — Postgres comes up healthy; app container builds, applies migrations, listens on 8000.\n5. `curl -fsS http://localhost:8000/health` → 200 with the expected JSON.\n6. `docker logs convfinqa-app | head -50` — verify alembic ran clean and uvicorn is bound to 0.0.0.0.\n7. `make down` — both containers stop and are removed.\n8. Repeat from step 4 to confirm idempotence (alembic upgrade head is a no-op on the already-migrated DB).\n\nIf any step fails, fix and re-verify. Do NOT merge with a known-broken compose flow.\n\n## Acceptance criteria\n\n- [ ] `docker-compose.yml` defines an `app` service with `build` pointing at the repo Dockerfile, `depends_on.postgres.condition: service_healthy`, the documented `environment` block, and `env_file: [{path: .aws.env, required: false}]`.\n- [ ] Postgres service unchanged (still `postgres:18.3-bookworm`, same volumes, same healthcheck).\n- [ ] `.aws.env` listed in `.gitignore`.\n- [ ] `makefiles/docker.mk` exists with `up`, `down`, `aws-creds` targets, each with `## ` docstrings discoverable via `make help`.\n- [ ] Root `Makefile` includes `makefiles/docker.mk`.\n- [ ] `make aws-creds` writes a non-empty `.aws.env` containing `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_SESSION_TOKEN` keys (verified manually with an active SSO session).\n- [ ] `make up` brings the stack up; `curl http://localhost:8000/health` returns 200 within ~30s.\n- [ ] `make down` stops and removes both containers.\n- [ ] App container logs show alembic upgrade head running before uvicorn (verified via `docker logs convfinqa-app`).\n- [ ] App container runs as UID 1000 (verified via `docker exec convfinqa-app id`).\n- [ ] `CLAUDE.md` Build \u0026 Test section references `make sync`, `make up`, `make run`, `make test`, `make aws-creds`.\n- [ ] `docs/how-to-run-the-app.md` updated to lead each step with the make target.\n- [ ] `.aws.env` is NOT committed (verified with `git status` after running `make aws-creds`).\n- [ ] No automated pytest tests of compose / Dockerfile added (per PRD testing decisions).\n- [ ] Re-running `make up` against a stack that is already migrated does not error (idempotence verified).\n\n## Blocked by\n\nSlice 2 (Dockerfile must exist before compose can `build:` against it).\n\n## User stories addressed\n\nUser stories 13, 14, 15, 16, 19, 21 from `convfinqa-s3m`.","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T13:11:12Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T13:48:50Z","started_at":"2026-05-10T13:44:53Z","closed_at":"2026-05-10T13:48:50Z","close_reason":"Closed","labels":["afk","milestone:dx-bundle"],"dependencies":[{"issue_id":"convfinqa-s3m.3","depends_on_id":"convfinqa-s3m","type":"parent-child","created_at":"2026-05-10T14:11:11Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-s3m.3","depends_on_id":"convfinqa-s3m.2","type":"blocks","created_at":"2026-05-10T14:11:16Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-s3m.2","title":"Slice 2: Production Dockerfile + entrypoint + .dockerignore","description":"## Parent PRD\n\nconvfinqa-s3m\n\n## What to build\n\nA multi-stage `Dockerfile` at the repo root that produces a runtime-agnostic production image, plus a `.dockerignore` and a small entrypoint script. The image runs as non-root, applies alembic migrations on start, then `exec`s uvicorn so signals reach the server directly.\n\nEnd-to-end behaviour delivered:\n\n- `docker build -t convfinqa:local .` produces an image (≤ ~300MB target, single arch matching host).\n- `docker run --rm -e DATABASE_URL=... -e API_HOST=0.0.0.0 -p 8000:8000 convfinqa:local` boots and applies migrations, then starts uvicorn on port 8000.\n- The image runs as a non-root user (UID 1000, name `convfinqa`).\n- The build context is small (no `.venv`, `.git`, caches, tests, `data/`, `.env*`, `.aws.env`).\n\n## How to build it\n\n### 1. `Dockerfile` (multi-stage)\n\n**Stage 1 — builder**:\n\n```dockerfile\nFROM ghcr.io/astral-sh/uv:python3.13-bookworm-slim AS builder\n\nENV UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy UV_PYTHON_DOWNLOADS=never\n\nWORKDIR /app\n\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-dev --no-install-project\n\nCOPY src/ ./src/\nCOPY alembic/ ./alembic/\nCOPY alembic.ini ./alembic.ini\nRUN uv sync --frozen --no-dev\n```\n\nThe two-step `uv sync` is intentional: the first call caches the dependency layer (changes to `src/` do not invalidate it). The second call installs the project itself.\n\n**Stage 2 — runtime**:\n\n```dockerfile\nFROM python:3.13-slim-bookworm\n\nRUN useradd --system --create-home --uid 1000 convfinqa\n\nWORKDIR /app\n\nCOPY --from=builder --chown=convfinqa:convfinqa /app /app\nCOPY --chown=convfinqa:convfinqa scripts/docker-entrypoint.sh /app/scripts/docker-entrypoint.sh\nRUN chmod +x /app/scripts/docker-entrypoint.sh\n\nENV PATH=\"/app/.venv/bin:$PATH\" \\\n    PYTHONDONTWRITEBYTECODE=1 \\\n    PYTHONUNBUFFERED=1\n\nUSER convfinqa\nEXPOSE 8000\n\nENTRYPOINT [\"/app/scripts/docker-entrypoint.sh\"]\n```\n\nConstraints:\n\n- Pin to `python:3.13-slim-bookworm` to align with `.python-version` (3.13.7) and `.claude/rules/python/pyproject.md` (bookworm preferred).\n- Do NOT use `latest`, `alpine`, `trixie`, or bare `3.13`. Same reasoning as the Postgres pin in the project rules.\n- Do NOT add a `HEALTHCHECK` directive in the Dockerfile — the compose file owns healthchecks (Slice 3). Keeps the image runtime-agnostic.\n- Do NOT bake any AWS credentials, env files, or settings overrides into the image.\n- Do NOT register a new `[project.scripts]` entry per `.claude/rules/python/pyproject.md`. The image uses the existing `convfinqa` and `main` console scripts only via `uv run` if needed; the entrypoint calls `uvicorn` directly via the venv's PATH.\n\n### 2. `scripts/docker-entrypoint.sh`\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\nalembic upgrade head\n\nexec uvicorn convfinqa.main:create_app --factory --host 0.0.0.0 --port 8000\n```\n\nWhy `exec`: makes uvicorn PID 1 inside the container. Without `exec`, the bash process traps SIGTERM and uvicorn never gets the graceful-shutdown signal until the 10-second kill timer fires.\n\nWhy `set -euo pipefail`: strict failure semantics. If `alembic upgrade head` fails, the container exits non-zero immediately and orchestrators can restart or alert.\n\nWhy hard-code `--host 0.0.0.0 --port 8000` here rather than relying on `Settings`: the container's network contract is fixed (it always listens on 0.0.0.0:8000 and the orchestrator maps externally). Letting `Settings.api_host` default to `127.0.0.1` keeps the host workflow safer. This is the ONE place where the container deviates from `Settings`-driven config; document inline why.\n\nPlace the script at `scripts/docker-entrypoint.sh` alongside the existing `scripts/install-prek-hook.sh` and `scripts/ralph`. Make it executable (`git update-index --chmod=+x` if needed).\n\n### 3. `.dockerignore`\n\n```\n.venv/\n.pytest_cache/\n.ruff_cache/\n.mypy_cache/\n__pycache__/\n*.pyc\ntests/\n.git/\n.github/\n.beads/\ndata/\ndocs/\n.env\n.env.*\n.aws.env\nMakefile\nmakefiles/\nREADME.md\nLICENSE\nAGENTS.md\nCLAUDE.md\n.python-version\n.pre-commit-config.yaml\n```\n\nGoal: keep build context under a few MB, and ensure no secrets (`.env`, `.aws.env`) or developer caches end up in image layers.\n\n### 4. Local verification (manual)\n\nThis slice does NOT add automated tests of the Dockerfile per the PRD's testing decisions. Manual smoke before merge:\n\n- `docker build -t convfinqa:local .` succeeds.\n- `docker images convfinqa:local --format '{{.Size}}'` returns a reasonable size (target ≤ 350MB; if much larger, audit the build context).\n- `docker run --rm convfinqa:local --help` is NOT meaningful (entrypoint is the script). Instead: `docker run --rm --entrypoint /bin/bash convfinqa:local -c 'id'` prints `uid=1000(convfinqa) ...`.\n- `docker history convfinqa:local` shows two stages and no copies of `.venv`, `.git`, `tests/`, or env files (use `docker history --no-trunc`).\n\nFull end-to-end booting against Postgres lives in Slice 3.\n\n## Acceptance criteria\n\n- [ ] `Dockerfile` exists at repo root with two stages (`builder`, runtime).\n- [ ] Builder uses `ghcr.io/astral-sh/uv:python3.13-bookworm-slim`.\n- [ ] Runtime uses `python:3.13-slim-bookworm`.\n- [ ] Final image runs as user `convfinqa` (UID 1000), not root.\n- [ ] `docker build -t convfinqa:local .` succeeds on a clean checkout.\n- [ ] `docker run --rm --entrypoint /bin/bash convfinqa:local -c 'id'` prints `uid=1000`.\n- [ ] `scripts/docker-entrypoint.sh` is executable, uses `set -euo pipefail`, runs `alembic upgrade head` then `exec uvicorn ...`.\n- [ ] `.dockerignore` excludes `.venv`, `.git`, `tests/`, `.env*`, `.aws.env`, caches, docs, Makefile.\n- [ ] No new `[project.scripts]` entries are added.\n- [ ] No AWS credentials or `.env` content are present in any image layer (verify with `docker history --no-trunc`).\n- [ ] `pyproject.toml`, `Settings`, `convfinqa.config`, and any application code are NOT modified by this slice.\n- [ ] No comments in the Dockerfile or shell script explaining WHAT; brief WHY notes where non-obvious (e.g. why two `uv sync` calls, why `exec`).\n\n## Blocked by\n\nNone — can start immediately, in parallel with Slice 1.\n\n## User stories addressed\n\nUser stories 9, 10, 11, 12, 17, 18, 24 from `convfinqa-s3m`.","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T13:11:06Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T13:42:48Z","started_at":"2026-05-10T13:37:29Z","closed_at":"2026-05-10T13:42:48Z","close_reason":"Closed","labels":["afk","milestone:dx-bundle"],"dependencies":[{"issue_id":"convfinqa-s3m.2","depends_on_id":"convfinqa-s3m","type":"parent-child","created_at":"2026-05-10T14:11:05Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"convfinqa-s3m.1","title":"Slice 1: Makefile run/test targets + pytest marker scheme","description":"## Parent PRD\n\nconvfinqa-s3m\n\n## What to build\n\nAdd a Makefile-driven developer interface for tests and runtime, plus a path-based pytest marker scheme that splits the suite cleanly into `unit` and `integration` with strict enforcement.\n\nThe end-to-end behaviour delivered by this slice:\n\n- `make test-unit` runs only the fast in-process tests (zero Postgres testcontainer boots).\n- `make test-integration` runs only the testcontainer-backed tests.\n- `make test` runs unit then integration sequentially (fast failure on the cheap suite).\n- `make run` starts the FastAPI server via the existing `serve_app` console script.\n- `make cli ARGS=\"\u003csubcommand\u003e\"` invokes the Typer CLI.\n- `make help` lists every target with a one-line description.\n- A new test landing in any `tests/\u003cdir\u003e/` that has no path mapping fails collection loudly.\n- A misclassified marker (e.g. `pytest.mark.unti`) fails because of `--strict-markers`.\n\n## How to build it\n\n### 1. Register markers in `pyproject.toml`\n\nUnder `[tool.pytest.ini_options]`, add:\n\n- `markers = [\"unit: fast in-process tests\", \"integration: tests that boot a Postgres testcontainer or external service\"]`\n- Append `--strict-markers` to `addopts` (or create the key if absent).\n- DO NOT touch `asyncio_mode`, `asyncio_default_fixture_loop_scope`, `testpaths`, `pythonpath` — those are protected by `.claude/rules/python/tests.md`.\n\n### 2. Auto-tag conftest\n\nModify (or create, if absent) `tests/conftest.py`. Add `pytest_collection_modifyitems(config, items)`:\n\n- Build a path-prefix map (relative to `tests/`):\n  - `api/` → `integration`\n  - `entrypoints/api/` → `integration`\n  - `adapters/` → `integration` (rationale: hexagonal adapters typically need testcontainers)\n  - `cli/` → `unit`\n  - `entrypoints/cli/` → `unit`\n  - `domain/` → `unit`\n  - `application/` → `unit`\n  - `fakes/` → `unit`\n- For each item, derive the relative path from `item.path` (or `item.fspath`) and the project tests directory, find the longest matching prefix, and apply the corresponding marker if neither `unit` nor `integration` is already present.\n- After tagging, walk the items once more; collect any nodeids that still have neither marker. If non-empty, raise `pytest.UsageError(f\"Unmarked tests: {nodeids}. Add a path mapping in tests/conftest.py.\")`.\n\nConstraints:\n\n- No comments explaining WHAT (per `.claude/rules/python/comments.md`). A short WHY comment on the strict assertion is acceptable.\n- Use `from convfinqa.logging import get_logger` if logging is needed (probably not).\n- No imports from `src.convfinqa.*` — root all imports at `convfinqa.*` per `.claude/rules/python/imports.md`.\n- No leading underscores on module-level constants per `.claude/rules/python/variables.md`.\n\n### 3. Conftest behaviour test\n\nAdd ONE test (`tests/test_conftest_markers.py` or similar) using pytest's built-in `pytester` fixture. The test creates a synthetic mini-suite with one test under an unmapped path and asserts collection fails with a `UsageError`. Justification: the auto-tag is non-trivial logic that, if broken, silently mis-classifies the entire suite. Per `.claude/rules/python/tests.md`, this is the only meaningful test of the marker mechanism — DO NOT add tests that assert \"this file has the integration marker\".\n\nThe test itself is `unit` (no testcontainer). Place it under `tests/` root and ensure the conftest path mapping covers `tests/test_*.py` at root → `unit` (or apply an explicit `pytestmark = pytest.mark.unit`).\n\n### 4. Audit existing files\n\nConfirm path-based classification matches the actual suite at the time of merge:\n\n- `tests/api/test_chat*.py` → `integration` (testcontainer Postgres) — confirmed at PRD time.\n- `tests/cli/test_smoke.py` → `unit` (CliRunner only) — confirmed.\n- Any new files added between PRD writing and slice merging must fit the mapping or get a new path mapping.\n\n### 5. Makefile fragments\n\nCreate `makefiles/test.mk`, `makefiles/run.mk`, `makefiles/docker.mk`. Include them all from the root `Makefile` via `include makefiles/*.mk` (or explicit `include` lines).\n\n`makefiles/test.mk`:\n\n```makefile\n.PHONY: test-unit test-integration test\n\ntest-unit: ## Run unit tests\n\tuv run pytest -m unit -q tests/\n\ntest-integration: ## Run integration tests (testcontainer Postgres)\n\tuv run pytest -m integration -q tests/\n\ntest: test-unit test-integration ## Run unit then integration tests\n```\n\n`makefiles/run.mk`:\n\n```makefile\n.PHONY: run cli\n\nrun: ## Run the FastAPI server (uv run main → serve_app)\n\tuv run main\n\ncli: ## Run the Typer CLI; pass args via ARGS=\"...\"\n\tuv run convfinqa $(ARGS)\n```\n\n`makefiles/docker.mk`: created in this slice with empty placeholder OR deferred to Slice 3. Recommendation: **defer to Slice 3** so this slice does not introduce unused Make targets.\n\nUpdate root `Makefile` to `include makefiles/test.mk` and `include makefiles/run.mk` (alongside the existing `include makefiles/env.mk`). Confirm `make help` still lists every target — the existing `help` rule greps `## ` docstrings.\n\n### 6. Documentation\n\nOut of scope for this slice — Slice 3 owns the docs sweep. This slice only ships the targets.\n\n## Acceptance criteria\n\n- [ ] `make test-unit` passes locally, runs zero testcontainer boots (verifiable: no Docker socket activity, runtime ≪ 1s for current `tests/cli/test_smoke.py`).\n- [ ] `make test-integration` runs all `tests/api/test_chat*.py` and only those.\n- [ ] `make test` runs unit first, integration second; failure in unit aborts before integration starts.\n- [ ] `make run` starts uvicorn on the configured port; equivalent to `uv run main` today.\n- [ ] `make cli ARGS=\"--help\"` prints the Typer help.\n- [ ] `make cli` (no args) prints the Typer top-level help (Typer's default behaviour).\n- [ ] `make help` lists `test-unit`, `test-integration`, `test`, `run`, `cli` alongside the existing `sync` and `install-hooks`.\n- [ ] `pyproject.toml` declares `unit` and `integration` markers and `--strict-markers` is active.\n- [ ] `tests/conftest.py` auto-tags every test in current `tests/` based on the path mapping.\n- [ ] Adding a fresh empty file at `tests/_unmapped/test_x.py` causes `pytest` collection to fail with a `UsageError` listing that nodeid (verified by the new conftest test using `pytester`).\n- [ ] No tests of the form \"assert this file has marker X\" exist (per `.claude/rules/python/tests.md`).\n- [ ] `uv run pytest -q tests/` (no marker filter) still runs the full suite.\n- [ ] No new comments explaining WHAT; only WHY where genuinely needed.\n\n## Blocked by\n\nNone — can start immediately.\n\n## User stories addressed\n\nUser stories 1, 2, 3, 4, 5, 6, 7, 8, 20, 22, 23, 25 from `convfinqa-s3m`.","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T13:10:54Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T13:35:47Z","started_at":"2026-05-10T13:27:05Z","closed_at":"2026-05-10T13:35:47Z","close_reason":"Closed","labels":["afk","milestone:dx-bundle"],"dependencies":[{"issue_id":"convfinqa-s3m.1","depends_on_id":"convfinqa-s3m","type":"parent-child","created_at":"2026-05-10T14:10:53Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-s3m","title":"EPIC: DX bundle — Makefile, Dockerfile, compose app service","description":"## Problem Statement\n\nAs a developer working on convfinqa, I have to type long, error-prone commands every time I run tests, start the API, or open the CLI. I copy-paste from `CLAUDE.md` or muscle-memory, and the canonical command for \"run the unit tests\" doesn't exist — `uv run pytest -q tests/` runs everything, including testcontainer-backed tests that need Docker. There is no `Dockerfile`, so I can't hand a colleague a single `docker compose up` command, the production deployment story is undefined, and the only docker-compose service today is Postgres. The Makefile exists but only carries `sync` and `install-hooks`.\n\nThe friction is small per command but compounds: it slows new contributors, gives me no fast feedback loop for unit tests, and means CI/prod infra cannot be designed yet because nothing produces a runnable container image.\n\n## Solution\n\nShip a small, opinionated developer-experience (DX) bundle:\n\n1. A `Makefile` with five everyday targets — `test-unit`, `test-integration`, `test`, `run`, `cli` — plus three compose helpers (`up`, `down`, `aws-creds`). Each is a thin wrapper over the canonical command.\n2. A pytest-marker scheme that splits the suite cleanly into `unit` and `integration`, with auto-tagging by path inside `tests/conftest.py` and a strict collection-time assertion that no test is unmarked.\n3. A multi-stage `Dockerfile` that produces a runtime-agnostic production image (uv builder → slim Python runtime, non-root user, alembic migrations on entrypoint).\n4. A docker-compose `app` service wired in alongside the existing `postgres` service so `docker compose up --build -d` boots the full stack locally. Local AWS credentials flow via a generated `.aws.env` file (using `aws configure export-credentials`), refreshed by `make aws-creds`. The Dockerfile pulls credentials from the boto3 chain so future production runtimes (ECS Task Role, EKS Pod Identity, IAM Roles Anywhere) work without image changes.\n5. Update `CLAUDE.md` and `docs/how-to-run-the-app.md` to use the new make targets as the canonical commands.\n\n## User Stories\n\n1. As a developer, I want a single `make test-unit` command that runs only the fast, in-process tests, so that I get sub-second feedback while iterating on domain logic.\n2. As a developer, I want a single `make test-integration` command that runs only the testcontainer-backed tests, so that I can verify the database boundary without paying for the unit tests every time.\n3. As a developer, I want `make test` to run unit tests first, then integration tests, so that the slow suite never starts if a fast test is already broken.\n4. As a developer, I want `make run` to start the FastAPI server with one keystroke, so that I do not need to remember the uvicorn factory invocation.\n5. As a developer, I want `make cli` to launch the Typer CLI with one keystroke, so that I can drop into the chat REPL or other commands instantly.\n6. As a developer, I want every test to belong to either the unit or integration suite, so that the suite split cannot rot through neglect.\n7. As a developer, I want unmarked tests to fail collection loudly, so that mistakes are caught at PR time rather than going silent.\n8. As a developer, I want test markers applied automatically based on path, so that contributors do not have to remember to add a decorator.\n9. As a developer, I want a `Dockerfile` at the repo root that builds a production-grade image, so that I can hand it to any container runtime later without rewriting it.\n10. As a developer, I want the production image to run as a non-root user, so that the deployment posture meets baseline container security expectations.\n11. As a developer, I want the image entrypoint to apply alembic migrations before starting uvicorn, so that `docker compose up` is a single self-contained boot.\n12. As a developer, I want a `.dockerignore` that excludes `.venv`, caches, tests, git history, and credential files, so that the build context is small and no secrets are baked into image layers.\n13. As a developer, I want a docker-compose `app` service that depends on the Postgres healthcheck, so that the API never starts before the database is ready.\n14. As a developer, I want `make up` to build and start the whole stack and `make down` to stop it, so that the dockerised flow is symmetrical with the host flow.\n15. As a developer using AWS SSO locally, I want `make aws-creds` to materialise short-lived STS credentials into a `.aws.env` file, so that the dockerised app can call Bedrock without me hand-mounting `~/.aws`.\n16. As a developer, I want `.aws.env` listed in `.gitignore`, so that I cannot accidentally commit short-lived credentials.\n17. As a security-conscious operator, I want zero long-lived credentials baked into the image or env files, so that a leaked image or env file cannot grant durable AWS access.\n18. As a future production operator, I want the Dockerfile to read credentials from the boto3 chain, so that ECS Task Roles / EKS Pod Identity / IAM Roles Anywhere work without rebuilding the image.\n19. As a developer, I want `CLAUDE.md` and `docs/how-to-run-the-app.md` to reference the new make targets, so that documentation does not drift away from reality.\n20. As a contributor, I want `make help` to list every target with a one-line description, so that the build interface is self-documenting.\n21. As a developer running the host workflow (`make run` against compose's Postgres), I want my fast iteration loop preserved, so that adopting Docker for prod-shape testing does not slow my day-to-day.\n22. As a developer, I want path-based marker tagging to live in a single conftest, so that adding a new test directory only requires editing one file.\n23. As a developer, I want the marker registry in `pyproject.toml` to use `--strict-markers`, so that typos like `pytest.mark.unti` fail loudly.\n24. As a developer, I want migrations to be applied inside the container via the entrypoint, so that the host Python and the container Python never disagree about schema state.\n25. As a developer, I want `make run` to keep using the existing `serve_app` console script, so that there is exactly one way to start the API and config (host, port, reload) stays controlled by `Settings`.\n\n## Implementation Decisions\n\n### Test markers (HOW)\n\n- **Register markers** in `pyproject.toml` under `[tool.pytest.ini_options]`:\n  - Add `markers = [\"unit: fast in-process tests\", \"integration: tests that boot a Postgres testcontainer or external service\"]`.\n  - Add `--strict-markers` to `addopts` so unknown markers fail.\n  - Keep existing `asyncio_mode = \"auto\"` and `asyncio_default_fixture_loop_scope = \"session\"` per `.claude/rules/python/tests.md`.\n- **Auto-tag by path** in `tests/conftest.py` using `pytest_collection_modifyitems(config, items)`:\n  - Treat path prefixes `tests/api/`, `tests/entrypoints/api/`, `tests/adapters/` (when populated with testcontainer-backed adapters) as `integration`.\n  - Treat `tests/cli/`, `tests/entrypoints/cli/`, `tests/domain/`, `tests/application/`, `tests/fakes/` as `unit`.\n  - For each `Item`, if neither marker is already present, apply the path-derived one via `item.add_marker(pytest.mark.\u003cname\u003e)`.\n- **Strict collection assertion**: after auto-tag, walk `items` once more; if any item has neither `unit` nor `integration` markers, raise `pytest.UsageError` listing the offending nodeids. This means a brand-new test directory with no path mapping fails fast at collection time.\n- **No per-module `pytestmark`**: the auto-tag is the source of truth. If a single test must opt out, it can carry an explicit override marker — but this is the exception, not the rule.\n- **Audit existing files** before merging: confirm every file in `tests/api/`, `tests/cli/`, etc. classifies correctly. The known files today are `tests/api/test_chat*.py` (integration — testcontainer Postgres) and `tests/cli/test_smoke.py` (unit — Typer `CliRunner` only).\n- **No meaningless tests** per `.claude/rules/python/tests.md`: do NOT add a test that asserts \"the marker was applied\". The behaviour is verified implicitly by `make test-unit` / `make test-integration` running the right subset.\n\n### Makefile layout (HOW)\n\n- Keep the existing `Makefile` shape: `.DEFAULT_GOAL := help`, `SHELL := /bin/bash`, `include makefiles/*.mk`, `make help` greps for `## ` docstrings.\n- Add new fragment `makefiles/test.mk` with: `test-unit`, `test-integration`, `test`. Each is one line wrapping `uv run pytest -m \u003cmarker\u003e -q tests/` (or both for `test`).\n- Add new fragment `makefiles/run.mk` with: `run`, `cli`. `run` shells out to `uv run main`. `cli` shells out to `uv run convfinqa $(ARGS)` so `make cli ARGS=\"chat\"` works.\n- Add new fragment `makefiles/docker.mk` with: `up`, `down`, `aws-creds`.\n  - `up`: `docker compose up -d --build`.\n  - `down`: `docker compose down`.\n  - `aws-creds`: `aws configure export-credentials --profile $${AWS_PROFILE:-sandbox-admin} --format env-no-export \u003e .aws.env`. Note the doubled `$$` because Make eats single `$`.\n- Every target gets a `## ` description so `make help` lists it.\n- Targets are `.PHONY` since none produce files.\n\n### Dockerfile (HOW)\n\n- **Two stages**:\n  - Stage 1 (`builder`): `FROM ghcr.io/astral-sh/uv:python3.13-bookworm-slim AS builder`. Set `WORKDIR /app`. Copy `pyproject.toml` + `uv.lock` first, then `RUN uv sync --frozen --no-dev --no-install-project` to cache the dep layer. Copy `src/`, `alembic/`, `alembic.ini`, then `RUN uv sync --frozen --no-dev` to install the project.\n  - Stage 2 (`runtime`): `FROM python:3.13-slim-bookworm`. Create a non-root user (`useradd --system --create-home --uid 1000 convfinqa`). Copy `/app/.venv` and project files from `builder` with `--chown=convfinqa:convfinqa`. Set `PATH=/app/.venv/bin:$PATH`. Switch to the non-root user via `USER convfinqa`. `EXPOSE 8000`.\n- **Entrypoint**: ship a small shell script `scripts/docker-entrypoint.sh` that runs `set -euo pipefail; alembic upgrade head; exec uvicorn convfinqa.main:create_app --factory --host 0.0.0.0 --port 8000`. `ENTRYPOINT [\"scripts/docker-entrypoint.sh\"]`. The `exec` is critical so uvicorn becomes PID 1 and receives signals correctly.\n- **API host override**: the container sets `API_HOST=0.0.0.0` either via the entrypoint script or compose env. The host loopback (`127.0.0.1`) default in `Settings` stays untouched so `make run` keeps the safer host binding.\n- **No SETTINGS leak**: the entrypoint and Dockerfile MUST NOT hard-code values that exist in `Settings`. Per `.claude/rules/python/dependency-injection.md`, anything that varies by environment is a `Settings` field, overridden via env var.\n- **No alembic on host inside the image**: the image installs alembic via the dev/runtime split? `alembic` is in main `dependencies` already, so it ships in the prod venv. Confirm before merge.\n- **`.dockerignore`** at repo root excludes: `.venv/`, `.pytest_cache/`, `.ruff_cache/`, `.mypy_cache/`, `tests/`, `.git/`, `.github/`, `data/`, `.env*`, `.aws.env`, `*.pyc`, `__pycache__/`, `docs/`, `Makefile`, `makefiles/`, `README.md`. Keeps build context small and prevents host caches/secrets from being baked into image layers.\n\n### Docker-compose `app` service (HOW)\n\n- Add a second service `app` to `docker-compose.yml`:\n  - `build: { context: ., dockerfile: Dockerfile }`.\n  - `container_name: convfinqa-app`.\n  - `depends_on: { postgres: { condition: service_healthy } }`.\n  - `ports: [\"8000:8000\"]`.\n  - `env_file: [.aws.env]` — file is generated by `make aws-creds`.\n  - `environment:`\n    - `API_HOST: 0.0.0.0`\n    - `DATABASE_URL: postgresql+asyncpg://convfinqa:convfinqa@postgres:5432/convfinqa` (note hostname is `postgres`, the compose service name)\n    - `LLM_MODEL: ${LLM_MODEL:-bedrock/eu.anthropic.claude-haiku-4-5-20251001-v1:0}`\n    - `AWS_REGION: ${AWS_REGION:-eu-west-2}`\n- Postgres service unchanged: image stays pinned at `postgres:18.3-bookworm` per `.claude/rules/python/pyproject.md`.\n- `.aws.env` added to `.gitignore`. `.env` already ignored.\n\n### AWS credentials flow (HOW)\n\n- **Local dev**: developer runs `make aws-creds` once per SSO session. Output is `.aws.env` in repo root (gitignored). Compose loads it via `env_file`. Credentials expire with the SSO session (~1 hour) — refresh by re-running `make aws-creds`.\n- **Why not mount `~/.aws`**: the research summary in the EPIC discussion confirms SSO-cached tokens reference host paths the container cannot resolve, so the boto3 SSO provider fails inside the container.\n- **Why not bake creds into images**: violates the security goal in user story 17. `.dockerignore` blocks `.aws.env` from the build context; docker-compose injects it at runtime only.\n- **Production**: not implemented in this EPIC. The Dockerfile is runtime-agnostic — boto3 picks credentials from env, container metadata service, or the EC2/ECS/EKS provider chain without any image change. A future EPIC will bind to a specific runtime (likely ECS Fargate + Task Role per the research).\n\n### Documentation (HOW)\n\n- Update `CLAUDE.md` \"Build \u0026 Test\" section: replace the four raw commands with `make sync`, `make up` (or `make run`), `make test`. Keep the link to `docs/how-to-run-the-app.md`.\n- Update `docs/how-to-run-the-app.md` parallel sections for AWS login, env file, server start, cURL verification, CLI REPL — each step shows the `make` target first and the underlying command second (in a \"what this runs\" note) so the cheatsheet stays usable when Make is unavailable.\n- Mention `make aws-creds` prominently as the prerequisite to `make up`.\n- Do NOT add WHAT comments to the code or scripts per `.claude/rules/python/comments.md`. The Makefile `## ` strings are the exception (they ARE the help system, and they describe what — but `make help` is the user-facing surface, not code).\n\n### Cross-cutting rules to honour (HOW)\n\n- `convfinqa.*` import roots only — no `src.convfinqa.*`. Affects nothing in this EPIC since we do not write Python code outside `tests/conftest.py` (path-based marker logic uses `item.fspath` / `item.path`).\n- No reading `SETTINGS` directly inside route handlers / use cases. The Dockerfile entrypoint sets env vars; `Settings()` reads them. Routes get `Settings` via the existing container — unchanged here.\n- No new `[project.scripts]` entries. The two existing ones (`main`, `convfinqa`) are sufficient.\n- Variable naming: no leading underscores in module-level constants. Affects shell scripts implicitly (use `AWS_PROFILE`, not `_AWS_PROFILE`).\n- Logging: any new Python (only `tests/conftest.py` here) uses `from convfinqa.logging import get_logger` if it logs at all. `tests/conftest.py` does not need to log; `pytest.UsageError` carries the failure message.\n\n## Testing Decisions\n\n### What makes a good test for this EPIC\n\nA good test verifies an externally-observable behaviour a user actually exercises. For this DX bundle, the user is the developer running `make ...`, and the relevant behaviours are:\n\n- \"When I run `make test-unit`, the testcontainer-backed tests do not run.\"\n- \"When I run `make test-integration`, all testcontainer tests run.\"\n- \"When a new test file lands in `tests/api/` without a marker, collection still classifies it as integration.\"\n- \"When a new test file lands in a directory with no path mapping, collection fails loudly.\"\n- \"When I `docker compose up --build -d`, the API answers `GET /health` with 200.\"\n\n### What we will and will NOT test\n\n- We will add **one** behaviour test for the conftest auto-tag: a `tests/conftest_test.py` (or `tests/test_conftest_markers.py`) that uses `pytest`'s `pytester` plugin to run a synthetic mini-suite with a deliberately unmarked test in an unmapped path and asserts collection fails. This is the only meaningful test of the marker mechanism and is justified because the auto-tag is non-trivial logic that, if broken, silently mis-classifies the entire suite.\n- We will NOT add tests that assert \"this file has the integration marker\" — that is implementation detail per `.claude/rules/python/tests.md`.\n- We will NOT add a pytest test that boots `docker compose up`. That would duplicate the existing testcontainer coverage and depend on Docker availability twice. The Dockerfile/compose are verified manually once during this EPIC (per the EPIC discussion) — `make aws-creds \u0026\u0026 make up \u0026\u0026 curl :8000/health` then `docker compose down`.\n- We will NOT add a test that asserts `make run` invokes `serve_app`. Trivial wrapper, no logic.\n- We will NOT add tests that assert imports work. Per `.claude/rules/python/tests.md`: \"tests that check if we are able to import modules correctly … MUST be avoided at all costs.\"\n\n### Prior art\n\n- The auto-marker conftest test should follow the style of existing `tests/api/test_chat*.py` — minimal fixtures, behaviour-driven assertions, no monkey-patching. Use `pytest`'s built-in `pytester` fixture (no extra dependency).\n- The async test infrastructure rules in `.claude/rules/python/tests.md` continue to apply: `asyncio_default_fixture_loop_scope = \"session\"`, `loop_scope=\"session\"` on `@pytest_asyncio.fixture`, async engines use `NullPool`, schema setup uses `alembic upgrade head` not `Base.metadata.create_all`. None of this changes here.\n\n### Modules and what to test\n\n- **`tests/conftest.py` (modified)** — single conftest test (as above) verifies auto-tag and strict-collection behaviour end-to-end.\n- **`Makefile` + `makefiles/*.mk`** — no automated tests. `make help` is the contract; manual verification each target runs is sufficient.\n- **`Dockerfile`** — no automated tests. Manual smoke per the EPIC discussion.\n- **`docker-compose.yml` `app` service** — no automated tests. Manual smoke.\n- **`scripts/docker-entrypoint.sh`** — no automated tests. The shell is trivial; `set -euo pipefail` plus `exec` is the entire logic. A bash unit test would be more code than the script.\n\n## Out of Scope\n\n- Selecting a production compute target (ECS Fargate, EKS, App Runner, Lambda). The Dockerfile is runtime-agnostic; the runtime decision becomes a separate EPIC.\n- Terraform / CDK / IaC for any cloud resource.\n- A CI workflow (GitHub Actions) that runs `make test` on PRs. Worthy follow-up; not bundled here.\n- A `make migrate` target for the host workflow. Worth filing as a follow-up if the user wants it.\n- Caching strategy for the Docker build inside CI (BuildKit cache mounts, registry cache).\n- Hot-reload inside the container (the prod image runs without `--reload`).\n- Linting / typecheck make targets (`make lint`, `make typecheck`). Pre-commit already runs them; `make check` aggregate is intentionally skipped.\n- An automated container smoke test in pytest. Manual verification once during this EPIC instead.\n- Multi-arch builds (linux/amd64 + linux/arm64). Single-arch matching the developer's host is fine.\n- Image registry choice (ECR, GHCR, Docker Hub).\n\n## Further Notes\n\n- **Beads label**: every issue under this EPIC gets `milestone:dx-bundle` so autonomous agents can pick it up. The EPIC itself is also labelled `milestone:dx-bundle`.\n- **Slicing**: three child issues, each independently mergeable. (1) Makefile + markers + conftest. (2) Dockerfile + entrypoint + .dockerignore. (3) Compose `app` service + `.aws.env` flow + docs update. The third issue depends on the second; the first is independent.\n- **Unresolved questions remaining from the grilling phase**:\n  - Whether to add a `make migrate` target for the host workflow. Default: no, file as follow-up.\n  - Whether `make cli` should accept arguments via `make cli ARGS=\"...\"`. Default: yes, as documented above.\n  - Whether `make up` should auto-refresh `.aws.env`. Default: no, manual `make aws-creds` first.\n  - Whether to force `API_RELOAD=true` in `make run`. Default: leave env-driven.\n  - Whether `.env.example` should grow `API_HOST` / `API_RELOAD` examples. Default: yes — small change, improves discoverability.\n  - Whether the auto-tag mapping for currently-empty `tests/adapters/` should default to `integration` (because adapters historically need testcontainers) or `unit`. Default: `integration` — it's the safer default given hexagonal adapter responsibilities.\n- **Why no aggregate `make check`**: the user explicitly asked for \"super super super simple\". `pre-commit` covers lint/typecheck on commit; CI will (in a future EPIC) call `make test`. Adding `make check` now is premature.\n- **Why a non-root user in the image**: baseline container security hardening; required by Pod Security Standards \"restricted\" and equivalent ECS hardened patterns. Free to add now, painful to retrofit later.\n- **Why `exec uvicorn` in the entrypoint**: makes uvicorn PID 1, so SIGTERM from `docker stop` reaches uvicorn directly and graceful shutdown works. Without `exec`, the shell traps the signal and uvicorn never sees it until the 10-second kill timer fires.\n- **Future hardening**: when prod runtime is chosen, add a non-root `USER` directive that maps to a UID matching the runtime's expected security context (e.g. EKS restricted PSS), pin base image digests, add a `HEALTHCHECK` directive (currently provided by compose). These are explicitly out of scope here.","notes":"## Codebase Patterns (added 2026-05-10)\n- **Pytest marker policy lives in `tests/marker_policy.py`** as a small importable helper (`PATH_MARKERS`, `apply_path_markers`, `unmarked_node_ids`). The conftest hook in `tests/conftest.py` calls these. This shape lets the pytester behaviour test exercise the real logic via import (in-process `runpytest()`, not `runpytest_subprocess`).\n- **Adding a new test directory** requires editing `tests/marker_policy.py:PATH_MARKERS` to map the new prefix → `unit` or `integration`. Forgetting to do so makes collection fail with `pytest.UsageError` listing the offending nodeids — fail-fast by design.\n- **Single test file at tests/ root** uses explicit `pytestmark = pytest.mark.unit` rather than a root path mapping. If multiple root tests appear, prefer adding a root mapping then.\n- **`pytest_plugins = [\"pytester\"]`** is loaded globally for the suite (top-level conftest requirement). Negligible cost; only relevant for tests using the `pytester` fixture.\n- **`getattr(pytest.mark, name)` trips pyright strict** (`reportUnknownMemberType`). Branch explicitly: `pytest.mark.unit if name == \"unit\" else pytest.mark.integration`.\n## Codebase Patterns (added 2026-05-10)\n- **Hatchling reads README.md at install time**: `pyproject.toml` has `readme = \"README.md\"`. Any builder/installer step that does `uv sync --frozen --no-dev` (without `--no-install-project`) will fail with `OSError: Readme file does not exist: README.md` if README.md is excluded from the build context or not COPY'd into the image.  Either keep README.md out of `.dockerignore` and COPY it before the second `uv sync`, or use a no-readme strategy in pyproject (out of scope).\n- **Two-step `uv sync` pattern**: First call `uv sync --frozen --no-dev --no-install-project` after copying just `pyproject.toml` + `uv.lock` (cached dep layer). Second call `uv sync --frozen --no-dev` after copying `src/`, `alembic/`, `alembic.ini`, `README.md` (project-install layer, invalidates only on code/readme change).\n- **Image runtime layout**: `/app/.venv` (deps), `/app/src` (code), `/app/alembic` + `/app/alembic.ini` (migrations), `/app/scripts/docker-entrypoint.sh`. PATH starts with `/app/.venv/bin` so `uvicorn` and `alembic` resolve without `uv run`.\n- **`useradd --system --create-home --uid 1000 convfinqa`** works on Debian even though `--system` typically picks UID \u003c 1000 — explicit `--uid` overrides. The created GID is system-range (e.g. 999) which is fine.\n- **`docker history --no-trunc` audit**: confirmed no `.git`, `tests/`, `.env*`, `.aws.env`, `.pytest_cache`, `.ruff_cache`, `.mypy_cache`, `data/`, `.beads/` end up in any layer.\n## Codebase Patterns (added 2026-05-10)\n- **Compose `env_file` long form for optional files**: `env_file: [{path: .aws.env, required: false}]` is the compose v2.24+ syntax that lets a missing file be silently skipped. The shorter list-of-strings form (`env_file: [.aws.env]`) hard-fails when the file is absent — bad UX for a freshly-cloned repo.\n- **`make aws-creds` syntax**: `aws configure export-credentials --profile $${AWS_PROFILE:-sandbox-admin} --format env-no-export \u003e .aws.env`. Doubled `$$` so Make passes literal `${...}` to the shell. `env-no-export` produces bare `KEY=value` lines (no `export` prefix), exactly what compose `env_file` expects. Re-run when SSO session expires (~1 hour).\n- **Health route is `/healthz`** (`src/convfinqa/entrypoints/api/health.py:6`). Don't write `/health` in docs/smoke tests.\n- **Compose `app` service overrides only what differs from host**: `API_HOST=0.0.0.0` (vs `Settings.api_host=127.0.0.1` default) and `DATABASE_URL=...@postgres:5432/...` (vs `localhost`). Everything else inherits from `Settings` defaults / env. Don't duplicate values that match the defaults.\n- **Compose's app uses container hostname `postgres`** (the compose service name), NOT `localhost`. Host workflow keeps `localhost:5432`. The two flows MUST stay independently runnable.","status":"closed","priority":2,"issue_type":"epic","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T13:00:57Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T13:49:46Z","closed_at":"2026-05-10T13:48:58Z","close_reason":"All children completed","labels":["afk","milestone:dx-bundle"],"comments":[{"id":"019e121a-3870-7392-af43-d05d592d6e5d","issue_id":"convfinqa-s3m","author":"Francesco Albanese","text":"# 2026-05-10 (milestone:dx-bundle)\n- **Task completed**: Slice 1 — Makefile run/test targets + pytest marker scheme (convfinqa-s3m.1)\n- **Files changed**: pyproject.toml, Makefile, makefiles/test.mk, makefiles/run.mk, tests/conftest.py, tests/marker_policy.py, tests/test_marker_policy.py\n- **Decisions made**:\n  - Extracted marker policy (PATH_MARKERS + apply/unmarked helpers) into tests/marker_policy.py instead of keeping the logic inline in tests/conftest.py. This lets the pytester behaviour test exercise the *real* policy via import, not a duplicate.\n  - Used explicit `pytest.mark.unit if ... else pytest.mark.integration` branching instead of `getattr(pytest.mark, name)` to satisfy pyright strict.\n  - Added two pytester behaviour tests (unmapped fails, mapped passes). The mapped test is one beyond the spec but cheaply guards against the policy over-flagging files in mapped paths.\n  - Used explicit `pytestmark = pytest.mark.unit` on tests/test_marker_policy.py rather than adding a \"root → unit\" path mapping (keeps the mapping minimal and the choice explicit).\n  - `pytest_plugins = [\"pytester\"]` lives in tests/conftest.py (top-level required by pytest).\n  - Synthetic INI in pytester sets `asyncio_mode = auto` and `asyncio_default_fixture_loop_scope = session` to silence pytest-asyncio deprecation warnings inside the subprocess.\n- **Architectural decisions**:\n  - Marker policy is `Final[dict[str, str]]` keyed by relative path prefix → marker name. Longest-prefix match wins (sorted desc by length) so `entrypoints/api` beats `entrypoints/cli` if both ever applied.\n  - Hook raises `pytest.UsageError` (not assert) so collection fails cleanly.\n- **Verification**:\n  - `make test-unit` → 3 passed, 15 deselected, 0.08s, no Docker.\n  - `make test-integration` → 15 passed, 3 deselected, 3.42s.\n  - `make test` → unit then integration sequentially (Make halts on first failure).\n  - `make cli` (no args) → Typer help, exit 0.\n  - `make cli ARGS=\"--help\"` → Typer help, exit 0.\n  - `make help` lists every target.\n  - `uv run --frozen ruff check .` and `uv run --frozen pyright` → clean.\n- **Notes for next iteration (Slice 2 — Dockerfile)**:\n  - `make up`, `make down`, `make aws-creds` deferred to Slice 3 per PRD (no `makefiles/docker.mk` shipped here).\n  - Docs sweep (CLAUDE.md, docs/how-to-run-the-app.md) belongs to Slice 3.\n  - Slice 2 (Dockerfile + entrypoint + .dockerignore) is independent of Slice 1 and unblocked.\n- **Blockers encountered**: None.","created_at":"2026-05-10T13:36:09Z"},{"id":"019e1220-c135-789b-b718-2df868435e5a","issue_id":"convfinqa-s3m","author":"Francesco Albanese","text":"# 2026-05-10 (milestone:dx-bundle)\n- **Task completed**: Slice 2 — Production Dockerfile + entrypoint + .dockerignore (convfinqa-s3m.2)\n- **Files added**: Dockerfile, .dockerignore, scripts/docker-entrypoint.sh\n- **Decisions made**:\n  - Did NOT exclude README.md from .dockerignore as the slice spec originally specified. Reason: pyproject.toml has `readme = \"README.md\"` and hatchling validates the readme during `uv sync --frozen --no-dev` (the second sync, which installs the project itself). Excluding it caused `OSError: Readme file does not exist: README.md`. Spec gap; included LICENSE, AGENTS.md, CLAUDE.md, .pre-commit-config.yaml, .python-version exclusions verbatim.\n  - Also added `COPY alembic.ini README.md ./` (combined with alembic.ini line) so the project-install layer has the readme without invalidating the dep-install cache layer.\n  - Used `COPY --from=builder --chown=convfinqa:convfinqa /app /app` so the runtime image owns everything as the non-root user. Entrypoint copy gets the same chown.\n  - Hard-coded `--host 0.0.0.0 --port 8000` in the entrypoint per slice spec; documented WHY inline (fixed container network contract; keeps Settings.api_host=127.0.0.1 default for host workflow).\n- **Architectural decisions**:\n  - Two-stage build: `ghcr.io/astral-sh/uv:python3.13-bookworm-slim` builder → `python:3.13-slim-bookworm` runtime. PATH=`/app/.venv/bin:$PATH` so uvicorn/alembic resolve directly.\n  - Two-step `uv sync` (deps then project) keeps dep cache layer warm across code-only edits.\n  - `exec uvicorn …` so uvicorn is PID 1 and SIGTERM reaches it for graceful shutdown.\n  - No `HEALTHCHECK` in image; compose owns it (Slice 3).\n- **Verification**:\n  - `docker build -t convfinqa:local .` → succeeds.\n  - `docker images convfinqa:local --format '{{.Size}}'` → 591MB. Above the aspirational 350MB target but expected for litellm + boto3 + sqlalchemy + langfuse. .venv alone is 287MB (litellm bundles many provider SDKs).\n  - `docker run --rm --entrypoint /bin/bash convfinqa:local -c 'id'` → `uid=1000(convfinqa) gid=999(convfinqa)`.\n  - `docker run --rm --entrypoint /bin/bash convfinqa:local -c 'cat /app/scripts/docker-entrypoint.sh'` → script intact, `set -euo pipefail` + alembic + exec uvicorn.\n  - `docker run --rm --entrypoint /bin/bash convfinqa:local -c 'find /app -maxdepth 2 \\\\( -name .git -o -name tests -o -name .env\\* -o -name .aws.env -o -name .pytest_cache -o -name .ruff_cache -o -name .mypy_cache -o -name data -o -name .beads \\\\)'` → empty (no leaked excluded paths).\n  - `uv run --frozen ruff check .` and `uv run --frozen pyright` → clean (no Python source touched).\n  - `uv run --frozen pytest -m unit -q tests/` → 3 passed, 15 deselected.\n- **Notes for next iteration (Slice 3 — compose app + AWS creds + docs)**:\n  - The .dockerignore and Dockerfile are runtime-agnostic; compose file in Slice 3 layers the `app` service on top.\n  - Add .aws.env to .gitignore as part of Slice 3 (per slice 3 spec).\n  - When wiring compose's `app` service, set `API_HOST=0.0.0.0` env (compose env_file or environment) for symmetry — but the entrypoint already binds 0.0.0.0:8000 directly so it is informational only.\n  - End-to-end smoke (compose up → curl :8000/health) belongs to Slice 3.\n- **Blockers encountered**: README.md exclusion broke hatchling project install. Resolved inline by un-ignoring it and adding it to the project-install COPY layer. Documented above as a spec deviation.","created_at":"2026-05-10T13:43:17Z"},{"id":"019e1226-71b7-7576-8af5-dcff25aca643","issue_id":"convfinqa-s3m","author":"Francesco Albanese","text":"# 2026-05-10 (milestone:dx-bundle)\n- **Task completed**: Slice 3 — compose `app` service + AWS creds flow + docs (convfinqa-s3m.3)\n- **Files changed**: docker-compose.yml, .gitignore, makefiles/docker.mk, Makefile, CLAUDE.md, docs/how-to-run-the-app.md\n- **Decisions made**:\n  - Used compose `env_file: [{path: .aws.env, required: false}]` (the long form supported in compose v2.24+) so first-time `docker compose up` does not error when the developer hasn't yet run `make aws-creds`. The shorter `env_file: [.aws.env]` fails hard if the file is missing.\n  - Health endpoint is `/healthz` (existing route at `src/convfinqa/entrypoints/api/health.py:6`), not `/health` as the slice spec said. Used `/healthz` everywhere in docs and smoke verification — single source of truth wins.\n  - `.gitignore` insertion grouped under \"Virtual environments\" (next to `.env`) rather than a new \"Credentials\" section — keeps related ignores together; `.aws.env` is morally an env-file flavor.\n  - Did NOT widen scope to `.env.example` even though the EPIC marked it as a \"default yes\" unresolved question — kept slice 3 strictly to its acceptance criteria. File a follow-up if wanted.\n  - Did NOT change the existing CLAUDE.md \"Testing locally\" SSO instructions block — the new Build \u0026 Test references `make aws-creds` and the docs explain prerequisites; no need to duplicate.\n- **Architectural decisions**:\n  - `aws-creds` Make target uses `$${AWS_PROFILE:-sandbox-admin}` (doubled `$` so Make passes `${...}` to the shell), and `--format env-no-export` so the output is exactly the `KEY=value` lines compose `env_file` expects.\n  - `up` is `docker compose up -d --build` (always builds; no surprise that a stale image is reused after a code change). The dep-cache layer in the Dockerfile already keeps incremental rebuilds fast.\n  - Did NOT auto-trigger `aws-creds` from `up`. Per the EPIC discussion, manual refresh keeps the credential lifecycle explicit and avoids overwriting a freshly-rotated `.aws.env`.\n- **Verification (manual smoke)**:\n  - `make help` → all docker targets listed with descriptions.\n  - `docker compose config` → resolves cleanly; `env_file` silently dropped when `.aws.env` absent (expected; `required: false`).\n  - `make up` → image built (cache hits across the board after slice 2), postgres healthy, app started.\n  - `docker logs convfinqa-app` → `alembic.runtime.migration` logs, then `Uvicorn running on http://0.0.0.0:8000`. uvicorn is PID 1 (`Started server process [1]`).\n  - `curl -fsS http://localhost:8000/healthz` → `{\"status\":\"ok\"}`.\n  - `docker exec convfinqa-app id` → `uid=1000(convfinqa) gid=999(convfinqa)` (non-root, matches slice 2 contract).\n  - `make down` → both containers stopped + removed, network removed.\n  - `make up` (second run) → idempotent; alembic upgrade head is a no-op on the already-migrated DB; `/healthz` still 200.\n  - `make down` (final) → clean teardown.\n  - `aws sts get-caller-identity` failed (no live SSO session) so `make aws-creds` itself was NOT exercised end-to-end against AWS — but the syntax matches `aws configure export-credentials --help` and the Make target wiring is identical to the working slice-2 patterns.\n  - `uv run --frozen ruff check .` → clean. (`ruff format --check` reports drift on chat.py + main.py — pre-existing, no Python touched in this slice.)\n- **Notes for next iteration**:\n  - EPIC is closed (all 3 slices delivered). Future agents should pick up items filed under a new EPIC.\n  - Open question worth filing: should `.env.example` grow `API_HOST` / `API_RELOAD` examples (the EPIC's default-yes unresolved question)? Trivial follow-up; not blocking.\n  - Pre-existing format drift in `src/convfinqa/entrypoints/api/chat.py` and `src/convfinqa/main.py` exists. Likely worth a one-liner `uv run ruff format` commit on a separate branch.\n- **Blockers encountered**: None. Sandbox blocked Docker socket; resolved by re-running with `dangerouslyDisableSandbox=true` for compose commands only.","created_at":"2026-05-10T13:49:30Z"}],"dependency_count":0,"dependent_count":0,"comment_count":3}
{"_type":"issue","id":"convfinqa-1n6","title":"Drop src. import prefix; align with installed package name","description":"Console script uv run main fails with ModuleNotFoundError: No module named 'src' because pythonpath=['.'] is pytest-only and the wheel installs src/convfinqa as convfinqa. Rewrite all from src.convfinqa.* imports to from convfinqa.*, drop pytest pythonpath, update .claude/rules/python/imports.md.","status":"closed","priority":2,"issue_type":"bug","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T00:11:54Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T00:13:42Z","started_at":"2026-05-10T00:11:58Z","closed_at":"2026-05-10T00:13:42Z","close_reason":"Rewrote 18 files from src.convfinqa.* to convfinqa.*; updated pyproject (pytest pythonpath=['src'], pyright extraPaths=['src']); updated .claude/rules/python/imports.md and dependency-injection.md examples; verified with uv sync, pyright (0 errors), ruff (clean), pytest (11/11).","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-tvq.4","title":"Typer CLI REPL: convfinqa chat","description":"## Parent PRD\n\nEpic convfinqa-tvq\n\n## What to build\n\nInteractive Typer subcommand `convfinqa chat` that streams tokens from /v1/chat/stream. Defaults --user-id to os.getenv('USER','dev-user'); default --base-url=http://localhost:8000. REPL prompt '\u003e '. /exit or Ctrl-D leaves; /new resets conversation_id mid-session. Uses httpx.AsyncClient.stream('POST', ...) and parses 'data: ...\\n\\n' SSE frames; dispatches by type; prints text-delta with flush=True; captures data-conversation.data.conversationId.\n\nIncludes:\n- src/convfinqa/entrypoints/cli/app.py with Typer app + chat subcommand.\n- pyproject [project.scripts] convfinqa = convfinqa.entrypoints.cli.app:app (or similar).\n- tests/cli/test_smoke.py: convfinqa chat --help exits 0.\n\n## User stories addressed\n\n3, 19, 20","acceptance_criteria":"- [ ] uv run convfinqa chat --help exits 0 and shows --user-id/--base-url/--system flags\n- [ ] uv run convfinqa chat --user-id me starts a REPL with '\u003e ' prompt\n- [ ] Typed message streams token deltas to terminal (flushed)\n- [ ] /new resets conversation_id; subsequent message creates a fresh conversation\n- [ ] /exit and Ctrl-D both leave cleanly\n- [ ] DB shows distinct conversation_ids before and after /new","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-09T22:52:43Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T00:49:11Z","started_at":"2026-05-10T00:41:38Z","closed_at":"2026-05-10T00:49:11Z","close_reason":"Closed","labels":["afk","milestone:chat-slice-1"],"dependencies":[{"issue_id":"convfinqa-tvq.4","depends_on_id":"convfinqa-tvq","type":"parent-child","created_at":"2026-05-09T23:52:43Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-tvq.4","depends_on_id":"convfinqa-tvq.2","type":"blocked-by","created_at":"2026-05-09T23:52:43Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-tvq.3","title":"Concurrent-request 409 lock (pg advisory)","description":"## Parent PRD\n\nEpic convfinqa-tvq\n\n## What to build\n\nPrevent two in-flight requests on the same conversation_id from interleaving via Postgres advisory lock with try-acquire semantics. If busy, return 409 application/problem+json with type https://convfinqa.local/problems/conversation-busy (sync) or domain ConcurrentRequest event mapped to 409 (stream).\n\nIncludes:\n- In SendMessageUseCase.stream(), after persisting the user message and before the LLM call, open a transaction and call pg_try_advisory_xact_lock(hashtextextended(conversation_id, 0)). On false -\u003e yield ConcurrentRequest event.\n- API maps ConcurrentRequest to 409 problem-details body in BOTH sync and stream presenters.\n- The user message is persisted OUTSIDE this lock (its own transaction) so input is preserved even if a concurrent attempt is rejected.\n- Test: two concurrent /v1/chat/stream requests against the same conversation_id; the first holds the LLM open via FakeLLMPort blocking primitive; the second returns 409.\n\n## User stories addressed\n\n(supports cross-cutting concurrency safety for stories 5, 6 — server-stateful integrity)","acceptance_criteria":"- [ ] Two parallel POST /v1/chat/stream against the same conversation_id: second returns 409\n- [ ] 409 body is application/problem+json with type=conversation-busy and status=409\n- [ ] User message of the rejected attempt is NOT persisted (lock check happens after first user-msg persist of the holding request, but new arrival's user msg... clarify: per PRD, user msg persisted outside lock means each attempt's user msg lands; verify behavior matches PRD wording)\n- [ ] Lock auto-releases on transaction commit/rollback (no leak on crash)\n- [ ] Test simulates worker crash mid-lock and verifies next request succeeds","status":"closed","priority":2,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-09T22:52:31Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T00:39:47Z","started_at":"2026-05-10T00:20:15Z","closed_at":"2026-05-10T00:39:47Z","close_reason":"Closed","labels":["afk","milestone:chat-slice-1"],"dependencies":[{"issue_id":"convfinqa-tvq.3","depends_on_id":"convfinqa-tvq","type":"parent-child","created_at":"2026-05-09T23:52:30Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-tvq.3","depends_on_id":"convfinqa-tvq.2","type":"blocked-by","created_at":"2026-05-09T23:52:30Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-tw6","title":"Rename ConversationCreated event to ConversationResolved (or split)","description":"The use case yields ConversationCreated for both newly-created AND existing-resolved conversations. Misleading name — pre-existing in slice 1, made more visible by tvq.3 putting it inside the lock scope. Either rename to ConversationResolved or split into two events (Created vs Resumed) so the SSE presenter and any future client can distinguish them.","status":"closed","priority":3,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-10T00:38:37Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T00:52:43Z","started_at":"2026-05-10T00:50:07Z","closed_at":"2026-05-10T00:52:43Z","close_reason":"Closed","labels":["milestone:chat-slice-1"],"dependencies":[{"issue_id":"convfinqa-tw6","depends_on_id":"convfinqa-tvq","type":"parent-child","created_at":"2026-05-10T01:38:50Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"convfinqa-tvq.5","title":"Docs: how-to-run-the-app.md + CLAUDE.md build/test commands","description":"## Parent PRD\n\nEpic convfinqa-tvq\n\n## What to build\n\nDocument the local dev workflow so a new dev (or fresh agent) can stand the stack up trivially.\n\nIncludes:\n- docs/how-to-run-the-app.md with the aws-login -\u003e switch-aws-env sandbox-admin flow and one verification line: AWS_PROFILE=sandbox-admin aws sts get-caller-identity. Plus the docker compose / alembic / uvicorn / curl walkthrough mirroring the PRD verification section.\n- CLAUDE.md Build \u0026 Test section filled in (replace the placeholder block with real commands: uv sync, docker compose up -d postgres, uv run alembic upgrade head, uv run uvicorn ..., uv run pytest -q tests/).\n\n## User stories addressed\n\n27 (and supports onboarding for all stories)","acceptance_criteria":"- [ ] docs/how-to-run-the-app.md exists and contains the aws-login + verification line\n- [ ] docs/how-to-run-the-app.md walks through docker compose up, alembic upgrade head, uvicorn boot, and the cURL verification calls\n- [ ] CLAUDE.md Build \u0026 Test section replaced with real commands (no placeholder remains)","status":"closed","priority":3,"issue_type":"task","assignee":"Francesco Albanese","owner":"albanesefrancesco.af@gmail.com","created_at":"2026-05-09T22:52:57Z","created_by":"Francesco Albanese","updated_at":"2026-05-10T00:56:07Z","started_at":"2026-05-10T00:53:20Z","closed_at":"2026-05-10T00:56:07Z","close_reason":"Closed","labels":["afk","milestone:chat-slice-1"],"dependencies":[{"issue_id":"convfinqa-tvq.5","depends_on_id":"convfinqa-tvq","type":"parent-child","created_at":"2026-05-09T23:52:56Z","created_by":"Francesco Albanese","metadata":"{}"},{"issue_id":"convfinqa-tvq.5","depends_on_id":"convfinqa-tvq.1","type":"blocked-by","created_at":"2026-05-09T23:52:56Z","created_by":"Francesco Albanese","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"memory","key":"bedrock-claude-haiku-4-5-from-london-eu","value":"Bedrock Claude Haiku 4.5 from London (eu-west-2): not in-region (only eu-north-1, eu-west-1 host directly), but eu-west-2 IS a valid SOURCE for the EU cross-region inference profile bedrock/eu.anthropic.claude-haiku-4-5-20251001-v1:0. The 'global.' profile also accepts eu-west-2. Source: AWS Haiku 4.5 model card + inference-profile support matrix. Don't 'correct' eu-west-2 to eu-west-1 — both work, eu-west-2 keeps boto3 calls in London while CRIS routes the inference."}
{"_type":"memory","key":"asyncpg-pytest-asyncio-cannot-perform-operation-another-oper","value":"asyncpg + pytest-asyncio: 'cannot perform operation: another operation is in progress' on second test means a session-scoped async fixture is bound to a different event loop than the per-test loop. Fix: set asyncio_default_fixture_loop_scope=session in pyproject [tool.pytest.ini_options] AND pass loop_scope='session' on every @pytest_asyncio.fixture AND use poolclass=NullPool on test engines. Verified in convfinqa slice 1."}
{"_type":"memory","key":"convfinqa-settings-singleton-anti-pattern-src-convfinqa-conf","value":"convfinqa SETTINGS singleton anti-pattern: src/convfinqa/config.py:SETTINGS exists ONLY for the lifespan to bootstrap the container once. Inside route handlers, use cases, and adapters NEVER import SETTINGS — inject Settings via container/DI dependency (entrypoints/api/dependencies.py:SettingsDep). Reading SETTINGS directly defeats Container.for_testing(settings=...) overrides and breaks tests with custom configs. Caught in slice 1 quality gate."}
{"_type":"memory","key":"convfinqa-single-generator-use-case-is-the-load","value":"convfinqa single-generator use case is the load-bearing design choice for /v1/chat + /v1/chat/stream. SendMessageUseCase exposes ONE public method: stream() -\u003e AsyncIterator[StreamEvent]. The sync presenter consumes the same generator and assembles a snapshot; the streaming presenter emits SSE frames from the same events. NEVER add a parallel complete() method — that's the drift the architecture exists to prevent. PRD called this out explicitly."}
{"_type":"memory","key":"convfinqa-streaming-disconnect-test-pattern","value":"convfinqa streaming disconnect test pattern: httpx ASGITransport CANNOT mid-stream-disconnect (it runs the ASGI app to completion regardless of client.stream context exit). Solution: test the GeneratorExit handler at the use-case layer with await events.aclose(). This is sound because Starlette's StreamingResponse cancels by calling aclose() on the body iterator on real client disconnect — same code path. Don't try to make ASGITransport do mid-stream disconnect; spin up a real server only if you need end-to-end network behaviour."}
{"_type":"memory","key":"convfinqa-hexagonal-layering-audit-run-before-merge-grep","value":"convfinqa hexagonal layering audit (run before merge): grep -rE 'fastapi|sqlalchemy|litellm|pydantic_settings|pythonjsonlogger' src/convfinqa/domain/ should be empty; grep -rE 'fastapi|sqlalchemy|litellm' src/convfinqa/application/ should be empty; grep -rE 'convfinqa\\.application|convfinqa\\.entrypoints' src/convfinqa/adapters/ should be empty; grep -rE 'convfinqa\\.adapters' src/convfinqa/entrypoints/ should be empty (composition lives in container.py)."}
{"_type":"memory","key":"litellm-strict-pyright-pattern-litellm-acompletion-is-poorly","value":"LiteLLM strict-pyright pattern: litellm.acompletion is poorly typed (returns Any | ModelResponse | CustomStreamWrapper, chunk attrs set in __init__). Solution: define small Protocols for the attributes you actually consume (Delta, Choice, Usage, Chunk), wrap the call in a private _open_stream() that does ONE cast(AsyncIterable[_LiteLLMChunk], response), and use # pyright: ignore[reportUnknownVariableType,reportUnknownMemberType] (BOTH codes — single-code ignores leak). The iteration body is then fully typed. See src/convfinqa/adapters/llm/litellm_adapter.py."}
{"_type":"memory","key":"pytest-marker-auto-tag-pattern-extract-the-path","value":"Pytest marker auto-tag pattern: extract the path→marker policy (PATH_MARKERS dict + apply/unmarked helpers) into tests/marker_policy.py rather than inlining in conftest. The conftest hook calls the helpers, AND the pytester behaviour test imports them and runs the real logic via runpytest() (in-process). Avoids duplicating the policy in a synthetic conftest. Use explicit branching (pytest.mark.unit if name == 'unit' else pytest.mark.integration), not getattr(pytest.mark, name) — the latter trips pyright strict reportUnknownMemberType."}
{"_type":"memory","key":"shadcn-cli-v4-7-monorepo-gate-resolved-2026","value":"shadcn CLI v4.7 monorepo gate (resolved 2026-05-11 in convfinqa-0hu.10): frontend/pnpm-workspace.yaml MUST NOT contain a 'packages:' key under the current single-package layout. Keep only 'minimumReleaseAge: 10080' + 'blockExoticSubdeps: true'. With that, 'pnpm dlx shadcn@latest add \u003ccomponent\u003e' works directly with no workaround. Re-add 'packages:' only if a true multi-package frontend workspace is introduced (e.g. apps/web + packages/ui); then invoke shadcn with '-c \u003csubdir\u003e'. Rule lives in .claude/rules/frontend/package-manager.md."}
{"_type":"memory","key":"convfinqa-postgres-image-pin-rationale-docker-compose-yml","value":"convfinqa Postgres image pin rationale: docker-compose.yml uses postgres:18.3-bookworm. Don't change to: latest (moves with releases), bare 18.3 (default Debian base may flip), trixie (Debian 13, less mileage with asyncpg), alpine (musl, occasional asyncpg/locale edge cases). Bookworm = Debian 12, what testcontainers + most CI stacks use."}
{"_type":"memory","key":"convfinqa-streaming-sse-presenter-pattern","value":"convfinqa AI SDK v5 UI Message Stream presenter at src/convfinqa/entrypoints/api/sse.py: one match-stmt over StreamEvent yields 'data: \u003cjson\u003e\\\\n\\\\n' frames. Wire shapes (verified against ai-sdk.dev): start carries messageId, text-delta uses field name 'delta' NOT 'textDelta', data-* parts nest payload under 'data' key, terminator is 'data: [DONE]\\\\n\\\\n'. Frame order: start, data-conversation, text-start, text-delta*, text-end, finish, [DONE]. Error path: error frame + [DONE] (no text-end, no finish). Buffers ConversationCreated until MessageStarted arrives so start-with-messageId fires first."}
{"_type":"memory","key":"convfinqa-timestamp-single-source-of-truth-when-a","value":"convfinqa timestamp single-source-of-truth: when a use case persists a row with a created_at and the presenter needs that same timestamp in its response, the use case MUST surface it (e.g. via the Finish event). Calling datetime.now(UTC) in both the use case AND the presenter produces millisecond drift between the DB row and the HTTP body — flaky tests + inconsistent client UX. Caught in slice 1 quality gate."}
{"_type":"memory","key":"files-matching-env-including-env-example-env-local","value":"Files matching '.env' (including .env.example, .env.local, etc.) are blocked from Read AND Bash cat by ~/.claude/hooks/protect-files.sh and block-cat-sensitive.sh. Workarounds: 1) read committed content via 'git show HEAD:.env.example'; 2) ask the user to apply changes manually and surface the diff. Edit/Write tools also fail because they require Read first. Don't try to bypass — surface to the user."}
{"_type":"memory","key":"frontend-package-manager-always-pnpm-never-bun-npm","value":"FRONTEND PACKAGE MANAGER: ALWAYS pnpm, NEVER bun/npm/yarn. Pinned by /Users/francescoalbanese/Documents/Development/convfinqa/.claude/rules/frontend/package-manager.md. Supply-chain: minimumReleaseAge=10080 + blockExoticSubdeps=true in pnpm-workspace.yaml (or .npmrc equivalent). CI uses pnpm/action-setup@v4. The convfinqa-0hu epic was wrongly scaffolded with Bun on 2026-05-10; remediation tracked under convfinqa-0hu.9 (blocks all sibling .2-.8). Before adding any frontend dep or scaffolding ANY JS workspace, re-read this rule."}
{"_type":"memory","key":"logrecord-message-keyerror-logger-log-level-msg-extra","value":"LogRecord 'message' KeyError: logger.log(level, msg, extra={'message': '...'}) raises KeyError because 'message' is a LogRecord built-in. Same goes for: asctime, name, levelname, module, funcName, lineno, pathname, filename, created, msecs, relativeCreated, thread, threadName, process, processName, args, exc_info, exc_text, stack_info. Use 'exc_message' instead of 'message' for 'the exception's message' field."}
