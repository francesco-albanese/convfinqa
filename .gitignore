# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class


# Virtual environments
.env
.aws.env
.venv

# Jupyter Notebook
.ipynb_checkpoints

# macOS
.DS_Store

.*cache

# Rogue pnpm store (created if pnpm runs outside frontend/ with no global store-dir)
/.pnpm-store/
**/.pnpm-store/

# Coverage artefacts
.coverage
.coverage.*
htmlcov/
coverage.xml
frontend/tests/live/.auth/



# Beads / Dolt files (added by bd init)
backend/scripts/ralph

# Local design references (large HTML mockups — referenced by agents locally)
example-html/

# Personal scratchpad
TODO.md

scripts/ralph

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Lambda build outputs (produced by `make build-lambdas`, zipped at TF plan
# time via data.archive_file — must NEVER be committed)
auth-lambda/dist/
terraform/environmental/modules/**/*.zip
terraform/environmental/modules/**/.build/
terraform/environmental/modules/keepalive/build/

# Ralph runtime
.ralph
